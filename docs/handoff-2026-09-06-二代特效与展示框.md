# 交接文档：二代特效 + 协议展示框改造（2026-09-06）

> 供新会话/新模型接手使用。本文件只记录**已由代码与会话确认**的事实；未确认项已注明。

## 0. 仓库与环境

- 仓库：`E:\studyE\AI大模型相关资料\自己的项目\compile`（GitHub `foreverin2/Compile-Web`，分支 main）
- 当前 HEAD：`910c3fa`，工作区干净，与 `origin/main` **0 差距**（全部已推送）
- 测试基线：**694/694 通过（76 文件）**；`npx tsc --noEmit` 0 错误；`npx vite build` 通过
- 常用命令（在仓库根执行）：
  - `npx tsc --noEmit` / `npx vitest run` / `npx vite build`
  - dev server（用户 5173 目检）：`npx vite --port 5173 --strictPort`（当前已停止；需要目检时启动，勿随意杀）
- **推送注意（重要）**：本机 git 全局配置了 github 专属代理 `http://127.0.0.1:7890`（`http.proxy`/`https.proxy` + `*.https://github.com.proxy` 四个键）。该代理时常不可用/直连也被拦，push 可能超时（`Failed to connect to github.com port 443`）。实测两种方式交替可成：
  1. 先直接 `git push origin main`（走配置的代理——代理可用时成功）；
  2. 失败则改用直连绕过全部代理键：
  ```
  git -c "http.https://github.com.proxy=" -c "https.https://github.com.proxy=" -c http.proxy= -c https.proxy= push origin main
  ```
  两者都失败 = 网络/代理暂时不可用，稍后重试（可用 `Invoke-WebRequest https://github.com` 与 `-Proxy http://127.0.0.1:7890` 分别探测）。
- 权威文本（只读参考，勿改）：`E:\studyE\compile\正版compile\`（`compile1文本.txt`、`compile2文本.txt`、`compile3文本.txt`、`compileFAQ文本.txt`、`Compile45套协议卡组评分与阵容推荐.txt`、`二代特效提示词.txt`、`修改提示词.txt`）

## 1. 本会话完成的工作（18 笔提交，全部已推送）

### A. 二代 15 套协议特效（11 笔：`edb32f1..00b1653`）
依据 `二代特效提示词.txt`，为 MN02 12 套（luck/mirror/peace/chaos/clarity/ice/smoke/fear/corruption/war/courage/time）+ AX02 3 套（diversity/assimilation/unity）实现：
- **卡牌效果触发特效**（引擎事件驱动 / 常驻状态驱动）
- **已编译特效**：主题色发光外框（呼吸或交替）+ 四角护边 + 周期中心特效（间隔 ≥10s 随机）

关键实现摘要：
| 协议 | 卡牌触发特效 | 已编译特效 |
|---|---|---|
| luck | 宣告骰子（luck-0/3）：转动→成功橙圆+烟花+666/888 / 失败蘑菇云+囧 | 橘色呼吸框 + 中心红黑转盘（铁珠滚动停格：红=烟花/黑=蘑菇云） |
| mirror | mirror-0 链路能量槽银白镜框+镜纹；mirror-1 复制虚影飞向复制者融合闪光 | 银白呼吸框 + 周期镜纹渐实→闪光消散 |
| peace | 弃牌和平鸽（飞落→停→抓卡飞远）+ 延后基础切割 | 海蓝/金币交替框 + 环绕鸽 + 偶停框边休息鸽 |
| chaos | chaos-0 抽对方牌库漩涡；chaos-3 手牌正面紫蓝闪烁+雾气；弃牌漩涡吸入 | 蓝紫呼吸框 + 中心漩涡扩散爆炸 |
| clarity | clarity-0 能量槽粉蓝框+眼睛；clarity-1 揭示幽灵眼睛+圣光；clarity-2/3 牌库眼睛+落点眼睛 | 淡粉淡蓝交替框 + 偶发眼睛圣光 + 中心金色金字塔 |
| ice | 偏转冰面滑道（双滑轨+起点雪花）；ice-1 对手线冰面；ice-4 卡框冰辉；ice-6 牌库封冰 | 深蓝呼吸框 + 雪花 + 周期冰面覆盖 2s |
| smoke | 反面打出落点灰雾罩（卡从雾中现+框灰光 2s）；smoke-2 线框灰光+章鱼触手 | 浓灰呼吸框 + 周期大雾+触手缩回 |
| fear | 偏转橙红覆盖+颤动+慢→快移动；fear-0 对手三链橙红闪烁+覆盖 | 橙红呼吸框 + 染色扩散+颤抖+残影 |
| corruption | 弃牌腐蚀液膜+斑纹+绿光点；删除腐蚀；翻转毒雾；腐化1 召回墨绿虚影 | 墨绿呼吸框 + 液滴渗出 + 周期毒雾骷髅 |
| war | war-0~3 常驻交叉双剑+火星+卡框赤红；被动触发成功战旗；翻转火花；弃牌刀光 | 赤红呼吸框 + 周期双剑互碰 + 战旗硝烟 |
| courage | 抽牌湖中剑斜插+光羽+框鎏金；弃牌金逆焰；删除剑影；偏转金圣光+落点火环 | 鎏金呼吸框 + 周期湖中剑插落+金色波纹 |
| time | 弃牌堆古铜时钟（time0/1/2/3）；time-1 光流；time-2 沙漏；弃牌古铜光膜 | 古铜呼吸框 + 斑驳铜锈 + 周期时钟齿轮 |
| assimilation | 弃牌青碧环收缩化光点；牌库顶反打青碧涟漪 | 青碧呼吸框 + 双光带环绕 + 光珠涟漪 |
| unity | unity 效果触发时与场上其它 unity 卡连携亮蓝光带 | 亮蓝/银白交替框 + 众星拱月 8 光带 + 光核冲击波 |
| diversity | 弃牌彩色光环（conic 5 色）收缩化彩光尘 | 彩色 5 色交替框（**取自场上其它已编译协议边框色**，不足 5 种循环复用）+ 5 面棱镜 |

### B. 协议选择页（草稿）展示框改造（6 笔：`357c9c0..cc6f5a8`）
- 由「中央池下方单个 hover 展示框」改为 **body 级 fixed 大面板 ×2**：P1 屏幕左下 / P2 屏幕右下（`.draft-preview.p1/.p2`）
- 结构：**上图下文**（图逆时针旋转 90° 横置展示，1.4:1 图盒比例法）；面板高度 `min(42vh,420px)`（避免矮窗口顶到顶部 sticky 选择框）
- 交互（最终版）：
  - **hover** 池卡或已选卡 → 即时预览到「当前操作者」侧面板（内容保留，不随移出消失）
  - **单击** → 固定该协议（`draftPinned`）；**固定后 hover 不再覆盖**；点其它卡切换固定；**再点同一卡取消固定**回 hover 自由模式
  - **双击** → openZoom 放大（`bindClickOrDouble` 320ms 判别）
  - 禁用模式：点击禁用前先固定显示该协议详情
  - 已选协议卡（`renderPickColumn`）同样支持上述机制，固定归属该列玩家侧
- **进入游玩页自动移除**（`renderApp` 中 `phase !== 'draft'` → `removeDraftPreviews()`）——修复了面板残留到游玩页的 bug

### C. 图鉴页（`home.ts` renderLibrary）
- 协议封面（loading + compiled）hover 显示**协议详情面板**：名称/座右铭/定位/关键词/6 维评分/点评/推荐搭配/推荐流派（复用 `buildProtocolRatingPanel`）
- 与草稿页**完全相同**的查看机制：hover 即时预览（保留）；单击固定（固定后 hover 不覆盖、点其它切换、再点同条目取消）；双击放大
- 布局：`library-preview-main` 允许 flex-wrap；文本面板 `min-width:240px` → 520px 框内图与详情并排放不下时详情折行到图下方

## 2. 关键代码结构（接手必读）

### `src/ui/fx-gen2.ts`（约 1700 行，本会话新建）
二代全部卡牌触发特效模块。要点：
- 导出 `initGen2Fx()`（main.ts 已注册，订阅 gameBus）、`clearGen2Fx()`（resetToMainInterface 调用，清全部瞬态层）
- 各 `playXxxExtra` 由 `src/ui/effects/index.ts` 的分支调用（peace/chaos/corruption/war/courage/time/assimilation/diversity 弃牌与翻转/偏转等）
- 自订阅事件：`luck:roll`、`card:copied`、`card:returned`（corruption 召回）、`card:deck-played`（time fromTrash / war 战旗）、`time:deck-to-trash`、`card:deleted|discarded`（war 战旗）、`card:drawn`（chaos 漩涡 / clarity 眼睛 / courage 湖中剑 / time 沙漏）、unity 连携（多类 action 事件 + triggerProtocol=unity）
- 层约定：body 级 fixed + `z-index: GEN2_Z(600)`（低于放大遮罩 1000+）；JS setTimeout 自清理；`clearGen2Fx` 兜底清扫
- 导出常量：`PEACE_PRE_MS`、`CHAOS_DISCARD_PRE_MS`、`CORRUPT_DISCARD_PRE_MS`、`ASSIM_DISCARD_PRE_MS`、`DIVERSITY_DISCARD_PRE_MS`（effects 侧延后基础切割用）

### `src/ui/render.ts`（约 5200 行）
- **常驻 sync（每帧渲染 + main.ts scroll/resize）**：`syncMirror0BatteryGlows`、`syncClarity0BatteryGlows`、`syncIceFx`、`syncSmoke2LineGlows`、`syncFear0TriGlows`、`syncWarBlades`、`syncDiversityColors`——均在 `renderBoard` 末尾调用，resetUiState 清表
- **已编译特效**：`buildCompiledFx` → `appendCompiledRing`（通用环骨架）+ `appendNewCompiledFx`（switch 分发各协议 builder：既有 10 套 + 二代 15 套 `appendLuckCompiled` 等）；周期大特效用 `scheduleCompiledLoop`（间隔 ≥10s 随机）、`fxTimer`、`layerGeom`、`appendCompiledCorners`
- **草稿展示框**：`draftPreviewHosts[2]`、`draftPinned`、`buildDraftPreviewBox`、`renderToPreview`、`resetPreviewToHint`、`pinDraftPreview`、`hoverDraftPreview`、`removeDraftPreviews`、`buildProtocolRatingPanel`（已导出，图鉴复用）、`bindClickOrDouble`（已导出）
- `renderApp`：`phase !== 'draft'` → `removeDraftPreviews()`

### 引擎侧小扩展（本会话）
- `luck.ts`：判定点 emit `luck:roll`（成功/失败，payload uid/defId/player/success）
- `resolve.ts`：copyMiddle emit `card:copied`；**所有 emitCardEvent 统一补 `triggerUid: pe.sourceUid`**；playFromHand 补 triggerProtocol；playFromTrash 补 `fromTrash: true`
- `time.ts`：time-1 直改状态后 emit `time:deck-to-trash`
- `types.ts`：`RevealedGhost.fx` 扩为 `'love' | 'clarity'`
- `main.ts`：`initGen2Fx()` / `clearGen2Fx()` 接入；syncPersistentFx 注册新 sync 函数

### `src/ui/styles.css`（约 8200 行）
- 二代 FX 区块按协议追加（`.fx-luck-*`、`.fx-mirror-*` … `.fx-diversity-*`）
- 已编译区块：`.compiled-ring-<defId>`（呼吸/交替 keyframes）、`.protocol.compiled-fx-<defId>`（静态兜底）、`.compiled-<defId>-corner`（护边色 `--cc/--ccg`）、各协议周期特效元素
- 展示框区块：`.draft-preview`（fixed 大面板）、`.draft-preview-fig/-img`（旋转 90° 比例法）、`.library-preview-main/-text`（图鉴，wrap）

## 3. 遗留与待用户验收（非代码缺陷）

1. **用户 5173 目检**：二代 15 套特效观感 + 草稿展示框 + 图鉴机制；dev server 当前停止，需目检时启动
2. **3 代 90 卡效果实测**：用户此前表示「明天再测试」
3. `docs/验收清单-修改提示词55行与3代90卡.md`：用户对照检验表（A 引擎/B UI/C 动画/3代/遗留）
4. 已知近似实现（提示词未强制的深度项）：
   - unity-1 编译光柱吸收删卡、assimilation-4 双光带互抽、diversity-0 六协议光柱汇聚 等「多卡联动」仅做单卡/连携近似
   - 透彻2/3 整副牌库揭示浮层（deckReveals）为既有遗留 UI 项，眼睛图案以选择 prompt 期浮层近似
5. 引擎级待确认：源卡被自己效果落地覆盖 → sourceValid 中断后续句（time-0 测试层已规避）

## 4. 用户偏好与协作约定

- 全程**中文**交流；术语用中文（偏转/阈值/链路/被覆盖/反面打出…）
- 视觉观感由用户 5173 亲自目检；改完 → 本地提交 → 用户目检反馈 → 逐条调整
- 改动完成后应跑 `tsc` + `vitest` + `vite build` 全绿再提交（本会话基线 694/694）
- 提交信息用中文、按批次（`feat(二代特效批N)` / `fix(草稿展示框 vN)`）
- 推送：用户明确说「推送/总结推送」时才推送；推送命令需绕过断掉的本地代理（见 §0）
- 已裁决的关键语义（沿用，勿改）：腐化0 落对方场=易主、腐化6 被盖自毁合法、多元 5 色取自场上、草稿展示框单击固定/双击放大、图鉴同款机制
