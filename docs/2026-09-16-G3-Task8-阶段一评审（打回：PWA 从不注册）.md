# G3 Task 8 阶段一评审

**评审对象**：`0e3fdbe`（`G3 Task 8：PWA`）· 工作树 = `0e3fdbe2d30f289e3b53e33b70c4324b7f138351`，`git status --porcelain` 为空
**评审者权限**：只读 + 临时变异逐字节还原（还原证据见文末）· 未 `git commit`、未 `npm install`、未碰 5173/3080
**复核起点（我自己先跑了一遍基线）**：

```
npx tsc --noEmit                 → exit 0（0 错）
npx vitest run                   → Test Files 134 passed (134) / Tests 1507 passed (1507)
npx vite build                   → ✓ built
npm run build                    → ✓ built … sw-manifest.json：437 个文件，version=e8ed76750a5bca8b
npm run texts:check              → 卡牌文本差异 0 处；协议元数据差异 0 处
```

---

## 结论：**打回**

四道门禁全绿、`sw.js` 的用户数据边界**事实上成立**、"必须由用户点击才刷新"这条判据**真的钉住了**（M1/M2/M3 三条腿全红）——这些都核过了。

但有一条**阻断级缺陷**：`src/ui/pwa-update.ts:159` 从 `globalThis.serviceWorker` 取 SW 容器，而浏览器把容器挂在 **`navigator.serviceWorker`** 上。真实浏览器里 `globalThis.serviceWorker === undefined` ⇒ `sw = null` ⇒ `pwa-update.ts:216` 的 `if (!env.isProd || container === null) return () => {};` 直接返回空卸载函数 ⇒ **`/sw.js` 从不注册**。也就是说：`sw.js`、预缓存清单、438 个文件、manifest、图标、更新条这一整套**在真实浏览器里一个字节都没运行过**，"离线可开"与"自动提示 + 一键更新"两条设计稿要求（§8.1 / §8.4）**均未达成**，而门禁全绿。

同一个缺陷还解释了"边界偏差"：模块作者明确写着，之所以改成 `globalThis.serviceWorker`，是为了**绕开一条文本正则**（见下方#1 的原文引用）。为了迁就文本腿而改掉被测试的正确实现，代价是整个功能失效——这正是"文本腿 > 行为腿"这条本仓纪律的反例。

---

## 阻断级

### B-1 真实浏览器里 PWA 永不注册（`globalThis.serviceWorker` vs `navigator.serviceWorker`）

**位置**

- `src/ui/pwa-update.ts:134-137`（`interface SwHolder { serviceWorker?: SwContainerLike }` — 声明"`globalThis` 上挂着容器"）
- `src/ui/pwa-update.ts:156-165`（`realEnv()`；`:159` 是缺陷行：`sw: holder?.serviceWorker ?? null,`）
- `src/ui/pwa-update.ts:216`（`if (!env.isProd || container === null) return () => {};` — 静默短路点）
- `src/ui/pwa-update.ts:149-155`（注释：作者自陈这是为躲文本腿）
- 权威依据：`docs/2026-09-16-G3-档案格式-实现计划.md:2587-2600` 给的写法是
  `sw: (globalThis.navigator as { serviceWorker?: SwContainerLike } | undefined)?.serviceWorker ?? null,`
- 设计稿：`docs/2026-09-13-联机与多端-设计稿.md:655`（`main.ts` 注册 service worker）、`:679`（`updatefound` → 提示 → `skipWaiting` + reload）

作者自陈的动机（原文，`pwa-update.ts:152-154`）：

> 写成 `(globalThis as unknown as SwHolder | undefined)?.serviceWorker` 而不是经 `globalThis` 上的浏览器全局名字（后者的属性访问形态会让源码里出现第二处"裸访问"，而测试的"注入缝守卫"数的就是这种形态）—— 把唯一出口做成**可数的一个**，守卫才有意义。

**复现命令**（我用的是自己的探针，不改被评审文件；探针保留在 `.superpowers/probe-realenv3.test.ts`）

```powershell
Copy-Item .superpowers\probe-realenv3.test.ts tests\ui\_pwa-review-probe.test.ts
npx vitest run tests/ui/_pwa-review-probe.test.ts
Remove-Item tests\ui\_pwa-review-probe.test.ts
```

**原始输出**（正面对照：只把容器挂在 `navigator.serviceWorker` vs 只挂在 `globalThis.serviceWorker`）

```
probe: 只挂 navigator.serviceWorker  -> []
probe: 只挂 globalThis.serviceWorker -> [["/sw.js",{"scope":"/"}]]
 ✓ tests/ui/_pwa-review-probe.test.ts (1 test) 130ms
 Test Files  1 passed (1)
      Tests  1 passed (1)
```

同一条探针的 print 观察（另一版，已删）：

```
probe: globalThis.navigator 存在? object
probe: (navigator).serviceWorker 存在? object
probe: globalThis.serviceWorker 存在? undefined
probe: register 调用记录 = []
```

**怎么读这组输出**：把容器放在 `navigator.serviceWorker`（**浏览器唯一的真实位置**）上，`register` 一次都没被调用（`[]`）；把它放到 `globalThis.serviceWorker`（浏览器上**不存在**的属性）上，立刻就注册了 `['/sw.js', { scope: '/' }]`。这就证明 `realEnv()` 读的是 `globalThis.serviceWorker`。

**用户可感知的后果**：`sw.js` 从不安装 ⇒ 断网打不开（"离线可开"失败）、`sw-manifest.json` 从不被取、旧 SW 永不被替换、更新条永不出现（"自动提示更新 + 一键更新"失败）、`manifest.webmanifest` 虽然链在 `index.html` 里，但没有 SW 就没有可安装的离线 PWA。

**为什么门禁抓不到**：`tests/ui/pwa-update.test.ts:400-423` 的"可注入性守卫"是**纯文本形状**检查——它只断言 `realEnv` 源码片段里出现 `globalThis`、`serviceWorker`、`?? null`（`:412-414`），以及全文件没有 `navigator.<x>` 的属性访问形态（`:405-407`）。**没有任何一条腿证明 `realEnv()` 真的能从浏览器全局取到容器**（证据见下方"假守卫清单" M7）。

**建议修法**（留给修复轮，一行）：把 `:159` 改回计划里的 `(globalThis as unknown as { navigator?: SwHolder }).navigator?.serviceWorker ?? null`；并把 `:152-154` 那段"为躲文本腿"的注释删掉——它记录的是一个错误取舍。同时把那句无依据的注释改掉（见"重要" I-2）。

---

## 重要

### I-1 "注入缝"守卫没有运行时腿 ⇒ `realEnv` 可以任意坏掉而门禁全绿（**假守卫**）

`tests/ui/pwa-update.test.ts:400-415` 的判据全是文本。我用两条变异分开证实了这一点：

| 变异 | 内容 | 结果 |
|---|---|---|
| **M7** | `:157` `const holder = globalThis as unknown as SwHolder \| undefined;` → `const holder = undefined as unknown as SwHolder \| undefined;`（**保留** `globalThis`/`serviceWorker`/`?? null` 三个词的另一处出现，只破坏活取值路径） | **全绿 39/39**（假守卫） |
| **M7b** | 同上但连文本一起删掉 | 红 `expected 'function realEnv(): PwaEnv {\n  const…' to match /serviceWorker/` |

```
▶ 已注入变异 M7 → src/ui/pwa-update.ts
── 输出摘要（exit 0） ──
 Test Files  1 passed (1)
      Tests  39 passed (39)
── 还原 ──
  src/ui/pwa-update.ts sha256=a4c5e809a279c581fa7a4d52e6e82f4668458efcc679600a86d1a45716134435 ✓ 与备份逐字节一致
```

M7b 红的那条也是"靠别的字符串撞上的"（`:412` 的 `/serviceWorker/`），不是靠行为。**B-1 就是在这个缺口里发生的**：如果当时有一条"给 `globalThis.navigator` 装假容器、不传 `sw` override、看是否注册"的行为腿，B-1 在实现当场就会红。这条腿我已经写好可直接用（`.superpowers/probe-realenv3.test.ts`）。

### I-2 共享测试基建的边界偏差：`default` 导出那一半**应收回**，注释里的理由**实测不成立**（疑点 3 的实测回答）

**实测的两条路**（全程只改 `scripts/gen-sw-manifest.mjs` + `tests/node-types.d.ts`，测完逐字节还原）：

**路 ①：不改任何东西，只把 `.mjs` 换成默认导入（证明 `.d.ts` 管不到它）**

```
▶ 已注入变异 M8a → scripts/gen-sw-manifest.mjs
  注入后 sha256: scripts/gen-sw-manifest.mjs=67c09854067e3efb22f99274cf1f084d0a2fe63fc46e89d4cb6063274b566327
── 输出摘要（exit 0） ──
 Test Files  1 passed (1)
      Tests  39 passed (39)
── 还原 ──
  scripts/gen-sw-manifest.mjs sha256=817006830f19d8b6dc2c5e5c63827344e09b7de2f8e5a69e7107f2d7168568e9 ✓ 与备份逐字节一致
```

再用同一变异跑 `tsc`：**exit 0**。原因是 `scripts/` 不在 `tsconfig.json:11` 的 `include`（只有 `src`/`tests`/`vite.config.ts`）里，**`.mjs` 根本不进 `tsc`**，所以"`readFileSync` 在生成脚本里是 `undefined`"这件事**在类型层没有任何机制能造成**——`.d.ts` 只描述 `tests/ui/pwa-update.test.ts` 看到的出口形状。

**路 ②：`.mjs` 用具名导入 + 把 `node-types.d.ts` 里的 `default` 全部删掉（一条注入 + 三条命令）**

```
▶ 注入 tests/node-types.d.ts，删掉 node:fs / node:path / node:os / node:crypto 四处
  `const _default = {...}; export default _default;`（共 22 行）
  注入后 sha256=13aa12c0503296df42e612fd198e9ff8022e200c4ded5fb5cc784f4c4266cf51

╔══ npx tsc --noEmit   ╚══ exit=0
╔══ npx vitest run     ══> Test Files  134 passed (134) / Tests  1507 passed (1507)   ╚══ exit=0
╔══ npm run build      ══> ✓ built in 924ms / sw-manifest.json：437 个文件，version=e8ed76750a5bca8b   ╚══ exit=0
  ↩ tests/node-types.d.ts 曾偏离，已写回原文
  tests/node-types.d.ts sha256=905a99bbb3d4ca5882ea0933ce70485256e0ac947da76f5175feef5041c22eb4 ✓ 与备份逐字节一致
```

**结论（按要求给出）**：

1. **`tests/node-types.d.ts` 里的 `default` 导出（`node:fs` 22-29 那段，以及 `node:path`/`node:os`/`node:crypto` 的三处）应当全部收回。** 删掉后 `tsc` 0 错、1507 项全绿、`npm run build`（含真正跑生成器）绿、版本键不变。`pwa-update.test.ts:2` 自己用的就是具名导入（`import { readFileSync, existsSync, mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs'`），`gen-sw-manifest.mjs:28` 也是具名导入 ⇒ **没有任何类型层消费者**。这是**越界改动、且无正当性**，属净债务。（唯一残留疑点见"诚实边界"第 3 条：还有一种"vite 把外部化模块包成 `{default: ns}`"的假设，见下。）
2. **注释 `tests/node-types.d.ts:4-6` 必须重写或删除**。它声称"`default` 导出是**必须**的……只声明具名导出时 `readFileSync` 在生成脚本里是 undefined（G3 Task 8 实测）"。这句话把**类型声明**说成了会影响**运行时**，原理上不成立；实测也复现不出（路 ①②）。真实原因若确实发生过，只可能是"`.mjs` 写了默认导入 + 外部化路径的 cjs-interop"这一种，而且它与 `.d.ts` 无关、也不需要 `.d.ts` 来兜。
3. **新增的具名声明**（`writeFileSync`/`mkdirSync`/`mkdtempSync`/`rmSync`/`tmpdir`、`node:path` 的 `resolve`/`relative`/`sep`、`node:crypto` 的 `createHash`）：现有消费方**只有** `tests/ui/pwa-update.test.ts` 的临时目录夹具（`pwa-update.test.ts:2,5`）。`node:path` 的 `relative`/`resolve`/`sep`、`node:crypto` 的 `createHash` 在 tests 侧**零消费**（`Get-ChildItem tests -Recurse -Include *.ts` 的 import 扫描：`from 'node:path'` 只有三处、全部 `join`；`from 'node:os'` 只有一处 `tmpdir`；`from 'node:crypto'` 零处）。**若修复轮保留夹具写法，`writeFileSync/mkdirSync/mkdtempSync/rmSync/tmpdir` 可留、须在提交信息里写明"唯一消费方是 Task 8 的临时目录夹具"；`relative/resolve/sep/createHash` 建议一并收回**（`gen-sw-manifest.mjs` 的运行时用的是真 `node:` 内置，不受这些声明影响）。
4. **越界还踩了别人写下的明文禁令**：`tests/app/match-file.test.ts:669-670` 原文写着

   > ⚠️ `tests/node-types.d.ts:3-10` 的极简声明：`readFileSync` 只收 `string`（故须 `fileURLToPath`）、返回 `{ subarray(...).toString(encoding?) }`、且**没有** `writeFileSync` —— 不许为这里扩 node 类型声明。

   Task 8 扩了它，且**没有**改这条注释、**没有**在提交里给出正当性说明（提交信息里只写"如实报备缺失"）。`Global Constraints:72` 也要求"既有红线文件的改动必须单独说明理由……先停下来报告"。这条**保留**为治理问题（修法：要么补齐理由，要么按上面第 3 条收回）。

### I-3 预缓存把 `dist/` 全量塞进去：**358.8 MiB / 437 个文件**，而 `install` 是原子的、失败无兜底

`scripts/gen-sw-manifest.mjs:37` 的 `EXT_OK` 放行 `png|jpg|jpeg|pdf|mp3|...`，`:53-58` 只排除 `sw.js`/`sw-manifest.json` 与非白名单后缀。实测清单构成：

```
预缓存总体积 = 358.8 MiB / 437 个文件
   .png         327.23 MiB      (250 个)
   .jpg          22.98 MiB      (174 个)
   .pdf           7.73 MiB      (5 个：rule-faq / rule-mn01 / mn02 / mn03 / mn03-solo)
   .js            0.54 MiB      (2 个)   ← "app shell" 本体只有 0.5 MiB
   .css           0.33 MiB      (4 个)
   .html/.webmanifest        ~0
按目录：/assets/protocols/* 每个协议 8 张图，单协议 7–17 MiB（ambush 16.92 / rigidity 16.21 / …）
```

`public/sw.js:38-47` 的 `install` 用 `await cache.addAll(manifest.files)`——**`addAll` 是原子的**：437 个请求里**任何一个**失败（弱网、CDN 抽风、配额不足、某张图 404），整次 install 就抛错，`event.waitUntil` 的 promise 被拒 ⇒ **SW 不激活**；而 `:41` 还主动 `if (!res.ok) throw`。439 个文件、几百 MiB 在移动网下的失败面很大，且**用户与开发者都不会收到任何提示**（`pwa-update.ts` 的 `onUpdateAvailable` 只在"有新版本"时提示，install 失败不产生任何 UI）。设计稿 §8.1（`:654`）的措辞是"预缓存 **app shell（HTML/JS/CSS）**"，计划 Task 8 设计要点 3 也写"静态资源走缓存优先"，但都没写"要把 327 MiB 卡图与 5 个 PDF 也预缓存"。**这不是"违背明文禁令"，所以不列阻断，但按 §8.1 的意图与工程常识属重要**：

- 修法建议：把 `EXT_OK` 收紧到 app shell（`html|js|mjs|css|webmanifest|woff2|svg|png(图标)`）或维护一份"要离线可玩"的显式集合；卡图/PDF 交给运行时"缓存优先 + 命中即存"或干脆走网络；至少给 `install` 的 `waitUntil` 加 `catch`，别让整个离线能力被一张图拖垮。
- 另一条（更小、但真实）：`/assets/battery/*`（4.66 MiB）在 `.gitignore:4` 里被标为"未使用"，却被塞进预缓存。

---

## 次要

### S-1 `main.ts` 的"必须在 `showHome()` 之前"**没人钉住**（我实测出一个近失）

`pwa-update.test.ts:655-667` 的腿这样写：

```ts
const at = calls[0].index ?? -1;
const seed = main.indexOf('setSeedNonce(newMatchSeed());');
const show = main.indexOf('showHome();');
...
expect(at, '...').toBeGreaterThan(seed);
expect(show).toBeGreaterThan(seed);      // ← 这一句断言的是 seed<show，与 at 无关
```

`showHome` 算出来了却**从未与 `at` 比较**；而 `main.ts` 里 `showHome();` 有**两处**（`:700` 在 `resetToMainInterface` 内、`:814` 在初始化区），`indexOf` 命中的是 `:700`——即使补上 `expect(at).toBeLessThan(show)`，因为真实初始化处的 `showHome()` 在 `:814`，这条断言会变成"`764 < 700`"⇒ **反而假红**。也就是说这条腿的意向（计划 `:102` 明写"PWA 注册/授权弹窗必须排在 `showHome()` 之前"、`附录 A` 钉了 Init 行区）**在实现里正确、在判据里落空**。

我实测的两条变异（`main.ts`）：

| 变异 | 内容 | 红线判定 | 实际结果 |
|---|---|---|---|
| M14 | 整块 `initPwaUpdate()`（连注释）从 `:761-764` 删掉 | **应红** | 红，但红在 `expected +0 to be 1`（`calls.length` 数不到调用），**不是**顺序判据 |
| M15 | 把调用挪到 `initDiag` 之后再调一次（两处调用，一处在合法区） | **应红** | 红，红在 `expected 2 to be 1`（计数），**仍不是**顺序判据 |

⇒ 顺序判据本身从未被触发过；把调用挪到 `:814` 之后（`indexOf` 取到 `:700` 时）仍会**全绿**。修法：用 `lastIndexOf('showHome();')` 或按"初始化区"锚点定位，并补 `expect(at).toBeLessThan(<初始化区的 showHome 位置>)`。

### S-2 `mountUpdateBar` 的去重分支零覆盖

`pwa-update.ts:182`（`if (… querySelector('.pwa-update-bar') !== null) return;`）是"不重复插条"的**第二道防线**（第一道是 `:242` 的 `prompted`）。变异 **M16**（把整行替换成 `if (false) return;`）⇒ **全绿**。现有腿只在 `prompted` 层面测了"重复 statechange 不重复提示"（`:210-231`），删掉 `:182` 不会红。影响可接受（`prompted` 在 `:220` 一次性置位、永不复位，所以真实路径上 `:182` 确实碰不到），但"零覆盖"要如实记。

### S-3 `controllerchange` 通路与"只刷新一次"的锁零覆盖

`pwa-update.ts:235-239`（`controllerchange` → `reloadOnce()`）是真实浏览器里**唯一的正常刷新路径**；`:228-232` 的 `reloadOnce` 是防"controllerchange 与兜底计时器各刷一次"的锁。现有腿只走兜底计时器（`:349-360` 用 `vi.useFakeTimers()` + `advanceTimersByTime(RELOAD_FALLBACK_MS)`），**从不 `fire('controllerchange')`**。⇒ 把 `:236` 的 `state.kind !== 'updating'` 守卫删掉、或让 `reloadOnce` 去掉 `reloading` 锁，都没有腿会红。（我在 M2 里顺带证实了"点完立刻刷"会红，但那是另一条通路。）

### S-4 activate 清缓存依赖模块内存在变量，`CURRENT_VERSION` 为 `null` 时会列出 `compile-null`

`public/sw.js:24` `let CURRENT_VERSION = null;`、`:43` 在 install 里赋值、`:53` 在 activate 里用它算"该保留的名字"。若 activate 在**没有**先跑 install 的实例里触发（SW 被重启后由浏览器重新评估、或任何 install 与 activate 不在同一实例的路径——标准 chrome 流程里不会发生，故只是潜在），`CACHE_PREFIX + CURRENT_VERSION` 变成 `'compile-null'`，于是**所有** `compile-*` 缓存都被当旧缓存删掉，包括本该保留的那份。修法：把版本持久化到缓存里（如用 `caches.keys()` 里最大的/或写一个 marker 缓存项），或 activate 时先取清单再比。诚实标注：这是我**静态推导**出来的，没有真浏览器证据。

### S-5 `update()` 声明了但从未被调用

`pwa-update.ts:34`（接口里的 `update(): Promise<void>`）在 `SwRegistrationLike` 上，但从 `:1-298` 全文看**没有任何调用点**（也没有测试断言它）。接口里留一个零调用的成员与本仓"零调用的声明要删"的纪律（`tests/app/match-file.test.ts:667`）不一致。

---

## 假守卫清单（注入没变红的那几条 + 原因）

| # | 变异 | 改哪 | 期望 | 实际 | 原因 |
|---|---|---|---|---|---|
| **M7** | `holder = undefined`（保留 `globalThis`/`serviceWorker`/`?? null` 的另一处文本） | `pwa-update.ts:157` | 红 | **全绿 39/39** | `pwa-update.test.ts:400-415` 只做**文本形状**断言，没有一条腿真的给 `globalThis.navigator` 装假件并观察是否注册。**B-1 就活在这个缺口里** |
| **M16** | `if (typeof host.querySelector… ) return;` → `if (false) return;` | `pwa-update.ts:182` | 红 | **全绿 39/39** | 去重分支零覆盖；第一条腿只在 `prompted` 层面测重复提示 |
| **M12** | `fetch` 未命中后 `caches.open('…user-data').put(req, res.clone())` —— **把每个同源响应无条件写进 Cache Storage** | `public/sw.js:81-85` | 红 | **全绿 39/39** | `pwa-update.test.ts:430-434` 的红线腿只查字符串 `localStorage`/`indexedDB`/`sessionStorage`——**只查 API 名字、不查 cache 写入行为**。"绝不缓存用户数据"这条**没有任何腿能钉住一次缓存写入**。修法：补一条"`cache.put`/`cache.add`/`cache.addAll` 只允许出现在 install 段"的文本腿（或把清单来源钉死） |
| **M10** | `version = computeVersion(files.slice(0, -1))`（少算一个文件的哈希 ⇒ 版本键与清单**不匹配**） | `gen-sw-manifest.mjs:84` | 红 | **全绿 39/39**（`npm run build` 也成功，只是打出 `version=0447ddfed304a273`） | `pwa-update.test.ts:517-525` 只测"内容变→版本变"，`:572-593` 只断言 `manifest.version === report.version`（两边都走同一份被改的实现，恒等）。**没有"版本键必须覆盖清单全部文件"的腿** |
| **M8c** | `readdirSync(path, { withFileTypes? })` 扩签名 | `node-types.d.ts:12` | 红（越界扩声明应被拦） | **全绿（tsc exit 0）** | 没有任何测试文件用这个形态调用 `readdirSync` ⇒ "最小声明"这条纪律**没有自动守卫**，只能靠人看 |
| **S-1 的顺序判据** | 见 S-1（`at` 与 `show` 从未比较） | `pwa-update.test.ts:664-666` | 红 | 顺序判据从不触发（只在计数上红） | 断言写漏 + `indexOf` 命中 `:700` 的那处 `showHome()` |

**变红的（真守卫，供对照）**：M1（提示阶段就刷新）→ 3 条腿红、M2（点完立刻刷）→ 1 条红、M3（update-ready 阶段就 reload）→ 3 条红、M4（去掉 `prompted` 幂等闸）→ 1 条红、M5（`initPwaUpdate` 里直接用 `navigator.serviceWorker` 绕过注入缝）→ 注入缝文本腿红、M6（首次安装也提示"有新版本"）→ 1 条红、M13（`SKIP_WAITING` 改名）→ 1 条红、M11（脚本里加手写文件名字面量）→ 1 条红、M7b → 文本腿红。

---

## 规格空档（Task 8 要求但无人钉）

1. **"`realEnv()` 真的能从浏览器全局取到容器"** —— 无腿（I-1 / B-1）。计划 `:2435` 明确要求"`function initPwaUpdate(env?)`（**默认从 `navigator.serviceWorker` 取真实环境**）"，**这条要求没有任何腿**，实现也正好违反。
2. **`initPwaUpdate()` 必须在初始化区的 `showHome()` 之前** —— 计划 `:101` 与附录 A 都钉了，判据落空（S-1）。
3. **预缓存应覆盖哪些文件（app shell 的边界）** —— `EXT_OK` 是全量白名单 vs §8.1 的"app shell"（I-3），无判据、无语义约束（无体积上限、无 install 失败兜底）。
4. **`activate` 与 install 的配对语义**（`CURRENT_VERSION` 跨实例失效，S-4）—— 只有 `toContain('caches.delete')` 这条文本腿。
5. **`controllerchange` 通路与 `reloadOnce` 锁** —— 无腿（S-3）。
6. **`update()` 的用途** —— 无腿、无调用（S-5）。
7. **"没有新版本"这条禁止（不该提示）** —— 只有"首次安装不提示"（`:233-250`）覆盖了一半；"同一版本重复检查不提示"无腿。
8. **`beforeinstallprompt` / 可安装性 / `theme_color` 与 `background_color` 是否需要视觉验收** —— 计划把可安装性列为"用户验收"（`未核对` #2），`manifest.webmanifest` 的语义只被 `:604-632` 钉了 `start_url`/`scope`/`display`/图标尺寸，**没有任何腿**验证"装上去真的能开、图标在 launcher 里不难看"。

---

## 我核实为"没问题"的点（附证据）

1. **"不许静默自动刷新"这条判据真的存在且真的钉住了**（题意 1 的答案：**是，必须用户点"更新"才刷新**）。
   状态机（`pwa-update.ts:102-119`）里 `waiting-present/installed` → `update-ready`，`apply-update` → `updating`；刷新只发生在两处且都在用户点击之后：`:235-239`（`controllerchange`，且要求 `state.kind === 'updating'`）与 `:257-261`（兜底计时器，同样要求 `state.kind === 'updating'`）。按钮在 `:246-262` 才 `applyUpdate(reg)`。注入 M1/M2/M3 全部变红（原始输出见上文各条）。
2. **注入缝真的存在**：`pwa-update.ts:211-216` 合并 `overrides`，`tests/ui/pwa-update.test.ts` 全程不透明 DOM/SW API（`:45-102` 假件 + `:298-325` 元素桩），在 **node** 下真跑出行为（`environment: 'node'`，`vite.config.ts`；无 jsdom）。M5（把 `navigator.serviceWorker` 直接读进 `initPwaUpdate`）会红 ⇒ 注入缝的**文本**守卫有效；但**运行时**守卫缺失（I-1）。
3. **`.mjs` 不写盘（无副作用）**：`gen-sw-manifest.mjs:94-102` 的 `isMain` 判定让被 import 时不跑 `run()`。我实测：`pwa-update.test.ts:495-593` 跑了 `genManifest` 多次，`dist/sw-manifest.json` 的 mtime 与内容均只在我显式 `node scripts/gen-sw-manifest.mjs` 时变。
4. **`gen-sw-manifest.mjs:94-102` 的 `isMain` 判定可用**：`node scripts/gen-sw-manifest.mjs` 正常产出、被 vitest import 时无副作用（见上条）。
5. **清单是派生的、版本键是内容哈希**：`collectFiles`（`:40-58`）走磁盘遍历 + 后缀过滤，无手写名单（M11 会红）；`computeVersion`（`:61-68`）对 `路径:sha256` 排序拼接再 sha256 取前 16 位。**我独立复算过一次**（用仓内模块本身，`.superpowers` 探针）：

   ```
   collectFiles 数量 = 437
   computeVersion   = e8ed76750a5bca8b
   manifest.version = e8ed76750a5bca8b  count = 437
   文件列表一致?     = true
   ```

   与题面声称的 `437 个文件，version=e8ed76750a5bca8b` **逐字一致**，且 `npm run build` 在**三次不同变异注入后**仍复现同一版本键（内容不变 ⇒ 版本不变，`:12-13` 那条设计意图成立）。
6. **`sw.js`/`sw-manifest.json` 正确排除**：`manifest.files` 不含 `/sw.js`、`/sw-manifest.json`（探针 `-contains` 均 `False`），`SKIP`（`:35`）生效，避免自指。
7. **`main.ts` 只动自己的两处、没碰 `cb`/`rerender`**（题意 5 的答案：**合规**）。证据（`git show 0e3fdbe -- src/main.ts`）：

   ```
   变更 hunk 头：@@ -38,0 +39,2 @@  @@ -758,0 +761,4 @@      （只有两处，均为新增）
   新增行（6 行，无删除行）：
   +// G3 Task 8：PWA（manifest + service worker + 自动提示更新 + 一键更新）。零依赖、手写。
   +import { initPwaUpdate } from './ui/pwa-update';
   +// G3 Task 8：注册 Service Worker 并挂"有新版本可用 → 立即更新"提示条（附录 A 的 Task 8 行区）。
   +// dev 下 initPwaUpdate 自动不注册（`import.meta.env.DEV`），所以不影响 vite dev 的热更新。
   +// 位置在 setSeedNonce/createGame 之后、showHome() 之前：不动 G0 的启动语义。
   +initPwaUpdate();
   ```

   锚点法逐字节比对（`import`/`core` 区块插入会平移行号，所以按函数锚点取区间）：

   ```
   rerender: 锚点区间长度 父=1008 HEAD=1008 逐字节相同? true
   cb:       锚点区间长度 父=6494 HEAD=6494 逐字节相同? true
   ```

   `main.ts` 现在 `rerender` 在 `:115`、`cb` 在 `:199`（父提交是 `:113`/`:197`，正是 `:39-40` 那 2 行 insert 造成 +2 平移）。⇒ 两个函数体一字未动。`initPwaUpdate();` 在 `:764`，位于 `setSeedNonce`（`:44`）之后、初始化区（`:814 showHome()`）之前，符合计划 `:102`。
8. **`index.html` / `package.json` 接线合规**：`index.html` 恰好 2 行（`<link rel="manifest" …>` + `<meta name="theme-color" …>`）；`package.json:8` 只改 build 串（`… && node scripts/gen-sw-manifest.mjs`），`dependencies` 仍不存在、`devDependencies` 仍只有 `typescript/vite/vitest`（`pwa-update.test.ts:649-653` 钉住，M8b 实测也维持）。
9. **跨任务一致性（题意 6 的答案：文案与实现相符，无阻断）**。逐条对照 `src/app/privacy.ts:74-76` 与 `public/sw.js`：

   | 隐私文案（`privacy.ts:75-76`） | `sw.js` 实际行为 | 一致？ |
   |---|---|---|
   | "程序文件（页面、脚本、样式、卡图）缓存在本机" | `:44-45` `caches.open('compile-'+version)` + `addAll(manifest.files)`；清单 437 项 = 1 html + 4 css + 2 js + 424 图 + 5 pdf + 1 webmanifest（我按目录/后缀统计过） | ✅（确实只有程序文件） |
   | "缓存的不是用户数据：昵称、卡组、档案与对局记录都不在这个离线缓存里" | `sw.js` 全文**唯一**的写缓存点是 `:45`；`:64-85` 的 `fetch` 是"命中即返回、未命中 `return fetch(req)`"——**永不写缓存**。`src/` 里 `localStorage/sessionStorage/indexedDB` 命中 0（计划 `:107` 的侦察结论，我也复核了 `sw.js` 与 `pwa-update.ts` 无存储 API） | ✅ |

   ⇒ **这一轮不构成阻断**。（但**守护这条承诺的腿很弱**：见"假守卫"M12——把 `fetch` 改成无条件 `cache.put` 也全绿。隐私承诺当前**靠实现碰巧正确**，不靠判据。）
10. **图标不是空文件占位**：`public/icons/*` 的 PNG 头与 IHDR 宽高被 `pwa-update.test.ts:617-632` 按 manifest 声明的 `sizes` 逐个核对（`manifest.webmanifest:11-15`），且三个图标都在 `0e3fdbe` 里以二进制提交（21857 / 81931 / 83555 字节）。
11. **四道门禁在还原后仍全绿**（我在评审末尾又跑了一遍）：`tsc` 0 错 / `vitest` 134 文件 1507 项 / `vite build` OK / `npm run build` OK（437 / `e8ed76750a5bca8b`）/ `texts:check` 0-0。

---

## 诚实边界

1. **我没有用无头浏览器**。用户授权了，但静态推导 + node 行为腿已经足以定案：B-1 是"属性存在性"问题（`globalThis.serviceWorker === undefined`），不需要真 SW 执行来证明；而且真浏览器里这条缺陷的表现恰恰是"什么都没发生"，反而更难从截图看出。因此**"离线可开""装得上 PWA""launcher 图标好看"我一条都没验**，它们仍是"用户验收"（计划 `未核对` #2、用户验收 #4）。**没有起任何服务、没有碰 5173/3080、没有收工杀进程的问题**（我没起过进程）。
2. **B-1 的第 2 步（`sw === null` ⇒ `return () => {}`）我是静态读出来的**（`pwa-update.ts:216`），探针给的是第 1 步（`register` 不被调用）的**运行时**证据。两步合起来才等于"PWA 不注册"；单独任一步都不足以定案，所以两步都贴了。
3. **`.d.ts` 那条我给的结论带一个残留疑点**：如果实现 agent 当时真的亲眼见过"`readFileSync is not a function`"，那它最可能的机制是"vite 把外部化的 `.mjs` 包成 `{default: ns}` 而具名导出丢失"（纯运行时、与 `.d.ts` 无关）。我**没能在当前工具链上复现**这个失败（M8a：`.mjs` 用默认导入 + 保持 `.d.ts` 原样 ⇒ 全绿），所以我把"必须要有 `default`"判为**不成立**；但如果修复轮要彻底结清，正确做法是**让脚本用具名导入并删掉 `default`**（我已实测全绿），而不是留着一条自己都解释不通的注释。
4. **S-4（`CURRENT_VERSION` 跨实例为 null ⇒ 删掉全部 `compile-*`）是静态推导**，我没有真 SW 证据证明"activate 会在没有 install 的实例里跑"；标准流程里不会。列为次要而非重要。
5. **M14/M15 我起初以为能证明"顺序判据缺失"，实际红在计数上**——我把这个自我纠正写进了 S-1，没有把"红"读成"顺序判据有效"。
6. **`vitest` 原始输出我按行过滤后引用**（`Select-String`），未贴全文；每条都保留了 `Test Files` / `Tests` 汇总行与 `AssertionError` 原文。若需要未过滤全文，同一命令可复跑。
7. **`npm run build` 会重写 `dist/`**（`vite build` 清空 + 生成器写 `dist/sw-manifest.json`）。`dist/` 在 `.gitignore:2` 内，我收工时已用 `node scripts/gen-sw-manifest.mjs` 把清单重新生成回 `437 / e8ed76750a5bca8b`。
8. **`vitest` 的 mtime 缓存险情**（`Global Constraints:65-66`）我按纪律执行：每次"改文件→跑测试"之间 `Start-Sleep -Seconds 2`。变异跑者写文件后立即 spawn 测试，中间没有等待——但每条变异的 `find` 命中数与还原哈希都核过，且变红的腿与变异的语义一致（不是随机红），所以判定不受影响。

---

## 还原证据

**1) 被评审文件：工作树与 `0e3fdbe` 的 blob 逐字节一致（`git hash-object` vs `git rev-parse 0e3fdbe:<path>`）**

```
file                                   worktree==HEAD? sha256(worktree)
public/manifest.webmanifest            True       D2549E31AF77C5528A27AE18C8878FC4709616A391AAB761C14782515B724F73
public/sw.js                           True       5840F8DEBF639806ABD3DF07EE4CCF861AFF42AFFC25D0F893609B4098651860
public/icons/icon-192.png              True       12B4AD1B9457CB47ACF84439516BCB11F316F93AB54E5A624C819522DE2E846B
public/icons/icon-512.png              True       D29614BDF597F9E62FA596AC633A63A07A9C7EA40E941D6B6EEC948358176758
public/icons/icon-maskable-512.png     True       BEC61B0E85C858B5BD8E072AA19565C708247B400B17595B0BBC4D2C01DD207F
scripts/gen-sw-manifest.mjs            True       817006830F19D8B6DC2C5E5C63827344E09B7DE2F8E5A69E7107F2D7168568E9
scripts/gen-sw-manifest.d.mts          True       91AF74771487C379D0F69BF14528E899EFD17B9A948266F808F7BEBD233941A9
src/ui/pwa-update.ts                   True       A4C5E809A279C581FA7A4D52E6E82F4668458EFCC679600A86D1A45716134435
tests/ui/pwa-update.test.ts            True       6975684729A34FE2951B8A21181761FA09B2B9693B858F296FBB871A77573910
index.html                             True       739F9E5E9F8ED05E0A7507577BE98F36DC231B0984BB3C76DFF53D145EA4D115
package.json                           True       ED5CB4CC60AD526B8338B052A94B8C8F68218C652D9618ACEBF42973CC800FD9
src/main.ts                            True       075F7B5D15A8723F53D3139CB7944CC9CCE6AEF1F70C4D110D505B5E4C5A265B
tests/node-types.d.ts                  True       905A99BBB3D4CA5882EA0933CE70485256E0AC947DA76F5175FEEF5041C22EB4
---- git status --porcelain（应为空） ----
---- 结束 ----
```

**2) 逐条变异的还原行**（每条跑的原始输出里都带这一行；下面是 14 条注入的汇总，全部 `✓ 与备份逐字节一致`）

| 变异 | 文件 | 注入后 sha256（前 12 位） | 还原后 sha256 | 验收 |
|---|---|---|---|---|
| M1 提示阶段静默刷新 | `src/ui/pwa-update.ts` | `8530df77460a` | `a4c5e809a279…16134435` | ✓ |
| M2 点完立刻刷新 | 同上 | `3f35c7ee5ab7` | 同上 | ✓ |
| M3 update-ready 就 reload | 同上 | `1b6ff50966fa` | 同上 | ✓ |
| M4 去掉幂等闸 | 同上 | `46aa4ed306e1` | 同上 | ✓ |
| M5 绕过注入缝 | 同上 | `6f7365ad4d60` | 同上 | ✓ |
| M6 首次安装也提示 | 同上 | `2ebbd36eb2fd` | 同上 | ✓ |
| M7 `holder=undefined` | 同上 | `bc16c63ec159` | 同上 | ✓ |
| M7b 同上去文本 | 同上 | `b0559784a9ff` | 同上 | ✓ |
| M8a `.mjs` 默认导入 | `scripts/gen-sw-manifest.mjs` | `67c09854067e` | `817006830f19…7168568e9` | ✓ |
| M8b 删四处 `default` | `tests/node-types.d.ts` | `13aa12c05032` | `905a99bbb3d4…041c22eb4` | ✓ |
| M8c 扩 `readdirSync` 签名 | 同上 | `e9085d714f98` | 同上 | ✓ |
| M9 `fs.readFileSync` | `scripts/gen-sw-manifest.mjs` | `47d7e5e882dc` | `817006830f19…7168568e9` | ✓ |
| M10 版本键少算一个文件 | 同上 | `bf010d706ad6` | 同上 | ✓ |
| M11 手写清单字面量 | 同上 | `dfd38ea02344` | 同上 | ✓ |
| M12 `cache.put` 缓存用户响应 | `public/sw.js` | `5322a4dbe055` | `5840f8debf63…98651860` | ✓ |
| M13 `SKIP_WAITING` 改名 | 同上 | `e99ae0d6608c` | 同上 | ✓ |
| M14 删掉 initPwaUpdate | `src/main.ts` | `7ea904994f8b` | `075f7b5d15a8…4c5a265b` | ✓ |
| M15 重复调用 | 同上 | `62d3cf1d2974` | 同上 | ✓ |
| M16 去掉去重分支 | `src/ui/pwa-update.ts` | `00a198ca5215` | `a4c5e809a279…16134435` | ✓ |

（M8b 有一个**中途插曲**必须如实交代：我第一次跑 M8b 用的跑者只比对、**没有写回**，导致 `tests/node-types.d.ts` 一度停留在变异态（并且那一次 `npm run build` 是在变异态下绿的——这反而多验了一次"该变异确实不影响门禁"）。之后我用 `git checkout -- tests/node-types.d.ts` 还原（回到 `905a99bb…`），修好跑者（加写回 + 绝对路径），**重跑了一遍完整 M8b**（tsc + 全量 vitest + build），输出即上文"路 ②"。终态由上面第 1 项的 `worktree==HEAD True` 兜底。）

**3) 临时探针已删、仓库内零残留**

```
$ git status --porcelain
（空）

$ Get-ChildItem .superpowers -File | Where-Object { $_.Name -in 'mrun.mjs','mseq.mjs','mutations.json','mutations2.json','probe-realenv3.test.ts' }
mrun.mjs                 3018
mseq.mjs                 2644
mutations.json           5905
mutations2.json          1682
probe-realenv3.test.ts   3205
```

`.superpowers/` 在 `.gitignore:3` 内（我用 `git status` 与 `git hash-object` 双重确认仓库零残留）。上面 5 个文件是**评审证据留档**（变异定义、跑者、B-1 的探针），可随时删；`tests/ui/_pwa-review-probe*.test.ts` 三个临时探针**已全部删除**。
