# G2 远程对战页面 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给游戏加一个**远程对战页面**（甲读法：3 条横带，每条带内 上=对手 / 下=自己；**自己的卡正立、对手的卡 180° 倒置**），并让它**零联机就能在 5173 上单视角预览**，由用户做视觉验收。

**Architecture:** 新渲染器 `src/ui/render-net.ts`（**不复用热座页的 `renderBoard`**），复用一批已参数化就够用的助手；为支持 180° 朝向，先给 FX 层补上 180°（`buildFxCard`/朝向判定）；G1 契约改成**按渲染器区分钩子要求**（`.rot-cw`/`.rot-ccw` 属热座专属，远程页改用新的朝向标记）。

**Tech Stack:** TypeScript 5（strict）/ Vite / vitest（node 环境、`pool: 'threads'`、**无 jsdom**）。零运行时依赖。

**依据文档：** `docs/2026-09-13-联机与多端-设计稿.md` §6（远程对战页面）、§6.2（座位相对渲染）、§6.4（信息遮蔽）、§11（阶段路线）；`docs/4代-FX DOM 契约.md`（G1 产出）。

**前置：** G0（确定性内核）与 G1（FX DOM 契约）已完成并推送（`main = 73131e4`）。

---

## Global Constraints

- **零运行时依赖**：不得新增任何 npm 包；`package.json` 的 `dependencies` 必须保持不存在。**不得** `npm install`。
- **无 jsdom**：UI 测试一律源码文本守卫（`readFileSync` + `new URL(..., import.meta.url)`）。不得引入 jsdom / happy-dom / playwright，不得给 `vite.config.ts` 加 DOM 环境。
- **热座页行为与观感零变化**：`renderBoard` 与热座相关 CSS 的可见结果不得改变。`src/ui/render.ts` 只在**明确要求**的地方改动（见 Task 4），改动后必须证明热座路径语义不变。
- **验证门（每任务收尾）**：`npx tsc --noEmit` → 0 错；`npx vitest run` → 全绿且不少于当前 **934** 项；`npx vite build` → 成功；`npm run texts:check` → 差异 0 处；`npx vitest run tests/docs tests/ui` → 绿。
- **⚠️ 工具链险情（Task 2 评审查出，务必遵守）**：文件 mtime 与 vitest 启动落在**同一秒**时，Vite 的转换缓存会让 vitest 使用**旧编译产物**（实测：测试文件是新的，但它 `import` 的模块改动未生效；`--no-cache` 无效）。
  → **每次「改文件 → 跑测试」之间先 `Start-Sleep -Seconds 2`**，否则拿到的是假红/假绿，据此下的结论全是错的。
- **测试总数地板**：905（G2 开工）→ 915（Task 1）→ **923**（Task 2）→ 929（2F）→ 934（2F2）→ 934（2F3，两处在既有 `it` 内加强）。
- **提交**：`git add src tests docs`（不要 `-A`）；中文提交信息、多个 `-m`；**不推送**（用户明确指示才推）。
- **视觉验收由用户在 5173 上做**：不得用无头浏览器实测观感。

---

## 侦察结论（G2 的设计依据，均已在源码核对）

| 事实 | 证据 | 对 G2 的含义 |
|---|---|---|
| 19 条 A 类钩子在战场页**全部有产出**，无遗漏 | 逐条核对 `render.ts`（如 `.stack-slot` `:208/210/211`、`.protocol-cell` `:1843/1845/1846`、`.deck` `:1473/1474`…） | 新页面只要照产出即可满足契约 |
| `renderApp` 按 phase 分派：`draft → renderDraft`，否则 `renderBoard` | `render.ts:5584-5588` | 新页面需要自己的分派入口或模式开关 |
| `renderApp` 的四个副作用：`no-anim` 类、`syncCheckCacheChains(s)`、`syncChainLayerPosition()`、`cb.onRendered?.()`，双 rAF 后移除 `no-anim` | `render.ts:5583, 5591, 5593, 5594, 5595-5599` | 新入口必须**复制这四个**，否则特效与回调时序错 |
| 持久特效同步函数**全部布局无关**（只按契约钩子找节点） | `render.ts:4932-4978`、`main.ts:661-686`；选择器见 `render.ts:293/404/455/507/565/766/846/1009/1095/1148` 等 | 新页面**不需要**改任何 `sync*` |
| **8 处按下标取手牌**：`querySelectorAll('.hand')[player]` | `fx-gen2.ts:693/1316/1786`；`effects/index.ts:849/942/1541/1590/1650`；另有 `:1703` 一次取两手 | ⚠️ 对手手牌只显示数量时这些取到 `undefined` → **静默跳过或飞到错误坐标**。见 Task 3 的硬约束 |
> 注（G2 Task 3F 订正）：本行原写「6 处」且行号是 Task 2 侦察时的旧值。按 3F 的
> `git grep -n "querySelectorAll" -- src/ui | grep ')\['` 复核后订正为 **8 处按下标 + 1 处取两手**。
| 方向性 FX 按**绝对玩家**：链端点 `owner === 0 ? right-90 : left+90` | `effects/index.ts:191-193`；`fx-gen2.ts:779/878/957` | ⚠️ 新页面**必须保持**「P0 向左长 / P1 向右长」，不得改成座位相对 |
| 控制轨按绝对玩家、硬编码 4%/96% | `render.ts:1815-1818`；`gen3-control.ts:654` | 同上，保持绝对玩家 |
| `visibleRectOf`/`clipInsetRightPct` 假设**覆盖者在其右侧** | `gen3-util.ts:51, 66`（注释 `:5-6`） | 同上：within-slot 生长方向不能改 |
| 「自己」目前用 `s.turnPlayer` 冒充 | `render.ts:207, 4603, 4605, 4660, 4701, 4718` | ⚠️ `turnPlayer` 是**回合**概念，当"我是谁"会让预览**每回合翻面**。必须引入真正的座位 |
| `.battery` 绝对定位在 `.stack-slot`，`.p1{left:-120px}` / `.p2{right:-120px}` | `styles.css:161-179, 199-200` | 维持 `.p1`/`.p2` 语义即可 |
| `.deck[data-player]`、`.trash-pile.pN` 绝对定位在 `.hand-side` | `styles.css:761-763, 824-835` | 远程页只有一个手牌区 → 对手的牌库/弃牌堆必须**重新安放**到顶部信息条 |
| `.rot-cw{rotate(90deg)}` / `.rot-ccw{rotate(-90deg)}` / `.rot-180{rotate(180deg)}` | `styles.css:454-457`；产出 `render.ts:227`、`:116` | **G2 的核心冲突点**，见下 |
| 契约守卫要求**每个** `src/ui/render*.ts` 都在 `RENDERERS` 里，且其源码文本含全部 A 类探针 | `tests/ui/fx-dom-contract.test.ts:31, 166-174, 176-188` | 新页面文件名必须是 `render-net.ts`（否则逃过守卫），且必须为契约改动做配套 |
| `resetUiState()` 清 render.ts 模块态 + ~20 个 body 级注册表 + ~25 个瞬时 FX 类 | `render.ts:5000-5094`；调用点 `main.ts:540` | 新页面必须有自己的 `resetNetUiState()` 并挂到 `main.ts:540` 旁边 |

### 核心冲突：180° 朝向 vs G1 契约

用户已拍板：**自己的卡正立（0°）、对手的卡 180° 倒置**。而热座页给场上卡加的是 `.rot-cw`(+90°)/`.rot-ccw`(−90°)，**FX 层依赖这两个类**判断浮层卡用哪种朝向构造：

```ts
// effects/index.ts:114-124 等多处
node.classList.contains('rot-cw')  // → cw
node.classList.contains('rot-ccw') // → ccw
buildFxCardAt(rect, cw, ccw, …)
```

两点后果，都必须正面解决：
1. **远程页不产出 `.rot-cw`/`.rot-ccw`** → G1 守卫（要求每个渲染器都含全部 A 类探针）会失败 → 契约必须改成**按渲染器区分要求**。
2. **±90° 会交换布局盒宽高，180° 不会** → 不能靠 CSS 伪造；`buildFxCard` 必须真正支持 180°。这就是 Task 1 存在的原因。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/ui/fx-orient.ts` | **朝向的单一出处**：从节点读出朝向（0/90/−90/180），供热座页与远程页共用 | **新建** |
| `src/ui/fx/clone-orient.ts` | 浮层卡按朝向构造的纯几何助手（含 180°） | **新建** |
| `src/ui/render-net.ts` | 远程对战页渲染器（甲读法 + 座位相对 + 单视角预览） | **新建** |
| `src/ui/styles-net.css` | 远程页布局（3 横带、上对手/下自己、卡片 0°/180°） | **新建** |
| `src/ui/fx-dom-contract.ts` | 契约支持「按渲染器区分要求」 | 修改 |
| `tests/ui/fx-dom-contract.test.ts` | 配套守卫 | 修改 |
| `docs/4代-FX DOM 契约.md` | 记录新朝向钩子与按渲染器要求 | 修改 |
| `src/ui/render.ts` | 只改 Task 4 明确列出的点（导出 3 个助手 + 座位化） | 修改 |
| `src/main.ts` | 预览入口 + `resetNetUiState` 挂载 | 修改 |

---

## Task 1: FX 支持 180° 朝向（单一出处 + 浮层卡几何）

**Files:**
- Create: `src/ui/fx-orient.ts`
- Create: `src/ui/fx/clone-orient.ts`
- Test: `tests/ui/fx-orient.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `type CardOrient = 0 | 90 | -90 | 180`
  - `orientOf(node: Element | null | undefined): CardOrient` —— 按类名读朝向，**唯一出处**
  - `orientToCwCcw(o: CardOrient): { cw: boolean; ccw: boolean; flip180: boolean }`
  - `cloneTransformOf(o: CardOrient): string` —— 返回 `''` / `'rotate(90deg)'` / `'rotate(-90deg)'` / `'rotate(180deg)'`
  - `cloneBoxSwaps(o: CardOrient): boolean` —— ±90° 为 `true`（宽高互换），0/180 为 `false`

**为什么必须有单一出处**：现在"读朝向"这件事散在十余处 `classList.contains('rot-cw')`。加 180° 时若只改其中几处，会出现"有的浮层卡正、有的倒"这种极难排查的不一致。先把它收成一个函数，再让所有调用点走它。

- [ ] **Step 1: 写失败的测试**

创建 `tests/ui/fx-orient.test.ts`（node 环境，**不用 DOM**——用手写的最小桩对象，只实现 `classList.contains`）：

```ts
import { describe, it, expect } from 'vitest';
import { orientOf, orientToCwCcw, cloneTransformOf, cloneBoxSwaps, type CardOrient } from '../../src/ui/fx-orient';

/** 最小桩：只需 classList.contains —— 避免引入 jsdom */
const node = (...cls: string[]) => ({ classList: { contains: (c: string) => cls.includes(c) } }) as unknown as Element;

describe('FX 朝向单一出处（G2）', () => {
  it('从类名读出四种朝向', () => {
    expect(orientOf(node())).toBe(0);
    expect(orientOf(node('rot-cw'))).toBe(90);
    expect(orientOf(node('rot-ccw'))).toBe(-90);
    expect(orientOf(node('rot-180'))).toBe(180);
  });

  it('同时带多个类时按优先级判定，且 180 优先于 90', () => {
    expect(orientOf(node('card', 'rot-180', 'rot-cw'))).toBe(180);
  });

  it('空节点/缺 classList 安全退化为 0', () => {
    expect(orientOf(null)).toBe(0);
    expect(orientOf(undefined)).toBe(0);
    expect(orientOf({} as unknown as Element)).toBe(0);
  });

  it('orientToCwCcw 与旧约定等价（热座页行为不变）', () => {
    expect(orientToCwCcw(0)).toEqual({ cw: false, ccw: false, flip180: false });
    expect(orientToCwCcw(90)).toEqual({ cw: true, ccw: false, flip180: false });
    expect(orientToCwCcw(-90)).toEqual({ cw: false, ccw: true, flip180: false });
    expect(orientToCwCcw(180)).toEqual({ cw: false, ccw: false, flip180: true });
  });

  it('±90 交换布局盒宽高，0/180 不交换', () => {
    for (const o of [90, -90] as CardOrient[]) expect(cloneBoxSwaps(o)).toBe(true);
    for (const o of [0, 180] as CardOrient[]) expect(cloneBoxSwaps(o)).toBe(false);
  });

  it('transform 字符串与 CSS 侧一致', () => {
    expect(cloneTransformOf(0)).toBe('');
    expect(cloneTransformOf(90)).toBe('rotate(90deg)');
    expect(cloneTransformOf(-90)).toBe('rotate(-90deg)');
    expect(cloneTransformOf(180)).toBe('rotate(180deg)');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/fx-orient.test.ts`
Expected: FAIL —— 无法解析 `../../src/ui/fx-orient`。

- [ ] **Step 3: 实现 `src/ui/fx-orient.ts`**

```ts
/**
 * 卡牌朝向的**单一出处**（G2）。见 docs/2026-09-13-联机与多端-设计稿.md §6。
 *
 * 热座页给场上卡加 rot-cw(+90°) / rot-ccw(−90°)：同一块屏幕前两位玩家各自看得正。
 * 远程页改为**自己正立(0°) / 对手 180° 倒置**（像隔桌对坐）—— 因此新增 180°。
 *
 * ⚠️ ±90° 会**交换布局盒宽高**，0°/180° 不会。浮层卡（飞行中的幽灵卡）的几何必须按此区分，
 * 否则出现"尺寸正确但朝向错"或"朝向对但尺寸错"的假正确。
 */

export type CardOrient = 0 | 90 | -90 | 180;

/** 从节点类名读朝向。180 优先于 ±90（同时带时按 180 算）。 */
export function orientOf(node: Element | null | undefined): CardOrient {
  const cl = (node as { classList?: { contains(c: string): boolean } } | null | undefined)?.classList;
  if (!cl || typeof cl.contains !== 'function') return 0;
  if (cl.contains('rot-180')) return 180;
  if (cl.contains('rot-cw')) return 90;
  if (cl.contains('rot-ccw')) return -90;
  return 0;
}

/** 与既有 buildFxCardAt(rect, cw, ccw, …) 的参数形状对齐 */
export function orientToCwCcw(o: CardOrient): { cw: boolean; ccw: boolean; flip180: boolean } {
  return { cw: o === 90, ccw: o === -90, flip180: o === 180 };
}

/** 该朝向是否交换布局盒宽高 */
export function cloneBoxSwaps(o: CardOrient): boolean {
  return o === 90 || o === -90;
}

/** 浮层卡应施加的 rotate() 值（空字符串 = 不施加） */
export function cloneTransformOf(o: CardOrient): string {
  switch (o) {
    case 90: return 'rotate(90deg)';
    case -90: return 'rotate(-90deg)';
    case 180: return 'rotate(180deg)';
    default: return '';
  }
}
```

- [ ] **Step 4: 实现 `src/ui/fx/clone-orient.ts`**

这是**纯几何**助手：给定源卡 rect 与朝向，返回浮层卡该用的尺寸与 transform。**不改动任何现有调用点**（那是 Task 2）。

```ts
/**
 * 浮层卡（飞行幽灵卡）按朝向的几何（G2）。
 * 与现有 buildFxCard/buildFxCardAt 的「未旋转布局盒 + 中心旋转」约定一致：
 * 元素用**未旋转**的尺寸建盒，再用 transform 旋到目标朝向 —— 这样 ±90° 的宽高互换
 * 由浏览器负责，浮层与源卡的视觉包围盒自然一致。
 */

import { cloneBoxSwaps, cloneTransformOf, type CardOrient } from '../fx-orient';

export interface CloneBox {
  /** 布局盒宽高（未施加旋转） */
  w: number;
  h: number;
  transform: string;
  /** 盒子的宽高是否相对源卡 rect 互换（±90° 为 true） */
  swapped: boolean;
}

/** srcRect 是**屏幕上看到**的包围盒（已含旋转）；返回应当构建的未旋转布局盒。 */
export function cloneBoxFrom(srcRect: { width: number; height: number }, o: CardOrient): CloneBox {
  const swapped = cloneBoxSwaps(o);
  return {
    w: swapped ? srcRect.height : srcRect.width,
    h: swapped ? srcRect.width : srcRect.height,
    transform: cloneTransformOf(o),
    swapped,
  };
}
```

- [ ] **Step 5: 跑测试 + 全量回归 + 提交**

Run: `npx vitest run tests/ui/fx-orient.test.ts` → PASS（6 项）
Run: `npx tsc --noEmit` → 0 错
Run: `npx vitest run` → 全绿，总数 ≥ 905

```bash
git add src tests
git commit -m "feat(G2): 新增卡牌朝向单一出处与浮层卡几何（含 180°）" -m "远程对战页改为自己正立 / 对手 180° 倒置，为此把散落在十余处的 classList.contains(rot-cw) 收成 orientOf() 单一出处，并新增 cloneBoxFrom() 处理「±90° 交换布局盒宽高、0/180 不交换」这一几何差异。" -m "本任务只新增模块与单测，不改动任何现有调用点（调用点迁移在 Task 2）。"
```

---

## Task 2: 迁移现有朝向判定到单一出处

**Files:**
- Modify: `src/ui/effects/index.ts`（`effects/index.ts:114-124` 及其余 `rot-cw`/`rot-ccw` 判定点）
- Modify: `src/ui/fx-gen3.ts:90`、`src/ui/fx-gen2.ts` 中同类判定点
- Test: `tests/ui/fx-orient.test.ts`（加一条源码守卫）

**Interfaces:**
- Consumes: Task 1 的 `orientOf` / `orientToCwCcw`
- Produces: 无新接口（行为等价迁移）

**这一步是纯重构，必须做到热座观感零变化。** 先枚举全部判定点，再逐处替换，最后用源码守卫确保不再出现裸判定。

- [ ] **Step 1: 枚举全部判定点**

Run: `git grep -n "rot-cw\|rot-ccw\|rot-180" -- src`
把结果记进报告。**预期**：产出点（`render.ts:227`、`:116`）保留不动；**读取/判定点**全部迁到 `orientOf`。

- [ ] **Step 2: 加源码守卫（先失败）**

在 `tests/ui/fx-orient.test.ts` 末尾追加：

```ts
  it('源码中不得再有裸的 rot-cw/rot-ccw 朝向判定（必须走 orientOf）', () => {
    // 唯一的例外：产出类名的地方（render.ts 的 renderStackSlot / renderProtocol）
    // 与 CSS 无关的字符串（本测试只扫 .ts）
    const files = ['effects/index.ts', 'fx-gen2.ts', 'fx-gen3.ts', 'fx-gen3-swap.ts', 'gen3-control.ts', 'compiled-gen3.ts'];
    const root = new URL('../../src/ui/', import.meta.url);
    const bad: string[] = [];
    for (const f of files) {
      let src: string;
      try {
        src = readFileSync(fileURLToPath(new URL(f, root))).subarray(0, 8 * 1024 * 1024).toString('utf8');
      } catch {
        continue; // 文件不存在则跳过（模块清单允许演进）
      }
      src.split('\n').forEach((line, i) => {
        if (/contains\(\s*'rot-(cw|ccw)'\s*\)/.test(line)) bad.push(`${f}:${i + 1}`);
      });
    }
    expect(bad, `以下位置仍在裸判朝向，请改走 orientOf()：\n${bad.join('\n')}`).toEqual([]);
  });
```

（需要在该文件顶部补 `import { readFileSync } from 'node:fs';` 与 `import { fileURLToPath } from 'node:url';`。）

Run: `npx vitest run tests/ui/fx-orient.test.ts`
Expected: **FAIL**，列出所有裸判定点。

- [ ] **Step 3: 逐处迁移**

**迁移前必读——三个已由 Task 1 评审核实的陷阱，逐条都要在报告里回应：**

1. **`.protocol-img` 在热座页本来就带 `rot-180`**（`render.ts:116`：`'protocol-img' + (player === 1 ? ' rot-180' : '')`）。这些节点今天**没有** `rot-cw`/`rot-ccw`，所以旧写法读到的隐式朝向是 **0°**；迁到 `orientOf` 后会变成 **180°**。这正是新模块想要的语义，但它**会改变现有行为**——凡是节点可能是 `.protocol-img.rot-180` 的迁移点，必须先判断"改成 180° 是否才是正确的观感"，并在报告里逐个说明，用热座零变化检查覆盖。
2. **`effects/index.ts:1732` 的克隆去类只删 `rot-cw`/`rot-ccw`** → 必须同时删 `rot-180`，否则带 180° 的卡的浮层会继承一个不该有的朝向。
3. **`--fx-rot` 只接受裸角度**。`cloneTransformOf` 返回的是**完整 `transform` 函数串**（`rotate(90deg)`），而现有 FX 消费路径写的是**裸角度**（`effects/index.ts:157-158`：`--fx-rot: '90deg'` 然后 `transform: rotate(var(--fx-rot, 0deg))`）。把完整函数串赋给 `--fx-rot` 会得到 `rotate(rotate(90deg))` → 计算值非法 → **整条 inline transform 连同组合的 translate/scale 一起静默丢弃**。所以：直接写 `style.transform` 用完整串，走 `--fx-rot` 用裸角度，**两者不可混用**。

对每个判定点：把

```ts
const cw = node.classList.contains('rot-cw');
const ccw = node.classList.contains('rot-ccw');
… buildFxCardAt(rect, cw, ccw, payload, z)
```

改为

```ts
const { cw, ccw, flip180 } = orientToCwCcw(orientOf(node));
… buildFxCardAt(rect, cw, ccw, payload, z, flip180)
```

**`flip180` 参数先只在 `buildFxCardAt`/`buildFxCard` 的签名上落地并透传到 `--fx-rot`**：现有实现用 `--fx-rot` 组合朝向（见 `styles-gen3-cards.css` 与 `styles.css` 的 `--fx-rot` 用法），180° 就是 `--fx-rot: 180deg`（**裸角度**，不是 `rotate(180deg)`）。若某处实现无法透传，**在报告里列出并说明**，不要静默忽略。

**源码守卫同步加强**：Step 2 的正则只覆盖 `contains('rot-cw')` 形态，还必须覆盖 `classList.remove('rot-cw', 'rot-ccw')` 这类**多参去类**形态与 `contains('rot-180')` 形态（后者在迁移后不应再出现在解析类名的地方，除非该处确实是产出/清理而非判定）。

- [ ] **Step 4: 验证热座语义零变化**

Run: `npx vitest run` → 全绿（总数不降）
Run: `npx tsc --noEmit` → 0 错
Run: `npx vite build` → 成功

**人工检查**：`git diff` 里每一处改动都必须是"同一语义的等价替换"。若某处无法等价替换 → 停下来报告，不要硬改。

- [ ] **Step 5: 提交**

```bash
git add src tests
git commit -m "refactor(G2): 朝向判定统一走 orientOf 单一出处" -m "把散在 effects/index.ts、fx-gen2.ts、fx-gen3.ts 等处的 classList.contains('rot-cw'/'rot-ccw') 裸判定迁到 fx-orient.ts 的 orientOf/orientToCwCcw，并加源码守卫禁止再出现裸判定。热座页语义等价（±90° 行为不变），为远程页的 0/180° 铺路。" -m "产出类名的两处（renderStackSlot / renderProtocol）保持不动。"
```

---

### Task 2 完成后修正（4 个提交，2026-09-14）

Task 2 本身（`0bfafc9`）的实现是对的，但**它的守卫经三轮独立评审判为不可信**，随后三个纯测试提交把它补齐。这段记录必须保留，因为**同样的判断错误会再犯**：

| 提交 | 内容 |
|---|---|
| `0bfafc9` | Task 2 实现（15 处读取点迁移；`(cw,ccw)` 布尔对 → `CardOrient` 单一入参；`buildFxCardAt`/`buildFlipOverlay` 各加 180° 分支；`--fx-rot` 走 `orientToFxRot` 裸角度） |
| `d4a2520` | 修三条守卫可信度问题（见下） |
| `428b9a1` | 修「守卫拒绝合法重构」与「豁免按文件名」 |
| `d6982df` | 封 `--fx-rot` 写入值的**变量洗白通道**；`stripOrientClasses` 守卫钉到真实调用语句 |

**① 编排者的判断错误（必须记住）**：本次任务简报断言「不把契约 `.rot-cw`/`.rot-ccw` 的 `requiredBy` 改指 `fx-orient.ts`，G1 的出处机检必红」——**这是错的，而且错在符号上**。那条断言是裸 `src.includes(probe)` 且**不区分代码与注释**，而 `effects/index.ts` 里 `rot-cw` 只出现在**两行中文注释**（`:115`、`:620`）里，所以**不改也会全绿**；反过来，**只改那两行注释的文字（零代码改动）就立刻变红**。改 `requiredBy` 依然正确（它如实反映新出处），但**理由是「诚实」，不是「否则必红」**。
→ 教训：**当守卫用 `src.includes()` 做「出处」判据时，先问「注释能不能满足它」**。这与 G1 那次「一个读取行一条钩子」的普查错误是同一类：**读起来像已验收，其实什么都没证明**。

**② 三处守卫可信度缺陷（均由变异实测证明，不是纸面推断）**：
- **注释盲区**：`requiredBy` 出处机检被注释满足。→ 加字符扫描式 `stripComments()`（**不能用正则**：字符串里的 `//` 会被误吃），并给它自己补单测。
- **产出方守卫是纸糊的**：`expect(src).toContain('rot-cw')` 会对**整文件**查找，于是把 `render.ts:227` 唯一的产出行改成 `'rot-clockwise'`（**热座所有场上卡立刻直立**）仍 **18/18 全绿** —— 被注释 `:196` 与类名镜像行 `:1443` 满足。→ 改为钉**完整产出表达式**（连 `card.owner === 0` 条件一起钉；只钉类名的话把条件反过来也全绿）。
- **豁免按文件名**：`/^render.*\.ts$/` 既用于豁免又只查 `±90°`，于是新建 `render-*-probe.ts` 只写 `'rot-180'` 时 **20/20 全绿**。→ **发现可以用名字，豁免不应该用名字**：注册表 `RENDERERS` 移入 `src/ui/fx-dom-contract.ts` 并导出，`producers` 改为**按注册表**判定，并补「磁盘上每个 `render*.ts` 都必须在册」「每个注册项都必须在磁盘上存在」两条双向断言。

**③ 两条最重要的守卫设计原则（都来自实测的假红/假绿）**：
- **守卫不得拒绝正确代码**。把 `--fx-rot` 写入值钉死成 `orientToFxRot(orient)` 会拒绝「先存中间变量」（语义完全等价、更易调试）的写法 → 假红。**会被绕过的守卫等于没有守卫**，所以放宽形态是必要的；但放宽**必须由另一条不变量补回保护**（这里是「写入值不得含 `rotate(`」）。
- **放宽到「允许标识符」会开出洗白通道**：`const fxRot = cloneTransformOf(orient); setProperty('--fx-rot', fxRot)` 会让 5a/5b/反空集合**全部通过**，而那条写入会让 `rotate(rotate(90deg))` → **整条内联 transform 静默丢弃**。→ 结论：**「允许间接」必须配一条对该间接来源的全类封杀**（封 `effects/index.ts` 里 `cloneTransformOf` 的**调用形态**；不封裸标识符是因为 `:164` 那条警告注释本身写着这个名字，封裸标识符会逼人删掉警告）。

**④ 接受残留（记录在案，不静默放过）**：
- `stripComments` 两处已知局限：模板串 `${}` 插值里的注释不会被删（**假绿**方向）、正则字面量里含未转义 `//` 会截断该行（**假红**方向）。**当前真实树零命中**，已写成实现注释。
- `effects/index.ts` 的 `cloneTransformOf` **别名 import**（`import { … as cf }`）不被 5c 拦住。需**刻意**引入，非事故形态；更严的一行修法是禁掉该文件对这个符号的 import（待 Task 4 评估）。
- `rotWritesOf` 只认单引号字面量属性名；`writes.length === 2` 是有意的数量耦合（新增第三个朝向分支时须同步更新）。
- 「误传活节点 `stripOrientClasses(node)`」不被拦 —— 但该操作对任意 Element 都安全（只摘三个类名），且只有一个调用点紧邻 `cloneNode`。

**⑤ 顺序约束（Task 4 必须遵守）**：`tests/ui/fx-dom-contract.test.ts` 的 `rendererSources` 在**模块作用域**就对 `RENDERERS` 里每个文件做 `read()` → **登记一个还不存在的文件会让整个测试文件在收集阶段 ENOENT**（`Tests no tests`，而不是走那条清楚的存在性断言）。所以 **Task 3 必须先建出 `render-net.ts`，Task 4 才能把它登记进 `RENDERERS`**。

---

## Task 3: 远程对战页 `src/ui/render-net.ts`（甲读法 + 座位相对 + 单视角预览）

**Files:**
- Create: `src/ui/render-net.ts`
- Create: `src/ui/styles-net.css`
- Test: `tests/ui/render-net.test.ts`

**Interfaces:**
- Consumes: Task 1/2 的朝向助手；G1 契约的 A 类钩子清单
- Produces:
  - `interface NetViewOpts { viewSeat: 0 | 1; handVisibility: 'all' | 'viewSeat'; }`
  - `renderNetBoard(root: HTMLElement, s: GameState, cb: UiCallbacks, opts: NetViewOpts): void`
  - `resetNetUiState(): void`

**布局规格（甲读法，用户已拍板）**：3 条横带，每条带内**上=对手、下=自己**；卡牌：**自己 0°、对手 180°**。

```
┌──────────────────────────────────────────────┐
│ 对手：昵称 · 手牌 ×n · 牌库 n · 弃牌 n · 连接   │
├──────────────────────────────────────────────┤
│ 线1   [对手链路 / 对手协议]                    │
│       ───── 中线 · 线值 · 编译状态 ─────        │
│       [自己链路 / 自己协议]                    │
├──────────────────────────────────────────────┤
│ 线2 / 线3（同构）                              │
├──────────────────────────────────────────────┤
│ 控制轨（横向，自己端 ←→ 对手端）                │
├──────────────────────────────────────────────┤
│ 自己：昵称 · 牌库 n · 弃牌 n · 手牌（正面）      │
└──────────────────────────────────────────────┘
```

**七条硬约束（违反会导致静默退化，逐条都要在报告里回应）**

1. **必须产出全部 19 条 A 类钩子**，且保持**产出方拼写**与热座页一致（`stack-slot p${n}`、`trash-pile p${n}`、`data-player`/`data-line` 走 `dataset`）。参照 `render.ts:208/210/211`、`:1843/1845/1846`、`:60/228/1576`、`:1473/1474`、`:1498/1499`、`:159`、`:1552/1555`、`:58`、`:1819/1820/1825`。
2. **`.rot-cw`/`.rot-ccw` 在远程页不产出**；改为产出**朝向标记**（自己不加类 = 0°，对手加 `.rot-180`）。这需要 Task 4 的契约豁免。
3. **保持"P0 向左长 / P1 向右长"与 `.stack.grow-left`/`.grow-right` 的对应关系按绝对玩家**（`render.ts:213/217-218`），控制轨 slider 仍按绝对玩家映射（`render.ts:1815-1818`）。**不得**改成"上=向左"。理由：`effects/index.ts:191-193`、`fx-gen2.ts:779/878/957`、`gen3-control.ts:654` 都按绝对玩家算方向，改了会让这些静默错位。
4. **within-slot 覆盖方向不得改变**（`gen3-util.ts:51/66` 假设覆盖者在右）。
5. **对手手牌只渲染数量**，但**必须仍然产出 `.hand[data-player=<对手>]` 节点**（哪怕是空的/占位），否则 `querySelectorAll('.hand')[player]` 的 **8 处按下标读取**会取到 `undefined`（另有 `effects/index.ts:1703` 一次取两手）。占位节点需带 `data-hand-count` 供 FX 判断。
6. **座位来源必须是新引入的 `viewSeat`**，不得再用 `s.turnPlayer` 冒充"自己"（`render.ts:207` 等的写法是错的来源）。
7. **【Task 2 侦察补入 · 编排者发现】两条 `.hand` 节点必须以「绝对玩家顺序」出现在 DOM 中：P0 的手牌节点在前，P1 的在后。**

   **为什么这条比「取到 `undefined`」更危险**：FX 读手牌是**按下标**的 —— `document.querySelectorAll('.hand')[player]`
   （`effects/index.ts:849/942/1541/1590/1650`、`:1683` 一次取两手，以及 `fx-gen2.ts:693/1316/1786`），
   **不是**按 `data-player`。而甲读法把「对手」放在**上方带**：`viewSeat = 0` 时对手是 P1，上带是 P1，
   若按视觉顺序自然挂载就得到 DOM 顺序 `[P1, P0]` → **下标 0 拿到的是对手的手牌节点**。
   这时特效**不报错、不跳过**，而是把卡飞到**对手手牌区**（比 `undefined` 更难发现 —— 后者至少会被守卫吞掉）。

   **要求的做法**：把两条 `.hand` 放进**同一个父容器**，DOM 顺序**恒定**为 `[P0 的手牌, P1 的手牌]`；
   「谁显示在上带」由父容器上的座位类用 **CSS** 决定（`order` / `grid-area` / 绝对定位均可，
   **不得** `display:none` —— 取不到 rect）。

   > 已核对：`src/ui/` 里除 `.hand` 外**没有任何**契约钩子被按下标读取（`git grep` 确认）；
   > 也全仓扫过 `children[0]`/`firstElementChild`/`previousElementSibling`/`compareDocumentPosition`/`parentElement`/`offsetLeft`/`nth-child`
   > —— 命中**全部是卡片内部的装饰性定位**（相对卡自身），与本页布局无关。故这是唯一的顺序隐患。

**复用 / 分叉清单（已逐函数核实，直接照此处置）**

| 助手 | 处置 | 依据 |
|---|---|---|
| `el` `:39`、`renderCardFace` `:57`、`renderProtocolCell` `:1842`、`renderBattery` `:156`、`renderDeck` `:1470`、`renderTrash` `:1495`、`renderRefreshButton` `:1517`、`choiceBar` `:5097`、`buildChoicePickOverlay` `:5112`、`playToLine` `:4528`、`bindCardDrag` `:5463`、`openZoom` `:5204`、`buildCardTextEl` `:5186` | **原样复用**（纯 `(player,line)` 或按 `data-player` 定位，与左右布局无关） | 侦察逐条核对；`bindCardDrag` 只按 `.stack-slot` + `dataset` 定位（`:5514-5515, 5543-5545`） |
| 全部 `sync*` 导出 | **原样复用**（无需改动） | 只按契约钩子找节点（`render.ts:293/404/455/507/565/766/846/1009/1095/1148` 等） |
| `renderStackSlot` `:198` | **加参数** | `isSelfSlot = player === s.turnPlayer`（`:207`）要换成座位；`.stack.grow-left/.grow-right` 与顺序（`:213, 217-218`）、`rot-cw/ccw`（`:227`）需要显式朝向入参 |
| `renderHand` `:1538` | **加参数** | `const reversed = player === 1`（`:1551`）与 `:1561-1564` 的排序/反转 |
| `renderProtocol` `:82` | **加参数** | `player === 1 ? ' rot-180'`（`:116`）—— 正立/180° 要成为入参 |
| `renderPlayerInfo` `:1448` | **加参数** | `p${player + 1}`（`:1452`）与 `.player-info.p2` 右对齐（`styles.css:30-31`） |
| `renderControlModule` `:1813` | **加参数或不改** | slider 4%/96% 与标签（`:1815-1822`）。**建议不改**（约束 3 要求保持绝对玩家） |
| `renderBoard` 本体 `:4572-4979` | **必须另写** | 整个带装配 + 它改的模块态（`:4981/4982/4988/4993-4994`）+ 三个 `choice-*` 分支（`:4775-4913`）；设计稿 §6.1 明确"不复用热座页" |
| `renderShield` `:1731` / `bindShieldDrag` `:1747` | **远程页不调用** | 设计稿 §6.1 已删挡板 |

**CSS 改动面（`styles.css` 只读参考，改动一律写进新的 `styles-net.css`）**

| 规则 | 行 | 现状 → 远程页需要 |
|---|---|---|
| `.lane-row` | `:57` | grid 4 列 `minmax(660px,1fr) auto auto minmax(660px,1fr)` → **带内 2 行**（上对手/下自己） |
| `.stack-slot.p1/.p2` | `:73-74` | `padding-right/left:25px` 是"协议在旋转卡的另一侧"的补偿 → 正立后须重新推导 |
| `.battery` | `:161-179, 199-200` | 绝对定位在 `.stack-slot`（`left/right:-120px`）→ 语义仍按绝对玩家，沿用 `.p1`/`.p2` |
| `.deck[data-player]` | `:761-763` | 绝对定位在 `.hand-side`（`-100px`）→ 远程页只有一个手牌区，对手牌库须移到顶部信息条 |
| `.trash-pile.p1/.p2` | `:824-835` | 同上 |
| `.hand-strip` | `:724` | grid `1fr auto 1fr` → 单列 |
| `.hand.reversed` | `:875, 882, 926` | 单手下成为死规则 |
| `.rot-cw/.rot-ccw/.rot-180` | `:454-457` | 远程页用 0°/180° |
| `.hand-shield*` | `:959-1010` | 远程页不需要 |

**读源码的起点**：`src/ui/render.ts:4560-5000`（`renderBoard` 及其调用的助手）与 `src/ui/styles.css` 上表列出的规则。侦察已确认 19 条 A 类钩子在战场页**无一遗漏**。

### Task 3 前置补正（Task 2 侦察补入，**开工前必须先看这一段**）

**① `playToLine` / `bindCardDrag` 不是「原样复用」，它们需要先导出共享状态。**
上表把它们列在「原样复用」里 —— **做不到**：`render.ts:4981-4982` 的 `let selectedUid` / `let selectedFaceUp` 是**模块私有**的，
而 `playToLine`（`:4528-4544`）**直接读写它们**（`if (!selectedUid) return; … selectedUid = null;`），
`bindCardDrag`（`:5463-5569`）也读写（`:5503`、`:5552-5553`）并依赖 `choicePromptId` / `activeDragCancel`。
远程页要用它们，就必须能读写**同一份**选择态。要求：
```ts
export function getHandSelection(): { uid: string | null; faceUp: boolean };
export function setHandSelection(uid: string | null, faceUp?: boolean): void;
/** 清除失效选择（所选卡已不在当前回合玩家手牌中）。原为 renderBoard 内联（:4581-4585），抽出后两页共用。 */
export function pruneSelection(s: GameState): void;
```
并把 `renderBoard:4581-4585` 的内联守卫**改为调用 `pruneSelection(s)`**（单一实现）。
**不允许**：在 `render-net.ts` 里另写一套"选中→校验合法→派发 play"的逻辑（会与 `playToLine` 的合法性与复位语义发散）；
也不允许把 `selectedUid` 改成 `export let`（ES 模块 live binding 对导入方只读，改不了）。

**② `buildChoicePickOverlay` 内部硬编码了 `renderApp`（`render.ts:5153`）→ 远程页复用它时，点候选卡会整页跳回热座棋盘。**
要求给 `UiCallbacks` 加**可选**成员 `rerender?(): void`，并把 `:5153` 改为 `if (cb.rerender) cb.rerender(); else renderApp(root, s, cb);`
（未提供时回退 → 既有调用方语义完全不变）。`renderBoard` 体内另外 9 处 `renderApp(...)`（`:4665/4674/4680/4687/4706/4715/4721/4728/4806`）
都在**远程页不会调用**的 `renderBoard` 里，**不要动**。

**③ 预览工具条（为了让「≥20 个点名特效抽查」做得下去）**
`handVisibility: 'viewSeat'` 时对手手牌只剩数量占位 → **轮到对手时无人可操作，预览会卡死**；而 `viewSeat` 固定 0
又无法检查「我是 P2」时上下带与 180° 是否也对。所以在 `NetViewOpts` 上加**可选**回调、并在它**存在时**渲染一条固定在角落的预览工具条（真实联机不传 → 工具条不渲染）：
```ts
export interface NetViewOpts {
  viewSeat: 0 | 1;
  handVisibility: 'all' | 'viewSeat';
  onPreviewChange?(next: { viewSeat?: 0 | 1; handVisibility?: 'all' | 'viewSeat' }): void;
}
```
两个开关：「视角：我 = P1 ⇄ P2」与「对手手牌：只显示数量 ⇄ 全部可见」。

**④ 附录 A.3 的行号已因 Task 2 推移，且只有行号不够** —— 下表是**当前**（`d6982df`）的准确位置与所在函数：

| 位置 | 函数 | 退化形态 |
|---|---|---|
| `effects/index.ts:849` | `playReturn` | 回手飞往**持有者**手牌末尾 |
| `effects/index.ts:942` | `playWaterReturn` | 同上 |
| `effects/index.ts:1541` | `playSpeedShiftExtra` | speed 偏转终点 = 该玩家手牌末尾 |
| `effects/index.ts:1590` | `playLoveDrawExtra` | love 抽牌落点 |
| `effects/index.ts:1650` | `playLoveGiveExtra` | love 给牌落点 |
| `effects/index.ts:1683` | `playRevealFly`（一次取**两手**） | 揭示飞行的起点与终点 |
| `fx-gen2.ts:693` | `spawnClarityCardEye` | `undefined` 被守卫吞掉 → 效果静默跳过 |
| `fx-gen2.ts:1316` | `courageLandPos` | `undefined` 传进落点计算 |
| `fx-gen2.ts:1786` | `handLandingPos` | 同上 |

→ 硬约束 5（对手也必须产出 `.hand` 占位节点）**只解决了「取到 `undefined`」这一半**；
**「取错手牌」那一半必须靠硬约束 7 的 DOM 顺序**。这两条是配套的，缺一条就会静默退化。

- [ ] **Step 1: 写失败的测试（源码守卫，无 jsdom）**

创建 `tests/ui/render-net.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { FX_DOM_CONTRACT, hooksOfCategory } from '../../src/ui/fx-dom-contract';

const root = new URL('../../src/ui/', import.meta.url);
const read = (rel: string): string =>
  readFileSync(fileURLToPath(new URL(rel, root))).subarray(0, 8 * 1024 * 1024).toString('utf8');

describe('G2 · 远程对战页', () => {
  it('文件存在且导出 renderNetBoard / resetNetUiState', () => {
    const src = read('render-net.ts');
    expect(src).toContain('export function renderNetBoard');
    expect(src).toContain('export function resetNetUiState');
  });

  it('产出全部 A 类钩子的判别 token', () => {
    const src = read('render-net.ts');
    const missing: string[] = [];
    for (const h of hooksOfCategory('A')) {
      // 与契约守卫同口径：优先用显式 probe
      const tokens = h.probe ?? [h.hook.startsWith('[') ? /^\[([a-z-]+)/.exec(h.hook)?.[1] ?? h.hook : h.hook.split('[')[0].replace(/^\./, '')];
      for (const t of tokens) if (!src.includes(t)) missing.push(`${h.hook} 的 token ${t}`);
    }
    expect(missing, `远程页缺少以下 A 类 token：\n${missing.join('\n')}`).toEqual([]);
  });

  it('不产出热座专属的 ±90° 朝向类（改用 rot-180）', () => {
    const src = read('render-net.ts');
    expect(src).not.toMatch(/['"]rot-cw['"]/);
    expect(src).not.toMatch(/['"]rot-ccw['"]/);
    expect(src).toContain('rot-180');
  });

  it('不得用 s.turnPlayer 冒充"自己"', () => {
    const src = read('render-net.ts');
    expect(src, '远程页的"自己"必须来自 viewSeat，不能用 turnPlayer').not.toContain('s.turnPlayer ===');
    expect(src).toContain('viewSeat');
  });

  it('对手手牌必须仍产出 .hand 占位节点（供 FX 按索引取手牌）', () => {
    const src = read('render-net.ts');
    // 两手都要有 .hand 产出路径
    expect((src.match(/'hand'/g) ?? []).length).toBeGreaterThanOrEqual(2);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/ui/render-net.test.ts`
Expected: FAIL —— `render-net.ts` 不存在。

- [ ] **Step 3: 实现 `render-net.ts`**

**复用清单（已由侦察核实"加参数即可"）**：从 `render.ts` 导出并在远程页复用
`renderCardFace`、`renderProtocolCell`、`renderBattery`、`renderDeck`、`renderTrash`、`renderRefreshButton`、`choiceBar`、`buildChoicePickOverlay`、`playToLine`、`bindCardDrag`、`openZoom`、`buildCardTextEl`，以及全部 `sync*`。

**必须自己写**：顶/底信息条、3 条带的组装（上对手/下自己）、中线（线值/编译状态）、控制轨容器、手牌区（自己正面 + 对手数量占位）、单视角预览的座位映射。

**必须不要在远程页调用**：`renderShield` / `bindShieldDrag`（设计稿 §6.1 已删挡板）。

入口的四件事要与 `renderApp` 对齐（`render.ts:5583-5599`）：加 `no-anim` 类、`syncCheckCacheChains(s)`、`syncChainLayerPosition()`、`cb.onRendered?.()`，双 rAF 后移除 `no-anim`。

- [ ] **Step 4: 实现 `styles-net.css` 并在 `main.ts` 引入**

3 条横带用 flex column；每条带内部用 **2 行**（上=对手、下=自己）；`.rot-180{transform:rotate(180deg)}`；`.hand` 单列；对手牌库/弃牌堆放在顶部信息条内；`.battery`、`.deck`、`.trash-pile` 的绝对定位需按新布局重新给值（可覆盖 `styles.css` 的规则，**不要改 `styles.css`**）。

在 `src/main.ts` 顶部 `import './ui/styles-net.css';`（或按项目既有 CSS 引入方式）。

- [ ] **Step 5: 跑测试 + 提交**

Run: `npx vitest run tests/ui/render-net.test.ts` → PASS（5 项）
Run: `npx tsc --noEmit` → 0 错；`npx vitest run` → 全绿；`npx vite build` → 成功

```bash
git add src tests
git commit -m "feat(G2): 新增远程对战页渲染器 render-net（甲读法 + 座位相对）" -m "三条横带、带内上=对手/下=自己；自己卡正立、对手卡 180°；对手手牌只显示数量但仍产出 .hand 占位节点（否则 FX 的 6 处按索引取手牌会静默退化）；保持 P0 向左长/P1 向右长与覆盖方向按绝对玩家不变；座位来源为 viewSeat，不用 turnPlayer。" -m "复用 render.ts 的卡片/协议/电池/牌库/弃牌/选择条/拖拽等助手；不复用 renderBoard 本体与挡板。"
```

---

## Task 4: 接线与契约配套（预览入口 + 契约按渲染器区分 + 重置）

**Files:**
- Modify: `src/ui/fx-dom-contract.ts`（**`RENDERERS` 登记 `render-net.ts` 与 `exempt`** + 新增 `.rot-180` 钩子）
- Modify: `docs/4代-FX DOM 契约.md`（登记 `.rot-180` + 「按渲染器区分要求」小节）
- Modify: `src/main.ts`（重渲染路由 + 预览入口 + `resetNetUiState` 挂载 + 预览模式复位）
- Modify: `src/ui/home.ts`（模式选择页加一张预览卡；`ModeSelectNav` 加 `startNetPreview`）
- Create: `tests/ui/net-preview-wiring.test.ts`（源码守卫）

### Task 4 前置补正（**本节范围已被 Task 2/3 修正，以此为准**）

1. **`render.ts` 的导出与座位化已全部归入 Task 3**（导出是 Task 3 的硬前置，不可能放到 Task 4）。
   计划原文要求把 6 处 `s.turnPlayer` 冒充「自己」（`:207, 4603, 4605, 4660, 4701, 4718`）座位化 ——
   其中 **5 处（`:4603/4605/4660/4701/4718`）都在 `renderBoard` 体内**，而远程页**根本不调用 `renderBoard`**
   （设计稿 §6.1 + 复用表都明确「本体必须另写」）→ **按 YAGNI 不改**；剩下的 `:207`（在 `renderStackSlot` 内）已由 Task 3 参数化。
   **本任务不再修改 `src/ui/render.ts`。**
2. **`RENDERERS` 的所在地已变更**：Task 2F2 起它导出在 **`src/ui/fx-dom-contract.ts`**
   （`export interface FxRenderer { file: string; exempt?: readonly string[] }` + `export const RENDERERS`），
   两个测试文件都 **import** 它。**不要**在测试文件里再建本地副本，也**不要**再用 `as readonly string[]`（它会绕过类型检查）。
3. **顺序约束**：`tests/ui/fx-dom-contract.test.ts` 的 `rendererSources` 在**模块作用域**就对 `RENDERERS` 里每个文件 `read()`
   → **先建文件、再登记**。Task 3 已建出 `render-net.ts`，故本任务登记它是安全的；但**若把它写成两步（先登记后建文件）会让整个测试文件 ENOENT**。
4. **预览入口的形态已修正**：原来是「在模式选择页加一项」；现在是**一张卡 + 预览页内的工具条**
   （工具条由 `NetViewOpts.onPreviewChange` 是否存在决定，理由见 Task 3 前置补正 ③：固定 `viewSeat`/固定手牌可见性会让预览卡死或漏检）。
5. **新增两处小加固（来自 Task 2 终审的延后清单）**：
   - `exempt` 的键必须**真的是 A 类钩子**（拼错会静默豁免掉一条本该验收的钩子）—— 本任务是首次真正使用 `exempt`，加上这条。
   - `rg` 检查：`src/ui/effects/index.ts` 对 `cloneTransformOf` 的 **import**（别名 import 可绕过 Task 2F3 的调用形态封杀；
     该文件对它的合法用途为**零** —— 唯一生产消费者是 `src/ui/fx/clone-orient.ts`）。一行断言即可。

**Interfaces:**
- Consumes: Task 3 的 `renderNetBoard` / `resetNetUiState`
- Produces:
  - 契约：`RENDERERS: readonly { file: string; exempt?: readonly string[] }[]`
  - `main.ts`：一个"单视角预览"入口（零联机）

- [ ] **Step 1: 契约改为按渲染器区分要求（先失败）**

把 **`src/ui/fx-dom-contract.ts`** 里已导出的 `RENDERERS`（Task 2F2 起移居于此；**不是**测试文件里的本地常量）改为：

```ts
export const RENDERERS: readonly FxRenderer[] = [
  { file: 'render.ts' },
  // G2：远程页刻意不产出 ±90° 朝向类，改用 rot-180（见 docs/4代-FX DOM 契约.md）
  { file: 'render-net.ts', exempt: ['.rot-cw', '.rot-ccw'] },
];
```

测试侧只需：①「A 类钩子必须被当前渲染器提供」已按 `r.exempt` 跳过（Task 2F2 已接好通路，**本任务只需确认它生效**）；
② 新增一条「`exempt` 的每一项都必须是真实的 A 类钩子」（拼错键会静默豁免掉一条本该验收的钩子 —— 本任务是首次真正使用 `exempt`）；
③ 磁盘发现守卫已按 `.file` 比较（**不得**再出现 `as readonly string[]`）。

把 `.rot-180` 登记进契约（`category: 'A'`，`requiredBy` 指向远程页与 FX 的朝向消费者）。**注意**：`.rot-180` 在热座页也会产出（`render.ts:116` 的 P1 协议图），所以它对两个渲染器都成立，**不需要**豁免；把它登记为 A 后，热座页也必须含它的 token——先确认 `render.ts` 里有，否则如实报告。

Run: `npx vitest run tests/ui/fx-dom-contract.test.ts` → 期望先红（`render-net.ts` 未登记时会由发现守卫报出；登记后若 token 缺失则逐条报出）。

- [ ] **Step 2: 在 `main.ts` 加零联机预览入口 + 重渲染路由**

**预览必须"可玩"**，否则用户做不了「≥20 个点名特效抽查」（那是 G2 验收第 3 项）。所以不能只在进入时调一次 `renderNetBoard`：

1. 模块态：`let renderMode: 'hotseat' | 'net' = 'hotseat'`、`let netViewSeat: PlayerId = 0`、`let netHandVisibility: 'all' | 'viewSeat' = 'viewSeat'`。
2. **唯一的整帧重渲染入口** `rerender()`：`renderMode === 'net' && state.phase !== 'draft'` → `renderNetBoard(root, state, cb, { viewSeat: netViewSeat, handVisibility: netHandVisibility, onPreviewChange })`；否则 `renderApp(root, state, cb)`。
   `main.ts` 里**所有** `renderApp(root, state, cb)` 调用点改为 `rerender()`（`main.ts:132` 的 `renderDraft(...)` 保持直调；`src/ui/render.ts` 内的一律不动）。
   并在 `cb` 里加 `rerender() { rerender(); }`（Task 3 已给 `UiCallbacks` 加了可选的 `rerender?()`）。
3. `onPreviewChange(next)`：合并进 `netViewSeat`/`netHandVisibility` 后 `rerender()`。
4. 入口：`src/ui/home.ts` 的 `ModeSelectNav` 加 `startNetPreview(viewSeat, banEnabled, randomPoolEnabled)`，`renderModeSelect` 的 `list` 里加**一张**卡「单视角预览（本地）」
   （描述里写清：远程对战页布局预览、上方是对手/下方是你、你的卡正立/对手的卡倒置、仍是本地热座流程零联机、页内工具条可切视角与手牌可见性）。
   `main.ts` 的 `showModeSelect` 实现它：设 `renderMode = 'net'` + `netViewSeat` → 走现有 `showCoin()` 流程（草稿阶段仍走热座草稿页，`phase !== 'draft'` 守卫负责切换）。
   同时 `startHotseat` 里显式 `renderMode = 'hotseat'`。

- [ ] **Step 3: 重置（含预览模式复位）**

`resetToMainInterface`（`main.ts:526-542`）里：在 `resetUiState();` 旁调用 `resetNetUiState();`，
并复位 `renderMode = 'hotseat'`、`netViewSeat = 0`、`netHandVisibility = 'viewSeat'`。
**不复位会让"打完一局预览 → 返回主页面 → 开热座"渲染成远程页。** 报告里说明放置位置与理由。

Run: `npx vitest run` → 全绿（热座相关断言不得变化）

- [ ] **Step 4: 更新契约文档**

在 `docs/4代-FX DOM 契约.md` 里：登记 `.rot-180`；新增「按渲染器区分要求」小节，写明 `render-net.ts` 豁免 `.rot-cw`/`.rot-ccw` 的理由（自己正立/对手 180°，±90° 会交换布局盒宽高故不适用）；并把 G2 的验收用法从"把远程页加入 RENDERERS"改为"已加入，跑 `npx vitest run tests/ui/fx-dom-contract.test.ts` 即得结论"。

- [ ] **Step 5: 全量验证 + 提交**

Run: `npx tsc --noEmit` → 0 错；`npx vitest run` → 全绿；`npx vite build` → 成功；`npm run texts:check` → 差异 0 处；`npx vitest run tests/docs tests/ui` → 绿

```bash
git add src tests docs
git commit -m "feat(G2): 接入远程页预览入口，契约改为按渲染器区分要求" -m "契约新增 .rot-180 并把 RENDERERS 改成带 exempt 的结构：render-net.ts 有意不产出 ±90° 朝向类（自己正立/对手 180°），豁免理由写进契约文档。main.ts 加零联机单视角预览入口并在 resetToMainInterface 挂 resetNetUiState。render.ts 把 6 处用 turnPlayer 冒充「自己」的写法改为座位参数，默认值保持热座语义不变。" -m "本任务完成后，远程页是否满足契约由 npx vitest run tests/ui/fx-dom-contract.test.ts 自动判定。"
```

---

## 用户验收（G2 完成后必须由用户做，不得由代理代替）

1. 在 5173 上进入**单视角预览**，确认：3 条横带、带内上=对手/下=自己、自己的卡正立、对手的卡倒置、对手手牌只显示数量。
2. 打一局热座，确认**热座页观感与之前完全一致**（本阶段不得影响热座）。
3. **≥20 个点名特效抽查**（契约文档 §6 要求的硬性项，因为源码守卫只能证明"token 在文件里"，证明不了运行时行为）。重点抽查：
   - 依赖 `.stack-slot` 的飞行/连接件（slotRectOf 落点）；
   - 依赖 **±90°/180° 朝向**的浮层卡（贪婪/嫉妒/愤怒的克隆卡是否尺寸与朝向都对）；
   - 依赖**按下标取手牌**的 8 处效果（`fx-gen2.ts:693/1316/1786`、`effects/index.ts:849/942/1541/1590/1650`）—— 对手手牌只剩占位节点，确认它们**没有静默跳过或飞到错误坐标**。

## 明确不做（属后续阶段）

- 不写 `MatchDriver` 会话层（G4）、不做信令/传输（G5）、不做回合计时（G6）、不做观战（G7）、不做 PWA 与多端打包（G3/G8）。
- 不改 `renderBoard` 的视觉结果，不删热座挡板。
- 不为远程页做真实联机（本阶段只做单视角预览）。

---

## 附录 A：侦察原始结论（G2 开工前的只读普查，避免重查）

以下均已在源码逐条核对。**上下文压缩后 Task 3/4 可直接照此施工，不必重读 `render.ts:4560-5000`。**

### A.1 19 条 A 类钩子的产出点（战场页**无一遗漏**）

| 钩子 | 产出（file:line + 表达式） | 从 `renderBoard` 的到达处 |
|---|---|---|
| `.stack-slot[data-player][data-line]` | `render.ts:208` `` el('div', `stack-slot p${player + 1}…`) ``；`:210` `slot.dataset.line`；`:211` `slot.dataset.player` | `:4627`、`:4637` |
| `.protocol-cell[data-player][data-line]` | `render.ts:1843` `el('div','protocol-cell')`；`:1845` `cell.dataset.player`；`:1846` `cell.dataset.line` | `:4634`、`:4635` |
| `.protocol-img` | `render.ts:116` `img.className = 'protocol-img' + (player === 1 ? ' rot-180' : '')` | `renderProtocol` `:82` |
| `.protocol` | `render.ts:83` `el('div', 'protocol' + (p.compiled ? ' compiled' : ''))` | 同上 |
| `.protocol-holder` | `render.ts:86` `el('div', 'protocol-holder')` | 同上 |
| `[data-uid]` | `render.ts:60` / `:228`（链）/ `:1576`（手牌） `…dataset.uid = card.uid` | `:4627/4637`、`:4659/4700` |
| `.trash-pile[data-player]` | `render.ts:1498` `` el('div', `trash-pile p${player + 1} trash-…`) ``；`:1499` `trash.dataset.player` | `:4656`、`:4736` |
| `.trash-pile.p1/.p2` | 同上 `:1498` | `:4656`、`:4736` |
| `.deck[data-player]` | `render.ts:1473` `` el('div', `deck deck-${…}`) ``；`:1474` `deck.dataset.player` | `:4655`、`:4737` |
| `.battery` | `render.ts:159` `` el('div', `battery battery-${state}`) `` | `renderStackSlot` `:254` |
| `.hand` | `render.ts:1552` `el('div', 'hand' + …)` | `:4659`、`:4700` |
| `.hand[data-player]` | `render.ts:1555` `hand.dataset.player` | 同上 |
| `.card` | `render.ts:58` `el('div', 'card')` | 所有出牌路径 |
| `.rot-cw` / `.rot-ccw` | `render.ts:227` `node.classList.add(card.owner === 0 ? 'rot-cw' : 'rot-ccw')` | `renderStackSlot` |
| `img` | `render.ts:65`、`:74`、`:113`、`:1824`、`:5132` | 卡面 / 协议 / 控制滑块 / 选择浮层 |
| `.control-module` | `render.ts:1819` `` el('div', 'control-module' + …) `` | `renderBoard` `:4604` |
| `.control-slider-img` | `render.ts:1825` `img.className = 'control-slider-img'` | 同上 |
| `.control-track` | `render.ts:1820` `el('div', 'control-track')` | 同上 |

### A.2 P0/P1 方向性硬编码清单（Task 3 必须**保持按绝对玩家**，不得改成"上=向左"）

| 位置 | 证据 |
|---|---|
| `:207` | `const isSelfSlot = player === s.turnPlayer`（"自己"的错误来源） |
| `:213, 217-218` | `.stack.grow-left`（P0）/ `.grow-right`（P1）与渲染顺序反转 |
| `:227` | `card.owner === 0 ? 'rot-cw' : 'rot-ccw'` |
| `:116` | 协议图 P1 才加 `rot-180` |
| `:1551-1564` | `const reversed = player === 1` + 手牌排序/反转 |
| `:1815-1822` | 控制轨 slider `control === 0 → 4%`、`control === 1 → 96%`，标签"玩家 1 / 玩家 2" |
| `:1732, 1753` | 挡板侧别 `player === 1 ? ' p2' : ' p1'`，拖拽方向 `player === 0 ? 1 : -1` |
| `styles.css:57` | `.lane-row` 4 列 = [P0 槽][P0 协议][P1 协议][P1 槽]（DOM 顺序即此） |
| `styles.css:73-74 / 92-94 / 454-455 / 457 / 762-763 / 834-835 / 974-975 / 178-179 / 199-200` | 见正文「CSS 改动面」表 |
| `effects/index.ts:191-193`、`fx-gen2.ts:779/878/957` | 链端点 `owner === 0 ? right-90 : left+90` |
| `gen3-control.ts:654` | 控制轨侧别硬编码 `to === 0 ? 0.04 : 0.96` |
| `gen3-util.ts:5-6, 51, 66` | 覆盖方向假设"覆盖者在右" |
| `render.ts:1732` | 克隆去类只删 `'rot-cw','rot-ccw'`（**需补 `rot-180`**） |

**`src/` 内不存在任何座位/视角抽象**：`grep viewSeat|viewerSeat|mySeat|seatIndex|NetDriver|LocalDriver|driver.seat` → **0 命中**。设计稿 §6.2 的 `.seat-self`/`.seat-foe` 仍是计划。

### A.3 8 处「按下标取手牌」（另 1 处一次取两手）——对手手牌变数量占位后会**静默退化**

> 注（G2 Task 3F 订正）：本节原写「6 处」且行号是 Task 2 侦察时的旧值；下面的数字与行号已按 3F 的
> `git grep -n "querySelectorAll" -- src/ui` 复核订正。

| 位置 | 表达式 | 退化形态 |
|---|---|---|
| `fx-gen2.ts:693, 1316, 1786` | `querySelectorAll('.hand')[player]` | `undefined` 被守卫吞掉（`:694, 1317, 1787`）→ **效果静默跳过** |
| `effects/index.ts:849, 942, 1541, 1590, 1650` | `querySelectorAll('.hand')[payload.owner]` | `undefined` 传进 `handEndPos` → 退化为错误矩形（`handEndPos` 在 `:186`）→ **飞到错误坐标** |
| `effects/index.ts:1703`（playRevealFly） | `querySelectorAll('.hand')`（一次取两手） | 需要两手都在，否则直接 `done()` 跳过飞行 |

→ Task 3 硬约束 5（对手也必须产出 `.hand` 占位节点）就是为这几处兜底。

### A.4 另外三条待办级发现

1. **`syncPersistentFx`（`main.ts:656-688`）漏了 `syncDiversityColors`**，而 `renderBoard:4971` 会调它 → 滚动/缩放时多样性配色层不同步。**既有问题，非 G2 引入**，但远程页若也依赖该清单会继承这个缺口。
2. **`.pN` 拼写是承重的**：`.stack-slot.p1/.p2`、`.hand-side.p1/.p2` 与 `.trash-pile.pN` 共用 `pN`，而契约刻意不把裸 `p1` 当探针（它也命中 `hand-shield p1`、`draft-preview p1`）。远程页必须**保持产出方拼写**（`stack-slot p${…}`、`trash-pile p${…}`），不要改成复合类名。
3. **契约守卫只能证明 token 在文件里**：新渲染器若只 *查询* `data-player` 而从不 *写入*，守卫照样通过。19 条钩子的真实验证只能靠运行时——即「用户验收」里的 ≥20 个点名特效抽查。
