# 3代 评审收口（90 卡效果全部实现，2026-09）

> 范围：3代 MN03 12 套 + AX03 3 套 = 15 协议 90 卡效果全量实现 + 控制权规则化（会话早段）。
> 卡文权威（唯一）= `E:\studyE\compile\正版compile\compile3\compile3文本.txt`；**`译世界3汉化.txt` 有误弃用**（用户 2026-09 明确）。
> 验证：**674/674 测试全绿（69 文件）**、tsc clean、vite build clean（控制权任务段 610 → 批1 638 → 批2 657 → 批3 674）。
> 状态：本地未提交（自 71e2d12 后全部改动在工作树）；推送需用户指示。

## 1. 控制权规则化（会话前段，独立任务）

- 判定改单向（规则文本「控制权判定」/FAQ 84-86）：控制阶段只查当前行动玩家，满足（≥2 线总值高于对手）→ 获得；不满足保持现状；失权只有三条路（自己编译/补满归还、对手自己回合夺取、卡牌效果）。
- 控制条滑块三态离散大幅移动（绑定 s.control：中立居中 50%、P1 4%、P2 96%）。
- 编译/补满持控制权 → 归还中立 + 重排模态（选侧 → 两两交换 → 继续）；卡牌效果触发的刷新/编译 → 效果栈内挂起重排选择（controlRearrangeFlow：love-2/spirit-0/assimilation-1/war-1/unity-1/greed-1/sloth-1）。
- war-1 语义修正（用户拍板：效果主体 = war-1 拥有者「你」，强制出来的刷新 = 刷新自己的手牌；顺带消除递归）。
- 相关文件：rules/control.ts、ui/control-rearrange.ts、render.ts 控制条、main.ts 拦截、rules/control-rearrange-flow.ts。

## 2. 三批规格与裁决（详细见各批文档 §6-7）

- **批1**（嫉妒/暴食/贪婪/色欲/傲慢）：`docs/3代-批1-规格与裁决清单.md`（RQ1-8 全按推荐）。
- **批2**（怠惰/愤怒/伏击/支点/压制）：`docs/3代-批2-规格与裁决清单.md`（B1-10；⚠️ B2 含被盖的「所有正面朝上的牌」；B3 失去控制权必做）。
- **批3**（动量/新星/惰性/刚性/柔性）：`docs/3代-批3-规格与裁决清单.md`（C1-11；C2 删该线双方；C7 区域禁用全禁）。

## 3. 引擎扩展清单（3代 基建）

| 能力 | 说明 |
|---|---|
| 控制权统一变更点 setControl | 一切获得/失去/归还走此；成为 X → 对手侧 after-opponent-gain-control（色欲4 底/傲慢6 顶） |
| 触发方向表 self/opp/both | triggers FIRE_DIR 显式化 + 8 新 kind（after-opponent-gain-control / after-self-compile / after-any-compile / after-any-clear-cache / after-own-delete / after-self-rearrange / after-any-rearrange / after-action-face-down-play）；types TriggerKind 同步 |
| 编译连锁补全 | compile-body 发 after-compile（opp 已有）+ after-self-compile + after-any-compile；**compile action 补 runStack**（修 2代 after-compile UI 悬挂）|
| 删除执行者侧触发 | delete op 补 after-own-delete（贪婪0 底）|
| 区域禁用指令 | inertia-0 顶禁顶 / inertia-1 底禁底（C7 全禁：collectTriggers/fireReactive/resolveTrigger/fireDirectedTop/值修正/全部常驻守卫接线）——context.cardCommandDisabled |
| 不可翻移 | rigidity-7 底（仅 faceUp 顶卡免疫；flip/shift op 守卫跳过+log，ice-4 模式）|
| 重排事件 | rearrangeProtocols/reorderProtocols/控制组件重排路径统一 fire after-self/any-rearrange |
| 行动反打触发 | PendingLanding.fromAction + s.pendingActionPlayLine + after-action-face-down-play（刚性2 底）|
| E6 toDeckBottom | 手牌 1 张放回牌库底（怠惰2 底）|
| E7 discardWholeDeck | 弃整库单次批量（惰性4；C9）|
| E11 playFromHand belowUid | 手牌卡垫某卡正下方（刚性3）|
| lust-0/lust-2 守卫 | 持控禁对手行动编译 / 此堆叠无视协议正面打（含区域禁用感知）|
| runStack 幂等 | 栈顶已有挂起 prompt 时不重复驱动（compile case 补 runStack 后发现的保护）|
| 自盖中断规避 | overwhelm-1/momentum-0/nova-0 顶 等多目标连续反打/删除：源卡所在线/自身排最后（sourceValid 限制）|

## 4. 卡效果文件（15 个，src/core/effects/cards/）

envy/gluttony/greed/lust/pride（批1）；sloth/wrath/ambush/fulcrum/overwhelm（批2）；momentum/nova/inertia/rigidity/flexibility（批3）。已 import resolve.ts 副作用区。

## 5. 测试

- tests/rules/control.test.ts（单向判定 12 例）；tests/rules/rearrange.test.ts（6）；tests/effects/control-rearrange-flow.test.ts（5）；gen3-batch1（23）/batch2（19）/batch3（17）。
- 控制条/重排模态为 UI（无自动化断言，观感由用户 5173 目检）。

## 6. 遗留 / MINOR（待用户反馈或后续排期）

1. **3代 效果观感目检（用户 5173，尚未做）**：devmode `get 色欲0/新星0/惰性0/刚性7` 等逐套体验；控制权效果（获得/失去/获得后连锁）、区域禁用（惰性0/1）、不可翻移（刚性7）、重排事件（动量1/新星2）、结束编译（贪婪1）、belowUid（新星0 底/刚性3）、行动反打（刚性2）。
2. rigidity-7 顶「对手打出1张牌」：当前实现 = 对手选卡 + 选线【反面】打出（简化，未做 faceUp 协议匹配路径）——如用户要完整常规打出规则（正面需匹配线），再扩。
3. war-1 语义已按用户拍板修正（正文主体=拥有者）；2代 旧测试无 war-1 覆盖，建议后续补 war-1 完整链测试（含对手刷新触发→拥有者弃任意+刷新→无递归）。
4. 2代 遗留未变：deckReveals 揭示浮层 UI、chaos-1/2 重排面板 UI、luck-0 宣告面板、2代 专属 FX、luck-3 FAQ 勘误、新手教程/单人/三人/打包。
5. 惰性区域禁用的边界组合（inertia-0 与各类顶命令守卫互作、被盖惯性牌等）在目检中发现再定。
