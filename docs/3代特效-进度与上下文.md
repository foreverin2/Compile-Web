# 3 代特效 · 进度与高频上下文（跨批工作文件）

> **作用**：批次之间只靠这份文件交接上下文。每批收尾时更新它，**下一批开工先读它**（不重读整份设计稿）。
> 设计稿（完整设计）在 `docs/3代-特效设计稿-文字版.md`；本文件只放**高频要用**的东西。
> 最后更新：2026-09-13（**批次 F 完成**：用户清单 #1~#18）

---

## 1. 批次进度

| 批次 | 内容 | 状态 | 提交 |
|---|---|---|---|
| **A** | 15 套配色 + 15 套协议已编译特效（§3 / §3.1） | **✅ 已完成**（tsc 0 / 773 测试 / build / texts 0 差异） | `feat(3代特效A)`（见 `git log`） |
| **B** | 弃牌 / 删除 / 翻转 / 偏转 四类附加层（**仅点名的卡**：§4.1~§4.15） | **✅ 已完成**（19 组合；tsc 0 / 780 测试 / build / texts 0 差异） | `feat(3代特效B)`（见 `git log`） |
| **C** | 抽牌 / 反面打出 / 编译后 三类 + 整摞弃置牌库（点名卡：暴食抽牌、支点抽牌、暴食0 反打、压制/刚性/惰性反打、贪婪1 编译、动量编译后、惰性4） | **✅ 已完成**（8 组合；tsc 0 / 787 测试 / build / texts 0 差异） | `feat(3代特效C)`（见 `git log`） |
| **D** | 控制权族 C1~C6 + 5 个常驻 sync + 贪婪1 硬币堆（§4.4 / §5.4） | **✅ 已完成**（tsc 0 / 794 测试 / build / texts 0 差异） | `feat(3代特效D)`（见 `git log`） |
| **E** | 交换附加层（支点1·3 / 柔性3）+ 收尾（被覆盖卡可见区域 / 性能观测 / 实现手册） | **✅ 已完成**（tsc 0 / 794 测试 / build / texts 0 差异） | `feat(3代特效E)`（见 `git log`） |
| **F** | 用户第二次试玩清单 **#1~#18**：缺陷修复（跟随/触发缺失/规则） + 视觉加强 + 动量4 重排 UX | **✅ 已完成**（tsc 0 / **819** 测试 / build / texts 0 差异） | `fix(3代特效F1)`/`feat(3代特效F2)`/`feat(3代特效F3)`（见 `git log`） |

## 1b. 批次 A 交付速查（下批直接用，不必回读实现）

- **协议已编译特效文件**：`src/ui/compiled-gen3.ts`（15 构建器 + 零件 `orbitRing/coilRing/scatter/burstParticles/shockRing`）
  ＋ `src/ui/styles-gen3.css`（外框/呼吸环/护边 + 元素样式与关键帧），`main.ts` 已 import。
- **接入点**：`render.ts` → `appendNewCompiledFx()` 首行 `if (appendGen3CompiledFx(layer, defId, GEN3_FX_API)) return;`
  （`GEN3_FX_API` 传 `el/layerGeom/reflow/corners/scheduleLoop/timer/rnd`；**新批次若需新宿主能力，往这个对象加**）。
- **可复用的 3代 CSS 零件**（写 B~E 批的卡牌特效时直接用，勿重造）：
  `@keyframes gen3-ring-travel`（offset-path 环带旅行）、`gen3-particle-fly`（--dx/--dy 粒子）、
  `gen3-shock-out`（冲击环 .on）、`gen3-drift`（残片漂浮）、`gen3-soft-blink`（余烬明灭）。
- **协议呼吸环**：`.compiled-ring-<proto>{animation:gen3-breathe-<proto> …}`；**护边配色** `.compiled-<proto>-corner{--cc/--ccg}`。
- **守卫测试**：`tests/ui/gen3-compiled-fx.test.ts` —— 新增任何 `gen3-*` 类都必须同时写进 `styles-gen3.css`，
  否则该测试会失败（这是刻意的：防"JS 建了元素但 CSS 没定义 → 特效不出现"；本批它已抓出 5 处真实疏漏）。

## 1c. 批次 B 交付速查（批次 C/D/E 直接用）

- **文件**：`src/ui/fx-gen3.ts`（4 个分发函数 `gen3DiscardFx/gen3DeleteFx/gen3FlipFx/gen3ShiftFx`
  + 零件 `geom/bodyLayer/arcTrack/queueStaggered/lineCenterX`）＋ `src/ui/styles-gen3-cards.css`（`g3-*` 类）。
- **接入点**：`effects/index.ts` 的 `GEN3_CARD_FX_API`（传 `el/buildFxCard/playCut/playCutAt/playShatterAt/playFlip/playShift/extraZ/rnd`）
  + 四个 case 首行的 `if (node && gen3XxxFx(node, payload, GEN3_CARD_FX_API)) break;`。
  **批次 C（抽牌/反面打出/编译后）需要新事件分支**：`card:drawn` / `card:deck-played` / `card:hand-played` / `line:compiled`
  —— 同样往 `GEN3_CARD_FX_API` 加能力、往 switch 加首行接管即可（保持"基础动画照常"）。
- **两条踩过的坑（务必守）**：
  1. **浮层卡自身 background / ::before 会被卡面 `<img>` 盖住** → 卡面覆盖类视觉一律走 `::after` 或子元素；
  2. **浮层卡类上不要做 transform 动画**（会覆盖横置卡 rot-cw/ccw 的内联 rotate，卡会"立起来"）
     → 位移感交给子元素/伪元素（粒子用 `--dx/--dy`，角度用 `--r` + keyframes 里的 `rotate(var(--r))`）。
- **多张连锁编排**：`queueStaggered(key, sortKey, run, {windowMs, gapMs, onAllDone})` —— 同一批（40ms 窗口）
  按 sortKey（到线中心距离）排序，逐张错开；新星0 整线删除 = 70ms、愤怒2 整线翻转 = 80ms。
- **跨卡连线/弧轨**：`arcTrack(fromRect, toRect, cls, proto, dots, ms, z, ribbon, api)` —— body 级满屏 SVG path
  ＋ `offset-path: path(...)` 光点，用于新星引力线、傲慢金弧、新星星轨、柔性飘带。
- **守卫测试**：`tests/ui/gen3-card-fx.test.ts`（覆盖范围不得擅自扩大 / 类名↔CSS / 分发点已接入 / 基础动画未被替换 /
  `playFlip` 默认 350ms 不变 / PRE 时序 / 禁 mask）。

## 1d. 批次 C 交付速查（批次 D/E 直接用）

- **新增分发器**（都在 `src/ui/fx-gen3.ts`）：`gen3DrawFx` / `gen3FaceDownFx(kind,'deck'|'hand')` /
  `gen3CompiledFx` / `gen3DeckDiscardFx`。
- **接入点**：
  - `card:drawn` → **早分支**（在 `if (!payload?.uid || !payload.defId) return;` 之前！该事件无 uid）；
  - `card:deck-played` / `card:hand-played` → case 首行 `if (gen3FaceDownFx(kind, payload, API)) break;`
    （注意：**break 直接跳出 switch**，不再走 gravity/smoke/assimilation 分支 —— 3代 协议与之不重叠）；
  - `line:compiled` → `initCompileFx` 内 `playCompile(p)` **之后**并列调用（编译横幅照旧）；
  - `deck:discarded` → 新增事件，`discardWholeDeck` 分支在**卡仍在牌库时**发射（UI 需牌库区矩形）。
- **宿主能力新增**：`deckPos(player)` / `playDeckPlay(p, durationMs?)` / `playHandPlay(p, durationMs?)`
  （后两者加了可选时长：惰性=慢 700ms；默认 MOVE_MS 不影响 1/2 代）。
- **编译事件新增字段**：`line:compiled` 的 `sourceDefId` / `sourceUid`（效果编译来源；规则编译为空）。
- **踩坑提醒**：`playDeckPlay`/`playHandPlay` 等共用函数**不要**全局替换 `MOVE_MS` → 会连带改到
  gravity/speed 等其它协议（本批已踩过一次，靠 tsc 报 `Cannot find name 'durationMs'` 抓回）。
- **未做（记录在案）**：贪婪1 的「卡上硬币堆常驻增高一级（≤3 级）」需要一张卡的持久等级注册表，
  留到批次 D 与常驻层（`sync*` 家族）一起做。
## 1e. 批次 D 交付速查（批次 E 直接用）

- **模块**：`src/ui/gen3-control.ts`（瞬态控制权族 + 全部常驻 sync）＋ `src/ui/styles-gen3-sync.css`
  （`g3sync-*` 常驻层 / `g3ctrl-*` 瞬态层 / `g3clear-*` 清缓存齿颚）。
- **常驻层统一入口**：`syncGen3Persistent(s)` —— render.ts 每帧末尾 + main.ts 滚动/缩放时各调一次；
  `clearGen3Persistent()` 在 `resetUiState` 内调用（新局清残留）。
- **写新 sync 的规矩**（照本批 7 个 sync 的写法）：
  1. 函数返回 `string[]`（本 sync 的 active 键）——**不要自己 prune**，由 `syncGen3Persistent` 统一 prune；
  2. 每帧先算"签名"（`sig`）：相同 → 只 `place(...)` 重定位（**动画不重启**）；不同 → `textContent=''` 重建；
  3. 条件消失 → 不放进 active 集合，自动被 prune 掉；
  4. 层波段：链路级 z=46 / 卡级 z=48 / 徽标 z=49（**低于 hover 弹卡**，与 1/2 代常驻层同级可见度）。
- **引擎事件（已落地）**：`control:changed {from,to,reason}`（reason: check/return/effect）、
  `rule:control-check {player,wins,leading,gained}`（**无论是否获得都发**）、`rule:clear-cache {player,count}`（两处路径）、
  `card:immune {uid,defId,kind:'flip'|'shift'}`（flip/shift 各一处）。
- **注意**：`card:played.fromAction`（§5.6 第 4 项）经核实**无需新增**——刚性2 底的反打最终仍走
  `card:deck-played`，与其它刚性反打同一条 FX 路径。
- **未做（记录在案）**：无。批次 D 范围内的条目全部完成。
- **构建提示**：JS 包已 >500 kB（vite 提示 chunk 过大）。不影响功能；若后续继续变大，批次 E 可考虑把
  特效模块（fx-gen3 / compiled-gen3 / gen3-control）拆分动态 import。
## 1f. 批次 E 交付速查（A~E 完成后的维护入口）

- **交换附加层**：`src/ui/fx-gen3-swap.ts`（`gen3FulcrumSwapFx` 整堆互换 / `gen3ProtocolSwapFx` 协议交换）；
  接入 `effects/index.ts` 的 `initRearrangeFx`（协议交换）+ 新增 `initGen3StackSwapFx`（堆叠交换，main.ts 已注册）。
  **交换事件带 `sourceDefId`**：只有 `fulcrum-*` / `flexibility-*` 才接管，玩家行动重排不受影响。
- **被覆盖卡可见区域**：`src/ui/gen3-util.ts` → 同步层用 `visibleRectOf(s, uid)`；浮层卡用
  `clipInsetRightPct(s, uid)` 设 `clip-path: inset(0 X% 0 0)`。**新写任何"标记被盖卡"的特效都要用它**。
- **性能观测（只观测不降级）**：导出的诊断日志含「3代特效层（活动）」段（`diag.ts: gen3LayerReport()`），
  卡顿时先看这一段定位"哪类层没被清理"。
- **维护手册**：`docs/3代特效实现手册.md` —— **未来改特效先读它**（文件地图 / 接入点 / 铁律 / 事件速查 /
  常见改法 / 9 条坑清单 / 守卫与验证 / 品质标准）。
- **守卫共 5 个测试文件**：`gen3-compiled-fx`（A）·`gen3-card-fx`（B·C·E）·`gen3-control-fx`（D）·
  `gen3-swap-fx`（E）·`tests/docs/gen3-docs-guard`（文档）。**改覆盖范围会立刻报红（本批就靠它抓出
  "惰性翻转被漏做"）**。
## 1g. 明日目视验收清单（2026-09-14 检查用）

> 我没法看画面，以下是**按"最该被眼检"排序**的清单。每条都写了"怎么最快触发"和"看什么"。
> 触发捷径：`Ctrl+Shift+P` 进开发者模式 → `get <卡名>` 把卡加到手牌（如 `get 嫉妒0`）；
> 强行编译用开发者模式的强制编译（绕过线值条件）。

### G. 批次 F 专项（2026-09-13 用户清单 #1~#18 的验收位置）
| # | 看什么 | 最快触发 |
|---|---|---|
| #2 | 贪婪2 回手（青玉爪 + 品红绳线 + 落点青玉环）/ 嫉妒3 反打（故障横条 + 「夺取」+ 橙色引力光线 + 涟漪）/ 嫉妒4 打出（已编译数对比计数块；对手多则抖动+故障闪） | `get 贪婪2`（开始阶段）、`get 嫉妒3`（让对手往这条线打牌）、`get 嫉妒4`（打出瞬间看；先让对方编译 1 个协议看"抖动"分支） |
| #8 | 控制权易主：**只有色欲卡效果**驱动时才有牵引链/断链；判定阶段/其它协议只有一圈脉冲 + 「控制组件 → Pn」文字标 | `get 色欲0` 打出（应有链条）vs 空场让判定阶段自动易主（应无链条） |
| #1 | 获得控制权时，组件幽灵的落点应**正好落在轨道上持有者那一端**（缩放窗口/换分辨率后仍对齐） | `get 色欲0`；改变窗口大小后再触发一次 |
| #3/#16 | 偏转三态要能一眼区分：傲慢=**实心金缆+箭形滑块**、新星=星轨+四角星、柔性=**波动绶带+落点缎带结**（都不再是同一根虚线） | `get 傲慢4` / `get 新星3` / `get 柔性2`（各自偏转一次） |
| #4/#7/#13/#6 | 已编译周期爆发：贪婪**硬币自边框抛物线喷发** / 色欲爱心（4 环绕 + 爆发 5 飞行）/ 愤怒**四边尖刺** + 3 道闪电 / 怠惰泥浆上涌 + 沉浊环 | 强制编译对应协议后**等 4~10s**（爆发有 ≥10s 最小间隔） |
| #9/#18 | 伏击翻面（620/500ms + 白闪 + 「伏击·现身/潜伏」+ 碎片）；惰性翻面（灰砂 + **停转齿轮** + 「惰性·停转」） | `get 伏击1`/`get 伏击3`、`get 惰性0`（中）/`get 惰性2` |
| #10 | 动量4「重排你的协议」应弹出**编译期同款重排窗口**（只显示自己一侧、点击两张交换、"完成重排"在未改动时置灰），完成后棋盘一次换位动画 + 日志一条 `重排协议 → XYZ` | `get 动量4` 打出；顺手验证连锁的「动量1 底弃牌」选择条不会被窗口永久挡住 |
| #11/#17b | 支点弃牌（立柱/底座/7 刻度/砸落砝码/卡滑落光带/青玉描边）；新星弃牌 = **陨石斜砸**（引力束 → 陨石 → 白闪 + 冲击环 + 灼痕 + 碎屑，切割在撞击瞬间） | `get 支点5` 弃牌一次；`get 新星2`/`get 新星5` 触发弃牌 |
| #5/#15 | （规则）反面怠惰牌盖住不应给怠惰0 加成；柔性1 翻转候选含自身；惰性2 可对自身生效 | `get 怠惰0` + 用反面牌盖它；`get 柔性1`、`get 惰性2` 看候选列表 |

### A. 协议已编译特效（先验这一批——15 套是基础观感）
编译任意 3 代协议（开发者模式强制编译），逐套看：**主体件是否一眼认出协议**、是否遮挡卡文与数值。
优先看这 5 套（差异最大）：嫉妒（双涡旋+故障条）、暴食（齿颚开合）、刚性（荧光黄迷宫）、惰性（停转齿轮）、柔性（飘带）。
- 每次爆发都在 10s+ 抖动之后出现一次（不是每帧都在动），**等一会儿**才能看到周期爆发。

### B. 卡牌附加层（四类动作，量最大）
| 看什么 | 最快触发 |
|---|---|
| 弃牌：贪婪硬币滚落 ／ 怠惰泥浆 ／ 愤怒爪痕 ／ 支点天平 ／ 动量速度线 ／ 新星引力线 | 打出对应 5 分卡（如 `get 贪婪5`）|
| 删除：暴食粉碎吞噬 ／ 愤怒爆刺 ／ 压制配重板 ／ 新星整线连锁 | `get 暴食1`（底：清缓存后删除）、`get 新星0`（顶：整线删除，**这条最值得看**）|
| 翻转：傲慢棱镜/金塔 ／ 怠惰慢翻+余烬 ／ 愤怒闪电 ／ 伏击揭幕 ／ 柔性缠绕 ／ 惰性灰化 | `get 伏击1`、`get 傲慢3`、`get 惰性0` |
| 偏转：傲慢金弧 ／ 新星星轨 ／ 柔性飘带滑轨 | `get 傲慢4`（持有控制权时）|

### C. 抽牌 / 反打 / 编译后
- 暴食抽牌（食道涡口 + 手牌"饱胀"）、支点抽牌（刻度标尺）：`get 暴食1` / `get 支点3`
- 压制/刚性/惰性**反面打出**：`get 压制1`/`get 刚性1`/`get 惰性3`（看落点阵列下压 / 护板升起 / 砂化慢落）
- **编译后**：`get 贪婪1`（回合结束触发编译 → 青玉契约印盖章）；场上有动量卡时编译一次（蓄力环+牌库加速条纹）

### D. 控制权族（最"重"的一套）
- 获得/失去：`get 色欲0`（中：获得控制权 → 组件幽灵飞向持有者 + 红链）；`get 色欲3`（底：失去 → 链断）
- 判定阶段：回合开始自动触发（三条线对比条扫描，领先亮起／未满足暗下+抖动）
- 常驻：持有控制权时组件上红环+`P1/P2`；若同时有未覆盖的 `get 色欲0` → 对手协议区出现"禁编译"封条
- 对手获得后：场上 `get 色欲4` / `get 傲慢6` 会亮红徽记 + 扩散波纹

### E. 5 个常驻 sync（持续生效中，**重点看"是否遮挡"与"是否太吵"**）
嫉妒0（汲取丝+吸收格+`+N`）、愤怒0（双方 == 最高值的卡划斜纹）、怠惰0（被怠惰牌覆盖时 `+5`）、
惰性0/惰性1（断电栅格 / 下缘条纹）、刚性7（迷宫纹+锚钉；被翻/偏转时闪亮）。
触发：`get 嫉妒0`（对手那条线放张高值卡）、`get 愤怒0`、`get 刚性7`（再对对手用一次翻转效果看免疫反馈）。

### F. 交换附加层
- 支点1（`get 支点1` 中：翻转全场 + **交换左右堆叠** → 整堆沿青蓝杠杆弧互换）
- 支点3 / 柔性3（"交换你的2个协议"分支 → 杠杆弧+砝码 / 紫飘带两端打结互换）

### 反馈建议给我这几类信息（越具体越好）
1. **哪条"像塑料"**（元素太少/太平面/节奏太直）——我会按设计稿 §8 七要件补层次与材质；
2. **哪条遮挡**卡文/数值/图标（我给的是"贴边+半透明"，但实际卡图密度我看不到）；
3. **哪条太吵或太长**（尤其常驻层：5 个 sync 同屏时会不会过载）；
4. **哪条看不清因果**（例如不知道是谁触发的、或判定失败没有反馈）。

> 诊断辅助：卡顿时导出诊断日志，里面有「3代特效层（活动）」段（按类别给层数与元素数），能定位是哪类层没被清理。
## 1h. ⚠️ 推送阻塞（2026-09-13 记录，尚未解决）

**现状**：本地 `main` 比 `origin/main`（`893de40`）领先 **19 个提交**，全部**无法推送**——不是网络问题（fetch 正常），
而是**远端 pre-receive 钩子拒绝**：历史里含超大文件。

**证据**
```
remote: error: File compile-windows/compile.exe is 329.41 MB; this exceeds GitHub's file size limit of 100.00 MB
 ! [remote rejected] main -> main (pre-receive hook declined)
```
- 该文件由提交 **`2a35ee6`**（"feat(日志强化+重编译规则)…"）引入，blob 实际 **345,411,375 字节**；
- `compile-windows/` 共 12 个文件 / **347.1 MB**，目前仍被 git 跟踪（`git ls-files` 可见）；
- 本机 **无 git-lfs、无 git filter-repo**；`.gitignore`（7 行）**未**排除该目录；
- 代理推送会先 **HTTP 408** 超时，需用免代理参数（见下）。

**三条可选处理路线（都需要你拍板，我没有擅自执行）**

| 方案 | 命令要点 | 代价 |
|---|---|---|
| **A. 重写历史去掉大文件**（推荐：这 19 个提交都是本地独有，重写不影响他人） | `git branch backup-before-clean` → `git filter-branch --force --index-filter "git rm -r --cached --ignore-unmatch compile-windows" --prune-empty -- --all` → `git reflog expire --expire=now --all && git gc --prune=now` → 推送 | 19 个提交哈希全变；`compile-windows/` 仍留在磁盘（只从 git 历史移除），本地照常能跑 |
| **B. 改用 Git LFS** | 安装 `git-lfs` → `git lfs install` → `git lfs migrate import --include="compile-windows/*"` → 推送 | 同样重写历史；远端需允许 LFS（配额） |
| **C. 只把当前代码推成新分支**（不重写历史） | `git checkout --orphan gen3-fx-clean` → 移除 `compile-windows` 并写入 `.gitignore` → 提交 → `git push origin gen3-fx-clean` | 该分支无历史（旧提交仍在本地备份分支）；`main` 仍推不上 |

**推送命令（免代理参数，代理会 408）**
```
git -c "http.https://github.com.proxy=" -c "https.://github.com.proxy=" -c http.proxy= -c https.proxy= -c http.postBuffer=524288000 push origin main
```

**建议顺带做**：把 `compile-windows/` 与 `其他人制作的版本/` 写进 `.gitignore`（后者一直是未跟踪状态，
每次都要小心不要被 `git add -A` 扫进去）。

> 19 个待推送提交（旧→新）：`2ecc11b 404fa99 2d03c92 cd42c35 08d9a4c d71e045 69828b4 2a35ee6 4ea594c 88c6ed6 c65ad75 aa15c5a e7827e8 26cb6b2 e951314 c4b247d 69b7322 f33060b 3475328`
> 其中 **3代特效 A~E** 是 `e7827e8 → f33060b`（含文档守卫 `e951314`、验收清单 `3475328`）。
## 1i. 实测 bug 与修复（2026-09-13 下午，用户第一次试玩反馈）

### Bug 1：部分卡牌边框/特效**不跟随滚动**，粘在屏幕固定位置
- **根因**：常驻 sync 在"签名未变"时**提前 continue**，只重定位了部分子元素：`syncEnvy0Absorb` 漏了
  `glow`（卡面边框光）/`mark`（源卡橙环）/`badge`（+N）；`syncWrath0Cull` 漏了 `seam`（中缝虚线）；
  `syncSloth0Bonus` 漏了 `ripple`（涟漪）。这些元素只在首次创建时定位 → 滚动后留在原屏幕坐标。
- **修法**：结构改为「**子节点只在集合变化时重建；位置每帧都重算**」——嫉妒0 抽出 `placeEnvy0()`
  覆盖全部子元素，愤怒0/怠惰0 把定位移出条件分支。
- **守卫**：`gen3-control-fx.test.ts` 禁止 `if (rec.sig === sig)` 写法复活，并要求六个 sync 每帧都调用 `place*()`。

### Bug 2：每回合双方链路上**蹦出奇怪粗线条**
- **根因**：`gen3ControlCheckFx`（控制权判定，**每回合触发**）把 14px 高的对比条**横铺整条 `.stack-slot`**
  （`width = slot 宽 + 12`、`top = slot 中心`）且**没有文字** → 视觉上就是横在卡上的怪线。
  附带 bug：`batteryNode()` 用了**不存在**的 `.line-row` → 回退到 `document.body`，取到**第一行**电池。
- **修法**：C4 重做——对比条**贴在该线己方能量槽正下方**、宽 `min(150, 电池宽)` 的**短条**，两端带数值
  （你/对手）+ 中间扫描线 + 上方「控制权判定 · Pn」标题 + 扫描后文字结果
  （`获得控制组件` / `未满足（领先 n 条，需 2 条）`），领先线双方能量槽加金圈；
  `batteryNode` 改为查 `.stack-slot[data-player][data-line] .battery`。
- 顺带：嫉妒0 汲取丝改为**卡片边缘→边缘**（旧版中心到中心、长线横穿棋盘）+ 虚线流动 + 箭头 + 「借 N」小标。

### Bug 3（**规则 bug**，用户实测）：嫉妒0 的阈值加成被错误地同时给了对手
- **根因**：`envy-0` 的顶框值修正注册成了 `valueModifier target: 'line'`。引擎里 `line` 的语义是
  "线上任一玩家链路的正面修正卡对**双方估值都生效**"（`stackValue` 对每个估值方各应用一次）——
  这与 **lust-0**（"每位玩家 +10"）、**wrath-0**（"不计入玩家总阈值"）相符，但**与 envy-0 卡文不符**：
  "此链路中，**你的**总阈值增加对手…最高阈值卡牌的阈值" → 只加**卡主自己**。
  于是对手也会被加上"你链路上的最高阈值"（白拿加成）。
  根源是 `docs/3代-批1-规格与裁决清单.md` 当时把实现规格写成了 `target=line`（已同步更正）。
- **修法**：`envy.ts` 改为 `target: 'own-stack'`（仅当估值方==卡主时应用，apply 的 owner=卡主 →
  加的是卡主**对手**的最高卡）。同族另两条 `line` 修正卡经核对**语义正确、保持不动**。
- **测试**：`gen3-batch1-cards.test.ts` 新增区分性回归（持有者该线还有高值卡时，对手**不得**被加成，
  旧实现会得到 1+4=5，正确为 1）；`gen3-batch3-cards.test.ts` 里固化旧行为的断言已更正。
- **特效侧无需改**：`syncEnvy0Absorb` 本来就是"按持有者算、只贴持有者能量槽"（与卡文一致）。

### 诊断增强
`diag.ts` 的「3代特效层（活动）」段重写：旧版按**前缀**统计会把子元素也算成"层"（数字虚高、无法定位残留）。
现在只统计**层容器**（class 含 `g3fx-layer`），按族分组，并**逐个列出类名/子节点数/位置尺寸**（前 40 个）。

### 未做（诚实记录）
**瞬态**特效（切割/碎裂/弧轨/印章等，存活 0.5~1.6s）仍按**事件发生时的屏幕坐标**定位（与 1/2 代一致）；
若在播放中滚动会短暂滞后（≤1.6s 自行消失）。若要"瞬态也跟随滚动"，需给 body 级浮层加统一 follow 注册表
（约 20 个调用点）——等你确认再动手。
## 1j. 批次 F：用户清单 #1~#18（2026-09-13 第二次试玩反馈）

**A 组（缺陷修复）**
1. **#1 控制权特效不跟组件卡** → 新增 `controlTrackSideX()`（`gen3-control.ts`）：落点按 `.control-track`
   实测矩形 + 与控制组件一致的 **4%/96%** 边缘百分比算，不再用视口 22%/78% 猜。
2. **#8 没打色欲也蹦牵引链** → 引擎 `setControl(s, holder, reason, sourceDefId?)` 多带**效果源卡 defId**
   （lust/envy/nova/wrath 的效果调用点传入）；UI `lustDrivenControl()` 只在 `reason==='effect' &&
   sourceDefId.startsWith('lust-')` 时才播链条/断链，其它来源走 `controlMiniFx()`（脉冲 + 文字标）。
3. **#2 贪婪2回手 / 嫉妒3 / 嫉妒4 特效从不出现**（三种不同的根因，都是"静默失效"）：
   - `GEN3_CARD_FX_COVER.return=['greed']` 是**空头覆盖表**（无实现、无分发）→ 新增 `gen3ReturnFx`
     + `effects/index.ts` 的 `case 'card:returned'` 里**在 water 分支之前**调用；
   - `gen3FaceDownFx` 缺 `case 'envy'`（嫉妒3 底反打）→ 补：故障横条横扫 + 「夺取」字样 + 橙金引力光线 + 落点涟漪；
   - `effects/index.ts` **完全没有 `case 'card:played'`** → 新增 `gen3PlayFx`（嫉妒4 E4：已编译数对比计数块，
     对手多 → 抖动 + 玉青故障闪 3 帧；条件不成立 → 灰色快速淡出）。
4. **#17a 新星删除特效不触发** → 延迟回调查节点时 DOM 已重渲染（`geom()` 为 null）→ 事件时定格
   `rect/cw/ccw`，用 `buildFxCardAt`/`playShatterAt` 播（顺带：`card:deleted` 回填 `line`，见下）。
5. **#5 怠惰0 被反面怠惰牌盖住也生效**（规则）→ `coveredBySloth` 要求覆盖者 `faceUp`；`unity0BeforeCovered` 同类修正。
6. **#15 柔性1 翻转目标含自身 / 惰性2 可影响自身**（规则）→ `CandidateFilter.includeSelf`；三张卡使用它并让源卡排最后。

**B 组（视觉加强，全部按 §8.1 七要件自查）**
- #3+#16 偏转弧轨**三态化**：傲慢=实心金缆（外鞘+亮芯+白芯+箭形滑块）、新星=星轨（拖尾+虚线芯+四角星）、
  柔性=绶带（正弦波动宽柔光带+落点缎带结）——不再和嫉妒0 的虚线汲取丝撞脸。
- #4 贪婪已编译：**硬币自卡牌边框抛物线喷发**（4~9 枚，`--dx/--peak/--fall` 由卡面尺寸算）。
- #6 怠惰（弃牌+已编译）、#7 色欲爱心（4 环绕 + 5 飞行）、#9 伏击翻面（620/500ms + 白闪 + 字样 + 碎片）、
  #11 支点弃牌（立柱/底座/7 刻度/砸落砝码/滑落光带/青玉描边）、#12 愤怒爪痕 3→8、#13 愤怒已编译
  （**四边各 3 根尖刺** + 3 道闪电 + 16 火星）、#14 动量弃牌（6 速度线 + 斜向疾风 + 加速带，**接线本就正确，纯粹观感太弱**）、
  #17b 新星弃牌改为**陨石撞击式**、#18 惰性翻面（灰砂覆层 + 停转齿轮 + 砂尘 + 字样 + 慢翻 900ms）。

**C 组（#10 动量4「重排协议」改用编译期同款重排窗口）**
- 模态 `control-rearrange.ts` 增 **draft 模式**：本地摆布局（多次点击交换）→「完成重排」一次性回填一条
  `action:order:XYZ`。**不改引擎**：效果栈挂起期间 `rearrange-protocols` 会被硬拒，所以走 `effect-choice`。
- 幂等：`sessionKey='effect:<pendingId>'`（`main.ts` 每帧 `syncRearrangeModalForEffect()` 调用），同键只刷新内容。
- 引擎只多一个可选字段 `ChoiceRequest.rearrangeSide`；`render.ts` 读到它就不再渲染 5 个布局按钮，只留一行提示；
  未改动顺序时「完成重排」按钮禁用（引擎会拒绝恒等排列）。
- 已知取舍：draft 期间棋盘仍是旧顺序（窗口遮罩 74% 黑）；窗口期内的**连锁选择条**（如动量1 底弃牌）
  位于遮罩之下——靠"栈顶不再是重排请求就自动关窗"兜住（该缺陷在 live 模式同样存在，非本批引入）。

**新增守卫**
- `tests/ui/gen3-card-fx.test.ts` +3：**覆盖表每一项 ⇒ 对应函数内真的有实现**（#2 的根因就是这条缺失）、
  每个导出的 `gen3*Fx` 都被 `effects/index.ts` 调用、回手/打出两个新分发点已接入。
- `tests/ui/gen3-control-fx.test.ts` +1：#1 轨道锚定 + #8 色欲门控 + 四张卡的 `setControl` 都带 `defId`。
- `tests/ui/rearrange-draft.test.ts`（新）：`swapOrder`/`orderChanged`/`orderToAction` 纯函数 + draft 接线守卫。
- `tests/effects/fx-payload.test.ts` +4：greed 回手 / envy-3 反打 / `card:deleted` 回填 `line` / `card:played` 载荷。


## 1k. 全协议同类审计（2026-09-13 第二轮，按你要求"看看别的卡有没有同样的没修的 bug"）

**方法**：把本批修过的 bug 归纳成 5 类，四条线并行只读审计（不写代码）——
① **事件载荷完整性**（emit 前字段被改/清空 → FX 定位退化）；② **特效接线覆盖**（覆盖表 ↔ 实现 ↔ 分发器）；
③ **规则语义**（valueModifier target / "被X覆盖"身份 / 候选是否含自身 / 源卡时序 sourceValid）；
④ **定位与跟随**（常驻层每帧重定位 / 视口猜测 / 选择器是否匹配真实 DOM）。

### ⚠️ 覆盖审计（第 2 条线）抓出的 **3 条整条点名特效"静默不播"**（已修，最高优先）

| # | 点名特效（设计稿） | 为什么完全不播 | 修法 |
|---|---|---|---|
| 1 | **支点3 / 柔性3 协议交换附加层**（F3b / X4） | `rearrangeProtocolSlots()` **没有 sourceDefId 参数**（注释却写着"由 resolve.ts 带 sourceDefId"）→ `fx-gen3-swap.ts` 的 `src.startsWith('fulcrum-'/'flexibility-')` 门控恒假，整段杠杆弧/砝码/飘带打结不可达 | 该函数增可选 `sourceDefId`，`resolve.ts` 的效果重排路径传 `pe.sourceDefId`（玩家行动重排仍不带） |
| 2 | **贪婪1 硬币堆 +1 级（R2④，文档里唯一标 未做 的行）** + 契约印"自贪婪1 卡面浮出"（R2①） | `line:compiled` **只发了 sourceDefId、没发 sourceUid** → `noteGreed1Compile(p.sourceUid)` 恒 undefined → `greed1Stack` 永远为空；契约印起点也永远退化到协议格 | `executeCompileBody` 的 opts 增 `sourceUid` 并 emit，`greed.ts` 传 `ctx.card.uid` |
| 3 | **惰性4 整摞沙化飞弃**（I2 后半） | `deck:discarded` 载荷**没有 uid/defId**，而分发器的 `if (!payload?.uid \|\| !payload.defId) return;` 在 switch 之前 → `case 'deck:discarded'` 永远不可达 | 与 `card:drawn` 同法：把 `deck:discarded` 提到 uid/defId 守卫**之前**处理（并加守卫测试锁死顺序） |

> 这三条都是"引擎发了事件 / UI 写了实现 / 测试全绿，但链路中间断了一节"——与本轮 #2 的三种病同族。
> 三处都已补**载荷级回归测试**（惰性4 事件无 uid、支点3 事件带 sourceDefId、贪婪1 编译带 sourceUid）。

### 覆盖审计同时修掉的中小项
- **支点1 整堆互换的幽灵卡"不动"**：引擎先换数组再发事件，而 DOM 还是旧序 → 旧代码把幽灵送到
  "它离开的那个槽"（dx≈0）。改为送到它**现在所属**的槽（`from`）→ 弧线互换真正可见。
- `card:drawn` 的 `draw` op 两处 emit 补 `triggerDefId/triggerUid` → 支点4「游标停在 4」、
  同化1 刷新光泽这两个按 defId 判定的分支第一次真正生效。
- `gen3ClearCacheFx` 忽略 `payload.player` → 对方清缓存时会在**你的** gluttony-0 上咬一口（已改为只看清缓存那位）。
- `syncSloth0Bonus` 的覆盖者判定漏 `faceUp` → 引擎不发 +5 时 UI 仍显示 +5/边框光（引擎已要求正面，UI 已对齐）。
- `gen3FlipFx` 的 envy 分支：计数对比此前对所有 envy 翻转都画（含未点名的 envy-1 中）且硬编码 P1/P2；
  现按 `triggerDefId==='envy-4'` 门控，视角从触发源卡持有者取（P2 触发不再左右反）。
- 暴食0 反打的齿颚浮层此前显示**那张反打牌的卡背**盖在 gluttony-0 上 900ms；现按设计稿显示 gluttony-0 卡面。
- 常驻层退场由"硬切 remove"改为 **0.3s 淡出**（设计稿 §8.1 要求收束；`drop()` + `.g3sync-out`）。

### 覆盖审计判定为**按设计/未点名**、不改的
- **动量6 中「删除此牌」**只有基础碎裂、没有反向蓄力环：设计稿 §4.11 M2 明写"删除由引擎照常走
  `card:deleted`，本层不重复" → **非缺陷**。
- `gen3DeleteFx` 的 gluttony 分支也会被 gluttony-3 顶触发（未点名）：范围红线只约束"不给未点名卡加特效"，
  同一函数的既有分支顺带覆盖属于实现细节，未扩范围。
- **envy-1 底 E2①「玉青镜面斜掠」没有钩子**：引擎对"start 触发被结算"不发事件 → 需新增事件，属独立改动（见 §7）。
- 瞬态层 >1.6s 的卡框光、`[data-uid]` 未限定作用域等：见 §7 待裁决。

### 用户裁决落地（2026-09-13 第二轮，5 项全部给了答复）

| 裁决 | 落地 |
|---|---|
| ① **nova-2「覆盖着1张新星牌」的下方卡必须正面** | `belowIsNova` 增 `below.faceUp`（与 sloth-0「被正面怠惰牌覆盖」/ unity-0 同口径）；新增区分性测试（反面下方卡 → 不走可重排，走 else 必得控制权） |
| ② **新增"start 触发被结算"事件并补 E2① 镜面斜掠** | 引擎新增 `card:trigger-resolved { …card, step, topCommand }`（在 `resolve-trigger` 里 **resolveTrigger 之后、runStack 之前**发 —— 此时源卡必然还在原位、DOM 可定位）；UI 新增 `gen3TriggerFx`：**只做点名的 envy-1**（玉青镜面斜掠 0.5s + 卡框脉冲 + 「夺取控制权」小标）；其它协议不加层 |
| ③ **补通用落地反馈** | `card:landed` 此前**没有任何消费方**（死事件）→ 新增 `gen3LandFx`：按 **owner+line 定位目标槽**（落地瞬间 DOM 还在起点槽，不能靠节点）+ 协议色冲击环 + 4 粒微尘 + 槽位短边框闪（≤0.55s）。通用（不绑协议），任意卡的偏转落地都有 |
| ④ `card:given`/牌库顶易主的 owner 语义 | **已做**：三处 `card:given`（give / takeRandom / takeFromField）+ `deckTopTransfer` 各补 `fromOwner`（改 owner **之前**的持有者；takeFromField 另带 `fromLine`）；消费方改为 `fromOwner ?? owner`：`playDeckPlay` 的**起点牌库**、`gen3FaceDownFx` 的牌库光束。落点仍用 `owner`+`line`（接收方/目标线，本来就对） |
| ⑤ **给 2~3.6s 的卡框光加跟随** | 新增极小注册表 `src/ui/fx-follow.ts`（`registerFollow(el, place)` + `syncFollowers()`，元素 remove 后自动出栈）；接进 `render.ts` 每帧 + `main.ts` 滚动/缩放 rAF；已接三处：迷雾卡框灰光 2s、透彻落点眼 3s、爱意牌库光芒 2s。**不做**"统一重定位所有瞬态层"（会牵动 20+ 调用点）|

**④ 说明（card:given / 牌库顶易主的 owner 语义）——已按裁决实现 `fromOwner`**：这类事件在引擎里
**先把卡的 owner 改成接收方**、再 emit（`card:given`、`deckTopTransfer`）。所以 payload.owner = **新持有者**，
而这张卡的 DOM 节点 / 牌库此刻还在**原持有者**那一侧（渲染要等本次行动结束）。现在四处 emit 都额外带上
`fromOwner`（`takeFromField` 另带 `fromLine`），需要按"从哪来"定向的特效用 `fromOwner ?? owner`；
**落点**仍用 `owner`+`line`（= 接收方 / 目标线，本来就对）。已改的消费方：`playDeckPlay` 的起点牌库
（同化2/6 的"从对方牌库抽出"不再画成自己牌库）、3 代反打附加层的牌库光束。


常驻层子件"只在 rect 非空时创建"（4 处 sync）｜`syncDiversity3Fx` 漏在滚动管线外｜rigidity-4 的覆盖者
只查 `pendingPlay`｜联合1 编译光柱的 `.protocol-holder` 全局回退｜greed-2 底 / pride-0 中 else 候选不含自身。

#### F4 明细（第 3 条线：规则语义）


| 问题 | 证据 | 修法 |
|---|---|---|
| 常驻层"子件只在 rect 非空时创建" → 重建帧取不到 rect 就**永远缺失**（与"滚动后特效消失"同类） | `gen3-control.ts` 的 envy0 thread/mark/borrow、sloth0 link、inertia0 栅格/光框、inertia1 条纹/灰边 | 子件**无条件创建**，位置交给每帧 `place*`（空 rect 自行跳过）；已加源码守卫 |
| **`syncDiversity3Fx` 只在 renderApp 里同步** → 滚动/缩放（及胜利后不再渲染）时 `.fx-div3-*` 粘在旧视口坐标 | `render.ts:4972` 调用，`main.ts` 的 `syncPersistentFx` 漏了它 | 加进滚动/缩放重定位管线；已加守卫 |
| 覆盖者来自**偏转**时不触发（只查 `pendingPlay`） | `rigidity.ts` 的 `rigidity4BeforeCovered`（unity-0 底 2026-09-12 修过同一个洞） | `pendingPlay[0]?.card ?? pendingShift[0]?.card`；已加回归测试（构造 pendingShift 走 `collectTriggerFor('before-covered')` 真实路径） |
| 「你的1张牌」（无"其他"）候选**不含自身** | `greed-2` 底「回手1张你的牌」、`pride-0` 中 else「偏转1张你的牌」——与用户 #15 已裁定的 flexibility-1 同构 | 两处加 `includeSelf: true`（pride-0 持控制权分支文本写"其他牌"→ 保持排除）；已加测试 |
| 联合1 编译光柱会回退到"文档里第一个 `.protocol-holder`"（锚到不相干的协议） | `fx-gen2.ts` `playUnityCompilePillar` | 去掉全局回退（协议格取不到就整体不播） |

**审计后判定为"误报"的（已逐一核对代码/现有测试，不改）**
- `overwhelm-2` 顶「3 线逐线反打后翻转自己」被怀疑会因源卡被盖而中断：其实该触发带 `top: true` →
  `sourceValid` 直接放行（`resolve.ts:101`），现有测试 `gen3-batch2-cards.test.ts:314-329` 已覆盖
  （自己那条线被打上后仍完成其余线与自翻）。
- `render.ts` 的 `.lane-row [data-player="1"] .protocol`：`lane-row` 下 P1/P2 各有自己的
  `protocol-cell[data-player]`，两条选择器各自命中正确一侧（第三条审计的判断有误）。
- `controlTrackSideX()` 返回的 4%/96% 位置**就是**滑动卡的中心（`.control-slider-img` 是 `left:4% + translate(-50%,-50%)`），
  不是"左边被当中心用"（#1 的落点几何是对的）。
- 1 代/2 代 mirror-3 / smoke-1 / water-4 的「你的1张牌」**按设计排除源卡**：
  `docs/批1规格-幸运明镜和平混沌明晰.md:252` 明写"源卡 mirror-3 自己由绑定自动排除，惯例同 water-4" → 不是 bug。
- 「多张同名 valueModifier 叠加」（apathy-0/smoke-2/clarity-0/mirror-0/diversity-3 两份就双倍）：
  与 lust-0「每位玩家+10（每份各算）」一致，属**多份静态效果各自生效**的常规解读，非缺陷。

**待你裁决（都只影响 1 张卡，改动各 1 行）**
1. **`nova-2` 中「此牌覆盖着1张新星牌」的下方卡是否必须是正面？** 现实现只看 defId（`nova.ts:124`），
   反面朝下的下方卡也算。按你 #5 的口径（反面牌身份未公开）应当**要求 faceUp**；但这条规则文档里没写明。
2. **`card:landed`（偏转落地）目前无人消费**——6 个偏转落地没有任何"落地"反馈（各卡自己的附加层照旧）。
   要补一条通用"落地闪光"吗？（2026-09-13 只做点名触发点，所以本轮没加。）
3. **`card:given` / 牌库顶易主（`deckTopTransfer`）的 payload.owner 已是新持有者**，而卡节点此刻仍在原持有者
    DOM 里——一/二代相关附加层按 owner 推方向时容易取错一侧（现有实现视觉上正常，属"潜在陷阱"）。
   要统一补一个 `fromOwner` 字段吗？（纯增量，不改现有判定。）
4. **2~3.6s 的常驻卡框光**（`.fx-smoke-cardglow` 2s / `.fx-clarity-card-eye` 3s / `.fx-gravity-*` 2.7s）
   超出"瞬态 ≤1.6s 可不跟随滚动"的约定，滚动时会短暂脱离卡面。要缩短还是给它们加跟随？

### 贪婪已编译重做（2026-09-13 用户反馈："硬币还不如改为环绕中心的触手，看起来一点都不像硬币"）

- **常驻主体换成触手**：旧的 6 枚品红硬币柱（`.gen3-greed-stack/.gen3-greed-coin` + `raise/spill`）整体删除，
  改为 **中心「取物口」+ 5 条青玉触手环抱中心**：触手根部贴卡边、尖端指向中心（`--x/--y/--r` 各自锚点与朝向），
  青玉渐变锥体 + `clip-path` 收尖，体侧 3 个品红吸盘错相呼吸；中心是暗口 + 玉青外环 + 品红虚线内环（逆向缓转）。
- **周期爆发改为两段动作**：收拢攫取 `.clench`（尖端探向中心、暗口与吸盘提亮）→ 猛张 `.lash`
  （尖端外撇 + 品红/玉青环闪）+ 9 枚品红碎粒 + 硬币自卡边抛物线喷发（保留用户清单 #4 的要求）。
- **硬币只作为"喷出的财货"保留，并补了币缘环 + 币面凹面**（`.gen3-greed-shower-coin::after`，放大到 16×11）
  —— 小尺寸下也能读出"圆币"而不是椭圆色块。若你觉得连爆发里的硬币也不要，改成"被触手拽出的品红宝石"很快能换。
- 守卫：`gen3-compiled-fx.test.ts` 新增一项——禁止硬币柱/`raise`/`spill` 复活，并要求触手/取物口/吸盘/
  蠕动关键帧/clench·lash 状态类齐全。
- **未动**：贪婪1 **卡面**上的硬币堆等级（R2④，`syncGreed1Stack` 的 `.g3sync-greed1-coin`）——那是另一处
  （卡上小徽标级元素），本轮只按你说的改"已编译特效"。要一起换成触手/宝石说一声。

## 2. 协议色与语法速查（做特效时只查这张表）



| 协议 | 主色 | 强调色 | 深底 | 一句话语法 |
|---|---|---|---|---|
| 嫉妒 envy | `#2fb3a8` | `#ffb347` | `#0f4f4a` | 玉青故障条 + 橙金"被夺取之物" |
| 暴食 gluttony | `#c9a227` | `#7a5c1e` | `#2b2b33` | 齿颚开合 + 碎屑向内收拢 |
| 贪婪 greed | `#1f8f8a` | `#d94fd9` | `#10333a` | 硬币滚落堆积；青玉=己得、品红=敌失 |
| 色欲 lust | `#e0344b` | `#ff6b6b` | `#3a1030` | 牵引链：获得=拉来、失去=崩断 |
| 傲慢 pride | `#d9a441` | `#ffe9a8` | `#4a3410` | 直线/塔/指针（区别于勇气的剑光） |
| 怠惰 sloth | `#8a5a5a` | `#ff5a5a` | `#26262a` | 全部慢 15~25%，灰红余烬 + 下沉泥浆 |
| 愤怒 wrath | `#c0392b` | `#2f6f8f` | `#1a0f10` | 爆裂尖刺 + 闪电 + 溅射（不做刀/手印） |
| 伏击 ambush | `#6f93c4` | `#bcd8ff` | `#16202c` | 阴影揭幕/合拢 + 冷蓝 9 宫格 |
| 支点 fulcrum | `#2f7f8f` | `#d8f0ff` | `#101c26` | 天平/杠杆/刻度 |
| 压制 overwhelm | `#1f6f8f` | `#e8f4ff` | `#0d1418` | 阵列下压（无一例外） |
| 动量 momentum | `#ff8a2b` | `#c9a6e8` | `#301a10` | 速度线 + 拖尾 + 蓄力环 |
| 新星 nova | `#ffb347` | `#8a3a2a` | `#3a1a12` | 尖刺爆发 + 星轨（强度随线内牌数） |
| 惰性 inertia | `#d8c9a8` | `#4a4a4f` | `#1c1c1e` | 去饱和（熄火）+ 停摆标记、无弹跳 |
| 刚性 rigidity | `#8f6fd0` | `#eaff3a` | `#241a38` | 护板落位 + 荧光黄迷宫走线 |
| 柔性 flexibility | `#a86fd0` | `#ffb0e0` | `#1c1430` | 飘带：翻=缠绕、偏转=滑轨 |

## 3. 层与 z（不许自创层级）

| 层 | z | 用途 |
|---|---|---|
| 常驻状态层 | 40~95 | 持续效果（形如既有 `.fx-spirit-handglow` 44 / `.fx-metal-lineglow` 61 / `.fx-fear0-triglow` 93） |
| 协议已编译层 | 100 | `.compiled-fx compiled-fx-<defId>`（body 级 fixed + `isolation:isolate`） |
| 基础特效 | 300 | 切割 / 碎裂 / 翻面 / 幽灵飞行 |
| 附加层 | 301 | 协议叠加层 |
| 2 代层 / 编译横幅 / 放大 | 600 / 620 / 1000+ | 沿用 |

## 4. 复用零件（改特效优先用这些，不重造）

- 浮层卡：`buildFxCard`（处理横置旋转卡、被盖卡可见区域）
- 弃牌：`playCut` / `playCutAt`（**"前置段 → 延后切割"模板**）、`fx/discard-cut.ts`
- 删除：`playShatter` / `playShatterAt`、`fx/delete-shatter.ts`
- 翻转 / 偏转 / 回手 / 反打：`playFlip` / `playShift` / `playReturn` / `playDeckPlay` / `playHandPlay`
- 协议层：`buildCompiledFx` + `append<协议>Compiled(layer, defId)`、`appendCompiledCorners`、`.compiled-ring`（`offset-path` 卡外环带）、`scheduleCompiledLoop(layer, defId, firstMs, burst)`（**按 defId 各自 ≥10s + 0~16s 抖动**）
- 常驻同步范本：`syncMetal1LineGlows` / `syncPsychicParticles` / `syncSmoke2LineGlows`（**按状态增删、复用节点、绝不重建**）
- 颜色工具：`protocolColorOf(defId)` / `hexToRgba(hex, a)`（`src/ui/protocol-colors.ts`）

## 5. 引擎事件速查（3 代特效只允许用这些）

| 事件 | payload 关键字段 | 用于 |
|---|---|---|
| `card:discarded` | `uid/defId/faceUp/owner/line/triggerProtocol/triggerDefId/fromDeckTop/toTrashOf` | 弃牌类 |
| `card:deleted` | 同上 +（批次 F 起）`line`/`pos` 回填为**删除前**的值 | 删除类（新星0 整线连锁排序靠 `line`） |
| `card:flipped` | 同上 | 翻转类 |
| `card:shifted` | + `fromLine` | 偏转类 |
| `card:returned` | 同上（注意：`line` 已被清空 → 只用 `owner`+`triggerUid` 定位） | 回手类（贪婪2 底 R3） |
| `card:drawn` | `player/count/triggerProtocol/triggerDefId/fromOpponentDeck` | 抽牌、刷新、揭示后抽牌 |
| `card:deck-played` / `card:hand-played` | `uid/defId/faceUp/owner/line/triggerProtocol` | 反面打出（牌库顶 / 手牌；嫉妒3 反打 E3 走此事件） |
| `card:played` | `uid/defId/protocol/owner/line/faceUp`（**批次 F 起才有 UI 分支**） | 打出瞬间（嫉妒4 E4 已编译数对比） |
| `card:landed` | `uid/defId` | 落地类（偏转/反打落地） |
| `card:revealed` | `owner/shownTo/defId/triggerProtocol` | 揭示类 |
| `line:compiled` | `player/line/protocolDefId/ownUids/oppUids/recompiled/ownValue/oppValue/sourceDefId/sourceUid` | 编译后族 |
| `protocols:rearranged` | `player/a/b`（或 `order`）+ `sourceDefId` | 协议重排/交换 |
| `stacks:swapped` | `player/a/b` + `sourceDefId` | **堆叠交换（支点1）** |
| `control:changed` | `from/to/reason/sourceDefId`（`reason`: check/return/effect；`sourceDefId` = 效果源卡，批次 F 起） | 控制权族 C1/C2/C5（**只有 lust 源才播牵引链**） |
| `rule:control-check` | `player/wins/leading/gained` | C4 判定阶段 |
| `rule:clear-cache` | `player/count` | 暴食咬合时刻 |
| `card:immune` | `uid/defId/kind` | 刚性7 免疫反馈 |
| `card:trigger-skipped` | 源卡字段 + `step`（end/start） | Q5 空动作反馈（可选触发被跳过） |
| `card:effect-skipped` | 源卡字段 + `promptTitle` | Q5 空动作反馈（可选选择被跳过：贪婪爪空抓 / 傲慢指针变灰下坠 / 暴食空咬） |
| `card:trigger-resolved` | 源卡字段 + `step` + `topCommand` | 「触发被结算」本身（E2① 嫉妒1 底镜面斜掠）；在效果结算前发 |
| `card:landed` | 源卡字段（**owner+line=落点槽**） | 偏转落地通用反馈（协议色冲击环 + 微尘 + 槽位短闪） |
| `deck:shuffled` / `luck:roll` / `card:copied` / `protocol:compiled-by-effect` / `time:deck-to-trash` | 各自的少量字段 | 一/二代既有特效（本轮审计确认都有消费方，非死事件） |

## 6. 约定（写代码时必须遵守）

0. **范围红线（2026-09-13 决定）**：**只给"点名的触发点"加特效**（设计稿 §4.1~§4.15 + 柔性3/支点1·3 交换附加层）。**其余 3 代卡牌触发时只播基础动画**——不要"顺手"给别的卡加特效；发现漏项先问，不要自行扩范围。
1. **叠加不替换**：基础动画永远照播，协议特效只加层。
2. **常驻 vs 瞬态分家**：持续生效走 `sync*`（复用节点、不重建）；单次触发走事件订阅 + 301 浮层。
3. **品质七要件**（设计稿 §8.1）：层次合成 / 材质 / 物理感 / 三阶配色 / 起止衔接 / 与基础动画咬合 / 可读性；**禁止清单见 §8.2**。
4. **Q6：全部完整特效，不降级**；必要时只用"错开时间"。
5. 术语：位移指令统一叫**偏转**（`shiftTerm()` 恒返回"偏转"）。
6. 验证门槛：`tsc` 0 错 + `vitest` 全绿 + `vite build` 成功 + `texts:check` 差异 0。

## 7. 已知问题 / 待办（滚动）

- **#8 的控制权门控口径待你确认**：现规则 = 「`control:changed` 且 `reason==='effect'` 且效果源卡是 `lust-*`」
  才播牵引链/断链，其余（判定阶段、嫉妒1、新星2、愤怒1·4 造成的易主）只播轻量脉冲+文字标。
  若你希望"判定阶段也牵链条"或"所有卡牌效果易主都牵链条"，改 `gen3-control.ts` 的 `lustDrivenControl()` 一行即可。
- **贪婪2 底"玩家跳过时播空抓一下"**（设计稿 R3 末句 / Q5）**未做**：引擎对"可选触发被跳过"不发任何事件，
  需要新增引擎信号（例如 `card:trigger-skipped`），属独立小改动——已明确记录，非遗漏。
- **draft 重排窗口期间**：棋盘仍显示旧顺序（窗口遮罩 74% 黑，属预期）；窗口期内出现的**连锁选择条**
  （如动量1 底弃牌）位于遮罩之下，靠"栈顶不再是重排请求就自动关窗"兜住（live 模式同样存在该现象，非本批引入）。
- **瞬态特效**（切割/碎裂/弧轨等 0.5~1.6s 层）仍按事件时刻的屏幕坐标定位，播放中滚动会短暂滞后（自行消失）；
  若要"瞬态也跟随滚动"，需给 body 级浮层加统一 follow 注册表（约 20 个调用点）——等你确认再动手。

## 8. 下批开工清单（模板）

- [ ] 读本文件 §1~§6
- [ ] 读设计稿中**本批对应小节**（其余不读）
- [ ] 列本批条目清单 → 逐条实现 → 逐条自查 §8.1 七要件
- [ ] 回归 + 提交 + 更新本文件 + 压缩设计稿（§9 流程）
