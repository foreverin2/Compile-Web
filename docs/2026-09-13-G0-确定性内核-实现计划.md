# G0 确定性内核 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `GameState` 成为"种子 + 操作序列"的纯函数结果 —— 同一串操作在任意设备、任意进程、任意存盘恢复路径上都得到同一个状态指纹。

**Architecture:** 新增 `src/core/rng.ts` 提供两类派生（命名流 `deriveInt` 与顺序流 `nextValue`），把随机源与 uid 计数器放进 `GameState`，消灭 `src/core` 内全部 5 处不确定性；新增 `src/core/fingerprint.ts` 提供状态指纹；新增 `tests/determinism/` 四条不变式测试台。规则、效果、触发、编译逻辑**不做任何改动**。

**Tech Stack:** TypeScript 5（strict）/ Vite / vitest（node 环境、`pool: 'threads'`、无 jsdom）。零运行时依赖。

**依据文档：** `docs/2026-09-13-联机与多端-设计稿.md` §2（确定性内核）、§2.3、§2.5、§2.7、§2.8。

---

## Global Constraints

- **零运行时依赖**：不得新增任何 npm 包（`package.json` 的 `dependencies` 必须保持不存在）。
- **`src/core` 保持 DOM-free**：不得 import 任何 `src/ui` 模块，不得使用 `document`/`window`/`localStorage`/`IndexedDB`/网络 API。
- **`src/core` 内 `Math.random` 零命中**；`Date.now` 仅允许存在于 `src/core/trace.ts`（纯诊断）。
- **验证门（每个任务收尾都要过）**：
  - `npx tsc --noEmit` → 0 错
  - `npx vitest run` → 全绿，且测试总数**不得少于现有 842 项**
  - `npx vite build` → 成功
  - `npm run texts:check` → 差异 0 处
- **热座玩法行为零变化**：三代全部对战测试必须保持通过。
- **提交规范**：`git add src tests docs`（**不要** `git add -A`）；中文提交信息，多个 `-m` 参数；**不推送**（除非用户明确要求）。
- **测试环境**：node（无 jsdom）。UI 类测试沿用 `readFileSync` + `new URL(..., import.meta.url)` 的源码守卫惯例（见 `tests/ui/fx-follow.test.ts:2,15,17`）。
- **文件读取惯例**（源码守卫测试统一用）：
  ```ts
  import { readFileSync } from 'node:fs';
  import { fileURLToPath } from 'node:url';
  const root = new URL('../../src/', import.meta.url);
  readFileSync(fileURLToPath(new URL(rel, root))).subarray(0, 4 * 1024 * 1024).toString('utf8');
  ```

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/core/rng.ts` | 确定性随机源：命名流派生 + 顺序流派生（`RngState` 级） | **新建** |
| `src/core/fingerprint.ts` | 稳定序列化 + 状态指纹 | **新建** |
| `src/ui/match-seed.ts` | 平台层种子来源（core 保持纯确定性） | **新建** |
| `src/core/models/types.ts` | `GameState` 新增 `rng` / `nextUid` 字段 | 修改 |
| `src/core/state/create.ts` | `createGame({seed})`、`nextUid(s)`、`randomPoolFromSeed` | 修改 |
| `src/core/engine/deck.ts` | 洗牌改走确定性随机源 | 修改 |
| `src/core/effects/resolve.ts` | `takeRandom` 随机取牌 + `draw` 洗弃牌堆 | 修改 |
| `src/core/effects/cards/fear.ts` / `lust.ts` / `time.ts` | 三处"随机取一张" | 修改 |
| `src/ui/home.ts` | 赌硬币结果由种子派生 | 修改 |
| `src/main.ts` | 开局种子 + 随机池由种子派生 | 修改 |
| `tests/rng.test.ts` | 随机源单测 | **新建** |
| `tests/state/create-seed.test.ts` | 种子与 uid 计数器进状态 | **新建** |
| `tests/state/random-pool.test.ts` | 开局派生（池 / 种子来源） | **新建** |
| `tests/fingerprint.test.ts` | 指纹单测 | **新建** |
| `tests/determinism/lib.ts` | 可重放步骤的记录/施加 | **新建** |
| `tests/determinism/replay.test.ts` | D1 / D2 / D3 | **新建** |
| `tests/determinism/fuzz-determinism.test.ts` | D4 | **新建** |
| `tests/core-purity.test.ts` | 源码守卫：core 内禁随机与时钟（Task 4 建、Task 9 扩展） | **新建** |
| `tests/fuzz/lib.ts` | 建局传显式 seed；移除 `Math.random` 补丁 | 修改 |
| `tests/models/types.test.ts` | 形状夹具补两个新字段 | 修改 |

---

## Task 1: 确定性随机源 `src/core/rng.ts`

**Files:**
- Create: `src/core/rng.ts`
- Test: `tests/rng.test.ts`

**Interfaces:**
- Consumes: 无（本任务不依赖 `GameState`）
- Produces:
  - `interface RngState { seed: string; n: number }`
  - `hash32(text: string): number`
  - `deriveInt(seed: string, label: string, bound: number): number`
  - `nextValue(r: RngState): number`
  - `nextInt(r: RngState, bound: number): number`
  - `pickFrom<T>(r: RngState, arr: readonly T[]): T | undefined`
  - `shuffleArr<T>(r: RngState, arr: readonly T[]): T[]`

**设计要点（务必理解，不要改动）**：值 = `f(seed, key)`，**没有任何内部可变状态**。顺序流靠调用方推进 `r.n`，因此"第 n 次随机"永远等于同一个值 —— 这让"中途存盘再恢复"天然安全。

- [ ] **Step 1: 写失败的测试**

创建 `tests/rng.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import {
  hash32,
  deriveInt,
  nextValue,
  nextInt,
  pickFrom,
  shuffleArr,
  type RngState,
} from '../src/core/rng';

const st = (seed = 'S'): RngState => ({ seed, n: 0 });

describe('rng：确定性随机源', () => {
  it('hash32 同串同值、异串异值', () => {
    expect(hash32('abc')).toBe(hash32('abc'));
    expect(hash32('abc')).not.toBe(hash32('abd'));
    expect(hash32('')).toBe(0x811c9dc5);
  });

  it('deriveInt 与调用顺序无关（先算别的标签不影响它）', () => {
    const before = deriveInt('S', 'coin', 2);
    deriveInt('S', 'pool', 45);
    deriveInt('S', 'pool', 45);
    expect(deriveInt('S', 'coin', 2)).toBe(before);
  });

  it('deriveInt 落在 [0, bound)', () => {
    for (const label of ['a', 'b', 'c', 'd', 'e', 'f', 'g']) {
      const v = deriveInt('S', label, 7);
      expect(v).toBeGreaterThanOrEqual(0);
      expect(v).toBeLessThan(7);
    }
  });

  it('deriveInt 对非正 bound 返回 0', () => {
    expect(deriveInt('S', 'x', 0)).toBe(0);
    expect(deriveInt('S', 'x', -3)).toBe(0);
  });

  it('nextValue 推进 n，序列可复现且不重复', () => {
    const a = st();
    const b = st();
    const seqA = [nextValue(a), nextValue(a), nextValue(a)];
    const seqB = [nextValue(b), nextValue(b), nextValue(b)];
    expect(seqA).toEqual(seqB);
    expect(a.n).toBe(3);
    expect(new Set(seqA).size).toBe(3);
  });

  it('nextInt 落在 [0, bound) 且分布大致均匀', () => {
    const r = st('dist');
    const hits = [0, 0, 0, 0];
    for (let i = 0; i < 400; i++) hits[nextInt(r, 4)] += 1;
    expect(hits.every((h) => h > 40)).toBe(true);
  });

  it('pickFrom 空数组返回 undefined 且不推进 n', () => {
    const r = st();
    expect(pickFrom(r, [] as number[])).toBeUndefined();
    expect(r.n).toBe(0);
  });

  it('shuffleArr 确定性排列、不改入参、元素不丢', () => {
    const src = [1, 2, 3, 4, 5, 6, 7, 8];
    const a = shuffleArr(st('sh'), src);
    const b = shuffleArr(st('sh'), src);
    expect(a).toEqual(b);
    expect([...a].sort((x, y) => x - y)).toEqual(src);
    expect(src).toEqual([1, 2, 3, 4, 5, 6, 7, 8]);
  });

  it('不同 seed 产生不同序列', () => {
    expect(nextValue(st('A'))).not.toBe(nextValue(st('B')));
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/rng.test.ts`
Expected: FAIL —— 无法解析模块 `../src/core/rng`（文件不存在）。

- [ ] **Step 3: 实现**

创建 `src/core/rng.ts`：

```ts
/**
 * 确定性随机源（G0；见 docs/2026-09-13-联机与多端-设计稿.md §2.3）。
 *
 * 两类派生，用途不同，不可混用：
 *  - 命名流派生 deriveInt(seed, label, bound)：与调用顺序无关。用于"必须先生成结果、
 *    再把它演算出来"的开局派生（掷硬币、随机池）—— 计算时机不会影响结果。
 *  - 顺序流派生 nextValue/nextInt/pickFrom/shuffleArr(r, ...)：原地推进 r.n。
 *
 * 值 = f(seed, key) 是纯函数，没有内部可变状态 → "中途存盘再恢复"天然安全
 * （只要 r.n 随状态一起序列化）。
 *
 * 铁律：本文件不得使用 Math.random / Date.now / crypto.* —— 否则整套确定性失效。
 * 平台层的种子来源见 src/ui/match-seed.ts。
 */

/** 随机源状态：必须进 GameState，因此进状态指纹 */
export interface RngState {
  seed: string;
  /** 已消耗次数 */
  n: number;
}

/** FNV-1a 32 位字符串哈希 */
export function hash32(text: string): number {
  let h = 0x811c9dc5;
  for (let i = 0; i < text.length; i++) {
    h ^= text.charCodeAt(i);
    h = Math.imul(h, 0x01000193);
  }
  return h >>> 0;
}

/** mulberry32：纯整数运算，跨 JS 引擎一致 */
function mulberry32(a: number): number {
  let t = (a + 0x6d2b79f5) >>> 0;
  t = Math.imul(t ^ (t >>> 15), t | 1);
  t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
  return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
}

/** 值 = f(seed, key)：同参恒同值，无内部状态 */
function streamValue(seed: string, key: string): number {
  return mulberry32(hash32(`${seed}|${key}`));
}

/** 命名流派生：与调用顺序无关 */
export function deriveInt(seed: string, label: string, bound: number): number {
  if (!Number.isFinite(bound) || bound <= 0) return 0;
  return Math.floor(streamValue(seed, `@${label}`) * bound);
}

/** 顺序流派生：推进 r.n，返回 [0,1) */
export function nextValue(r: RngState): number {
  const v = streamValue(r.seed, `#${r.n}`);
  r.n += 1;
  return v;
}

/** 顺序流派生：返回 [0, bound) */
export function nextInt(r: RngState, bound: number): number {
  if (!Number.isFinite(bound) || bound <= 0) return 0;
  return Math.floor(nextValue(r) * bound);
}

/** 从数组确定性取一个；空数组返回 undefined（不推进 n） */
export function pickFrom<T>(r: RngState, arr: readonly T[]): T | undefined {
  if (arr.length === 0) return undefined;
  return arr[nextInt(r, arr.length)];
}

/** 确定性洗牌：返回新数组，不修改入参 */
export function shuffleArr<T>(r: RngState, arr: readonly T[]): T[] {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = nextInt(r, i + 1);
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/rng.test.ts`
Expected: PASS（9 项）。

- [ ] **Step 5: 提交**

```bash
git add src/core/rng.ts tests/rng.test.ts
git commit -m "feat(G0): 新增确定性随机源 src/core/rng.ts" -m "命名流派生 deriveInt（与调用顺序无关）+ 顺序流派生 nextValue/nextInt/pickFrom/shuffleArr（推进 RngState.n）。值 = f(seed,key) 纯函数，无内部可变状态。"
```

---

## Task 2: 把随机源与 uid 计数器接进 `GameState`

**Files:**
- Modify: `src/core/models/types.ts:327-384`（`GameState` 接口）
- Modify: `src/core/state/create.ts:8-12`（`uidCounter`/`nextUid`）、`:26-35`（`CreateGameOptions`）、`:41-69`（`createGame`）、`:204`（`nextUid()` 调用点）
- Modify: `src/core/rng.ts`（追加 `GameState` 便捷包装）
- Modify: `tests/models/types.test.ts:10-39`（形状夹具补字段）
- Test: `tests/state/create-seed.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `RngState`、`nextInt`、`pickFrom`、`shuffleArr`
- Produces:
  - `GameState.rng: RngState`、`GameState.nextUid: number`
  - `nextUid(s: GameState): string`
  - `nextAutoSeed(): string`
  - `createGame(opts?: CreateGameOptions)`，其中 `CreateGameOptions.seed?: string`
  - `randInt(s: GameState, bound: number): number`
  - `randPick<T>(s: GameState, arr: readonly T[]): T | undefined`
  - `shuffleWith<T>(s: GameState, arr: readonly T[]): T[]`

**关键约束**：`createGame()` **不传 seed 时也不得影响既有行为**。`draftStarter` 的默认值仍是 `0`（不能改成"由种子派生"，否则 `tests/state/create.test.ts:12,22` 的 1-2-2-1 顺序断言会随机失败）。种子派生只发生在**调用方显式请求**时（Task 6）。

- [ ] **Step 1: 写失败的测试**

创建 `tests/state/create-seed.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { createGame, nextUid, nextAutoSeed, setSeedNonce } from '../../src/core/state/create';

describe('createGame 的种子与 uid 计数器（G0）', () => {
  it('显式 seed 写进状态', () => {
    expect(createGame({ seed: 'S1' }).rng).toEqual({ seed: 'S1', n: 0 });
  });

  it('不传 seed 时每局种子不同（保持"每局不同"旧行为）', () => {
    expect(createGame().rng.seed).not.toBe(createGame().rng.seed);
  });

  it('uid 计数器在状态里：两局都从 c1 开始', () => {
    const s1 = createGame({ seed: 'A' });
    const s2 = createGame({ seed: 'B' });
    expect(nextUid(s1)).toBe('c1');
    expect(nextUid(s2)).toBe('c1');
    expect(nextUid(s1)).toBe('c2');
    expect(s1.nextUid).toBe(3);
  });

  it('uid 计数器随状态序列化一起恢复', () => {
    const s = createGame({ seed: 'A' });
    nextUid(s);
    const restored = JSON.parse(JSON.stringify(s)) as typeof s;
    expect(nextUid(restored)).toBe('c2');
  });

  it('draftStarter 默认仍为 0（不得改成种子派生）', () => {
    expect(createGame({ seed: 'Z' }).draftStarter).toBe(0);
  });

  it('兜底种子格式为 auto-<nonce>-<n>', () => {
    expect(createGame().rng.seed).toMatch(/^auto-.+-\d+$/);
  });

  it('注入运行期 nonce 后，兜底种子跨进程/跨重启也不同', () => {
    setSeedNonce('runA');
    const a = createGame().rng.seed;
    setSeedNonce('runB');
    const b = createGame().rng.seed;
    expect(a).not.toBe(b);
    expect(a).toContain('runA');
    expect(b).toContain('runB');
  });

  it('显式 seed 不消耗兜底计数器', () => {
    setSeedNonce('counter');
    const first = nextAutoSeed();
    createGame({ seed: 'explicit' });
    const second = nextAutoSeed();
    const n1 = Number(first.slice(first.lastIndexOf('-') + 1));
    const n2 = Number(second.slice(second.lastIndexOf('-') + 1));
    expect(n2).toBe(n1 + 1);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/state/create-seed.test.ts`
Expected: FAIL —— `createGame` 不接受 `seed`（TS 报错）/ `s.rng` 为 undefined。

- [ ] **Step 3: 给 `GameState` 加字段**

在 `src/core/models/types.ts` 顶部 import 区加：

```ts
import type { RngState } from '../rng';
```

在 `GameState` 接口内（`pendingCompile` 之后）加：

```ts
  /** 确定性随机源（G0）：进状态 → 进指纹；重放/存盘恢复靠它，不得放在模块作用域 */
  rng: RngState;
  /** 卡牌 uid 计数器（G0）：原为模块级全局 uidCounter，会让"同一串操作"在不同进程得到不同 uid */
  nextUid: number;
```

- [ ] **Step 4: 改 `create.ts`**

`src/core/state/create.ts:8-12` 的

```ts
let uidCounter = 0;
export function nextUid(): string {
  uidCounter += 1;
  return `c${uidCounter}`;
}
```

替换为：

```ts
/** uid 计数器进状态（G0）：原为模块级全局，跨进程/重放会错位 */
export function nextUid(s: GameState): string {
  const uid = `c${s.nextUid}`;
  s.nextUid += 1;
  return uid;
}

/** 运行期 nonce：core 不产生熵，由平台层在启动时注入一次（src/ui/match-seed.ts）。
 *  未注入时用 'p0' 兜底 —— 此时**仅保证进程内每局不同**。 */
let seedNonce = 'p0';

/** 平台层注入运行期 nonce（src/main.ts 启动处调用）。
 *  幂等：重复注入直接覆盖。 */
export function setSeedNonce(nonce: string): void {
  seedNonce = nonce;
}

/** 未显式传 seed 时的兜底种子：运行期 nonce + 进程内单调计数。
 *  nonce 已注入 → 跨进程/跨重启也不同；未注入 → 仅保证进程内每局不同。
 *  ⚠️ **联机与重放必须显式传 seed** —— 否则两端拿不到同一个洗牌顺序。 */
let autoSeedCounter = 0;
export function nextAutoSeed(): string {
  autoSeedCounter += 1;
  return `auto-${seedNonce}-${autoSeedCounter}`;
}
```

> **为什么兜底种子不能只是进程内计数器**（评审发现 Important）：那样每个进程的第一局都拿到 `auto-1`，一旦洗牌接上 `s.rng`（Task 3），热座玩法每次重启都会得到**同一副牌序**。现有测试抓不到这个 —— 它只在同一进程内连开多局。nonce 由平台层注入，core 依旧不产生熵。

`CreateGameOptions`（`:26-35`）加一个字段：

```ts
  /** 对局种子（G0）：决定洗牌与全部对局内随机；联机/重放必须显式传入 */
  seed?: string;
```

`createGame`（`:41-43`）改成：

```ts
export function createGame(opts: CreateGameOptions = {}): GameState {
  const draftStarter: PlayerId = opts.draftStarter ?? 0;
  const seed = opts.seed ?? nextAutoSeed();
  return {
    rng: { seed, n: 0 },
    nextUid: 1,
```

（`rng`/`nextUid` 两行插在返回字面量最前面即可。）

`assignProtocols` 内的 `uid: nextUid(),`（`:204`）改为 `uid: nextUid(s),`。

- [ ] **Step 5: 在 `rng.ts` 追加 `GameState` 便捷包装**

先在 `src/core/rng.ts` **顶部**加 type-only import（`rng.ts` 原本没有任何 import，加在文件最前面）：

```ts
import type { GameState } from './models/types';
```

> 说明：`types.ts` 会反向 `import type { RngState } from '../rng'`，两者构成 **type-only 循环**。TS 在编译期擦除类型导入，不产生运行时循环依赖 —— 这是允许的。

然后在文件**末尾**追加：

```ts
/* ---------- GameState 便捷包装（引擎调用点统一用这三个） ---------- */

/** 顺序流派生：[0, bound) */
export function randInt(s: GameState, bound: number): number {
  return nextInt(s.rng, bound);
}

/** 从数组确定性取一个；空数组返回 undefined */
export function randPick<T>(s: GameState, arr: readonly T[]): T | undefined {
  return pickFrom(s.rng, arr);
}

/** 确定性洗牌：返回新数组，不修改入参 */
export function shuffleWith<T>(s: GameState, arr: readonly T[]): T[] {
  return shuffleArr(s.rng, arr);
}
```

- [ ] **Step 6: 补形状夹具**

`tests/models/types.test.ts:10` 的 `const s: GameState = {` 字面量内（`pendingCompile: null,` 之后）加：

```ts
      rng: { seed: 'test', n: 0 },
      nextUid: 1,
```

- [ ] **Step 7: 跑 tsc 与测试**

Run: `npx tsc --noEmit`
Expected: 0 错。若报"缺少属性 rng/nextUid"，说明还有手写的 `GameState` 字面量没补 —— 已知只有 `tests/models/types.test.ts:10` 一处。

Run: `npx vitest run tests/state tests/models tests/rng.test.ts`
Expected: PASS

- [ ] **Step 8: 全量回归**

Run: `npx vitest run`
Expected: 全绿（842 + 新增 14 项）。特别注意 `tests/state/create.test.ts:40`「opening hands differ across games」与 `tests/fuzz/gen3-random-games.test.ts` 仍通过。

- [ ] **Step 9: 提交**

```bash
git add src/core/models/types.ts src/core/state/create.ts src/core/rng.ts tests/models/types.test.ts tests/state/create-seed.test.ts
git commit -m "feat(G0): 随机源与 uid 计数器进 GameState" -m "uidCounter 从模块级全局改为 s.nextUid（跨进程/重放不再错位）；GameState 新增 rng 字段承载确定性随机源；createGame 接受 seed，不传时用进程内单调种子保持每局不同。" -m "draftStarter 默认值保持 0，不改为种子派生（避免既有草稿顺序断言随机失败）。"
```

---

## Task 3: 洗牌改走确定性随机源（5 处）

**Files:**
- Modify: `src/core/engine/deck.ts:8-15`（删除 `shuffle`）、`:42`、`:89`、import 区
- Modify: `src/core/state/create.ts:4`（import）、`:242-243`
- Modify: `src/core/effects/resolve.ts:2`（import）、`:401`
- Test: 复用 `tests/engine/deck.test.ts` 与 `tests/state/create.test.ts`（回归）

**Interfaces:**
- Consumes: Task 2 的 `shuffleWith(s, arr)`
- Produces: `src/core/engine/deck.ts` **不再导出** `shuffle`

**注意**：`shuffle` 当前从 `deck.ts` 导出，被 `create.ts:4` 与 `resolve.ts:2` import。三处 import 必须同步改，否则 `tsc` 会报未导出。已知调用点共 **5 处**，全部所在函数都已持有 `s`（`performDraftPick` / `drawCards` / `shuffleDeck` / `resolve` 的 `draw` case）。

- [ ] **Step 1: 确认调用点清单（防漏）**

Run: `git grep -n "shuffle(" -- src/core`
Expected: 只列出 `deck.ts` 定义 + 5 个调用点（`create.ts:242`、`create.ts:243`、`deck.ts:42`、`deck.ts:89`、`resolve.ts:401`）。若有第 6 处，一并处理。

- [ ] **Step 2: 改 `deck.ts`**

删除 `src/core/engine/deck.ts:8-15` 整个 `shuffle` 函数：

```ts
export function shuffle<T>(arr: T[]): T[] {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}
```

在 import 区（`src/core/trace.ts` 那行之后）加：

```ts
import { shuffleWith } from '../rng';
```

`:42` `p.deck = shuffle(p.trash);` → `p.deck = shuffleWith(s, p.trash);`
`:89` `p.deck = shuffle(p.deck);` → `p.deck = shuffleWith(s, p.deck);`

- [ ] **Step 3: 改 `create.ts`**

`:4` `import { drawCards, shuffle } from '../engine/deck';` → `import { drawCards } from '../engine/deck';`

并在 import 区加：

```ts
import { shuffleWith } from '../rng';
```

`:242-243`：

```ts
    s.players[0].deck = shuffle(s.players[0].deck);
    s.players[1].deck = shuffle(s.players[1].deck);
```

→

```ts
    s.players[0].deck = shuffleWith(s, s.players[0].deck);
    s.players[1].deck = shuffleWith(s, s.players[1].deck);
```

- [ ] **Step 4: 改 `resolve.ts`**

`:2` `import { drawCards, discardFromHand, shuffle } from '../engine/deck';` → 去掉 `shuffle`。

并在 import 区加：

```ts
import { shuffleWith } from '../rng';
```

`:401` `os.deck = shuffle(os.trash);` → `os.deck = shuffleWith(s, os.trash);`

- [ ] **Step 5: 跑 tsc 与全量测试**

Run: `npx tsc --noEmit`
Expected: 0 错（若报 `shuffle` 未导出，说明还有 import 没改）。

Run: `npx vitest run`
Expected: 全绿。

- [ ] **Step 6: 提交**

```bash
git add src/core/engine/deck.ts src/core/state/create.ts src/core/effects/resolve.ts
git commit -m "feat(G0): 洗牌改走确定性随机源（5 处调用点）" -m "deck.ts 不再导出 Math.random 版 shuffle；create.ts 开局洗牌、drawCards 洗弃牌堆、shuffleDeck 切洗、resolve 的 draw-from-opponent-deck 全部改走 shuffleWith(s, arr)。"
```

---

## Task 4: 四处效果内随机改走确定性随机源

**Files:**
- Modify: `src/core/effects/resolve.ts:668`（`takeRandom` case）、import 区
- Modify: `src/core/effects/cards/fear.ts:99`、import 区
- Modify: `src/core/effects/cards/lust.ts:51`、import 区
- Modify: `src/core/effects/cards/time.ts:120`、import 区
- Test: `tests/effects/fear.test.ts`、`tests/effects/love.test.ts`、`tests/effects/gen2-batch3-cards.test.ts`（time-0/time-2 所在）回归；其余由全量 `npx vitest run` 覆盖

**Interfaces:**
- Consumes: Task 2 的 `randPick(s, arr)`、`randInt(s, bound)`
- Produces: `src/core` 内 `Math.random` **调用数** = 0（**注释里提到该名称不算**：`rng.ts` 的「铁律」注释与 `love.ts` 的旧说明都属于此类，后者由 Task 10 清理）

**四处原文（改动前 → 改动后）**：

| 文件:行 | 原文 | 改为 |
|---|---|---|
| `resolve.ts:668` | `const idx = Math.floor(Math.random() * hand.length);` | `const idx = randInt(s, hand.length);` |
| `fear.ts:99` | `const pick = hand[Math.floor(Math.random() * hand.length)];` | `const pick = randPick(ctx.s, hand)!;` |
| `lust.ts:51` | `const pick = foeHand[Math.floor(Math.random() * foeHand.length)];` | `const pick = randPick(ctx.s, foeHand)!;` |
| `time.ts:120` | `const pick = trash[Math.floor(Math.random() * trash.length)];` | `const pick = randPick(ctx.s, trash)!;` |

三处效果都在前面有 `if (…length === 0) return;` 守卫，所以 `!` 断言安全。

- [ ] **Step 1: 定位全部命中**

Run: `git grep -n "Math.random" -- src/core`
Expected: 4 处（`resolve.ts:668`、`fear.ts:99`、`lust.ts:51`、`time.ts:120`）。若 Task 3 已完成，`deck.ts` 不应再出现。

- [ ] **Step 2: 写失败的测试**

创建 `tests/core-purity.test.ts`（Task 9 会在同一文件上继续扩展）：

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync, readdirSync, statSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { join } from 'node:path';

/**
 * G0 守卫：src/core 必须保持纯确定性。
 * 见 docs/2026-09-13-联机与多端-设计稿.md §2.2 / §2.8。
 */

const CORE_DIR = fileURLToPath(new URL('../src/core/', import.meta.url));

function walk(dir: string, out: string[] = []): string[] {
  for (const name of readdirSync(dir)) {
    const full = join(dir, name);
    if (statSync(full).isDirectory()) walk(full, out);
    else if (name.endsWith('.ts')) out.push(full);
  }
  return out;
}

const FILES = walk(CORE_DIR);

function read(file: string): string {
  return readFileSync(file).subarray(0, 4 * 1024 * 1024).toString('utf8');
}

/** 相对路径（报错时好读） */
function rel(file: string): string {
  return file.slice(CORE_DIR.length).replace(/\\/g, '/');
}

describe('src/core 纯确定性守卫（G0）', () => {
  it('扫描到足够的源文件（防路径写错导致空扫描假绿）', () => {
    expect(FILES.length).toBeGreaterThan(30);
  });

  it('不得出现 Math.random 调用', () => {
    // 只匹配"调用"（后跟左括号）：注释里提到 Math.random 不构成违规
    // （src/core/rng.ts 的文档注释里就写着这条铁律，宽松正则会把它误判为违规）
    const bad = FILES.filter((f) => /\bMath\s*\.\s*random\s*\(/.test(read(f))).map(rel);
    expect(bad, `以下 core 文件调用了 Math.random（会破坏联机确定性）：${bad.join(', ')}`).toEqual([]);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npx vitest run tests/core-purity.test.ts`
Expected: **FAIL**，错误信息列出 4 个文件：`effects/resolve.ts, effects/cards/fear.ts, effects/cards/lust.ts, effects/cards/time.ts`。
（若 Task 3 已完成，`engine/deck.ts` 不应出现在列表里。）

- [ ] **Step 4: 实施四处替换**

按上表逐处替换；每个文件在 import 区加：

- `resolve.ts`：`import { randInt } from '../rng';`
- `fear.ts` / `lust.ts` / `time.ts`：`import { randPick } from '../../rng';`

（按各文件既有 import 相对路径风格调整；`cards/*.ts` 到 `src/core/rng.ts` 是 `../../rng`。）

- [ ] **Step 5: 全量验证**

Run: `npx vitest run tests/core-purity.test.ts`
Expected: PASS。

Run: `git grep -n "Math.random" -- src/core`
Expected: 只剩**注释里的字面量提及**（`rng.ts` 的铁律注释，以及由 Task 10 清理的 `love.ts` 旧说明）；**调用数为 0**。判据是「调用形式（后跟左括号）」，不是这行 grep 是否为空 —— 权威判据是 `tests/core-purity.test.ts` 的守卫断言。

Run: `npx tsc --noEmit`
Expected: 0 错。

Run: `npx vitest run`
Expected: 全绿。

- [ ] **Step 6: 提交**

```bash
git add src/core/effects/resolve.ts src/core/effects/cards/fear.ts src/core/effects/cards/lust.ts src/core/effects/cards/time.ts tests/core-purity.test.ts
git commit -m "feat(G0): 四处效果内随机改走确定性随机源" -m "takeRandom（love-3 随机拿牌）/ fear-4 随机弃牌 / lust-3 随机揭示 / time-3 随机揭示弃牌堆，全部改走 randInt/randPick。src/core 内 Math.random 命中数降为 0，并加源码守卫防回归。"
```

---

## Task 5: 状态指纹 `src/core/fingerprint.ts`

**Files:**
- Create: `src/core/fingerprint.ts`
- Test: `tests/fingerprint.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `hash32`
- Produces:
  - `stableStringify(v: unknown): string`
  - `hash64(text: string): string`
  - `stateFingerprint(s: GameState): string`

**设计说明**：`s.log` 与其余字段全部纳入指纹 —— 已确认 `pushLog`（`src/core/log.ts:17-20`）只做字符串缩进拼接，**不含任何时间戳**，所以日志是确定性的，纳入指纹只会更严格而不会误报。

- [ ] **Step 1: 写失败的测试**

创建 `tests/fingerprint.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { stableStringify, hash64, stateFingerprint } from '../src/core/fingerprint';
import { createGame, performDraftPick, getDraftPool } from '../src/core/state/create';

describe('状态指纹（G0）', () => {
  it('stableStringify 与对象键顺序无关', () => {
    expect(stableStringify({ b: 1, a: 2 })).toBe(stableStringify({ a: 2, b: 1 }));
  });

  it('stableStringify 数组保序', () => {
    expect(stableStringify([1, 2])).not.toBe(stableStringify([2, 1]));
  });

  it('stableStringify 处理 null / 原始值 / 嵌套', () => {
    expect(stableStringify(null)).toBe('null');
    expect(stableStringify(3)).toBe('3');
    expect(stableStringify('x')).toBe('"x"');
    expect(stableStringify({ a: [1, { b: null }] })).toBe('{"a":[1,{"b":null}]}');
  });

  it('hash64 是 16 位十六进制且同串同值', () => {
    const h = hash64('hello');
    expect(h).toMatch(/^[0-9a-f]{16}$/);
    expect(hash64('hello')).toBe(h);
    expect(hash64('hello')).not.toBe(hash64('hellp'));
  });

  it('同一状态多次指纹相同', () => {
    const s = createGame({ seed: 'F1' });
    expect(stateFingerprint(s)).toBe(stateFingerprint(s));
  });

  it('随机源计数进指纹（证明 rng 被覆盖）', () => {
    const s = createGame({ seed: 'F2' });
    const before = stateFingerprint(s);
    s.rng.n += 1;
    expect(stateFingerprint(s)).not.toBe(before);
  });

  it('uid 计数器进指纹', () => {
    const s = createGame({ seed: 'F3' });
    const before = stateFingerprint(s);
    s.nextUid += 1;
    expect(stateFingerprint(s)).not.toBe(before);
  });

  it('操作会改变指纹，且同种子同操作指纹一致', () => {
    const a = createGame({ seed: 'F4' });
    const b = createGame({ seed: 'F4' });
    const start = stateFingerprint(a);
    performDraftPick(a, getDraftPool(a)[0].defId);
    expect(stateFingerprint(a)).not.toBe(start);
    performDraftPick(b, getDraftPool(b)[0].defId);
    expect(stateFingerprint(b)).toBe(stateFingerprint(a));
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/fingerprint.test.ts`
Expected: FAIL —— 无法解析模块 `../src/core/fingerprint`。

- [ ] **Step 3: 实现**

创建 `src/core/fingerprint.ts`：

```ts
/**
 * 状态指纹（G0；见 docs/2026-09-13-联机与多端-设计稿.md §2.6）。
 *
 * 用途：确定性测试台（D1~D4）比对、以及将来联机时的分歧检测。
 * 信任制下不需要密码学强度，只需要"能稳定地指出两个状态是否相同"。
 */

import type { GameState } from './models/types';
import { hash32 } from './rng';

/** 稳定序列化：对象键排序、数组保序。用于指纹与联机校验。 */
export function stableStringify(v: unknown): string {
  if (v === null || typeof v !== 'object') return JSON.stringify(v) ?? 'null';
  if (Array.isArray(v)) return `[${v.map(stableStringify).join(',')}]`;
  const obj = v as Record<string, unknown>;
  const keys = Object.keys(obj).sort();
  return `{${keys.map((k) => `${JSON.stringify(k)}:${stableStringify(obj[k])}`).join(',')}}`;
}

/** FNV-1a 32 位拼两段做 64 位，降低碰撞概率 */
export function hash64(text: string): string {
  const a = hash32(text);
  const b = hash32(`${text}#${a}`);
  return a.toString(16).padStart(8, '0') + b.toString(16).padStart(8, '0');
}

/** 状态指纹。全部字段纳入（含 log / rng.n / nextUid）——它们都是确定性的。 */
export function stateFingerprint(s: GameState): string {
  return hash64(stableStringify(s));
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run tests/fingerprint.test.ts`
Expected: PASS（8 项）。

- [ ] **Step 5: 提交**

```bash
git add src/core/fingerprint.ts tests/fingerprint.test.ts
git commit -m "feat(G0): 新增状态指纹 src/core/fingerprint.ts" -m "stableStringify（键排序）+ hash64（FNV-1a 拼两段）+ stateFingerprint。全部字段纳入指纹：pushLog 无时间戳，log 是确定性的。"
```

---

## Task 6: 开局派生（掷硬币 / 随机池）由种子决定 + UI 接线

**Files:**
- Create: `src/ui/match-seed.ts`
- Modify: `src/core/state/create.ts`（追加 `randomPoolFromSeed`）
- Modify: `src/ui/home.ts:22-26`（`CoinNav`）、`:361`（硬币结果）
- Modify: `src/main.ts:495-503`（`randomDraftPool`）、`:505-515`（`showCoin`）
- Test: `tests/state/random-pool.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `deriveInt`
- Produces:
  - `randomPoolFromSeed(seed: string, count?: number, pool?: ProtocolDef[]): ProtocolDef[]`
  - `newMatchSeed(): string`

**为什么硬币与随机池用命名流而不是顺序流**：`showCoin()` 里硬币先算、随机池后算（且池要等 `beginGame` 才建局）。命名流 `deriveInt(seed, label, bound)` 与计算时机/顺序无关，因此"先看硬币再决定要不要随机池"不会改变任何一个结果。

**为什么 `newMatchSeed()` 放在 `src/ui/` 而不是 `src/core/`**：它是平台职责（需要 WebCrypto 或系统随机），放进 core 会让"core 内禁 Math.random"的守卫失效。core 只接受种子，不生产种子。

- [ ] **Step 1: 写失败的测试**

创建 `tests/state/random-pool.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { randomPoolFromSeed } from '../../src/core/state/create';
import { DEMO_PROTOCOLS } from '../../src/data/demo';
import { newMatchSeed } from '../../src/ui/match-seed';

describe('开局派生由种子决定（G0）', () => {
  it('同种子得到同一个池', () => {
    const a = randomPoolFromSeed('S').map((p) => p.defId);
    const b = randomPoolFromSeed('S').map((p) => p.defId);
    expect(a).toEqual(b);
  });

  it('默认取 12 套且不重复', () => {
    const ids = randomPoolFromSeed('S').map((p) => p.defId);
    expect(ids).toHaveLength(12);
    expect(new Set(ids).size).toBe(12);
  });

  it('池是全部协议的子集', () => {
    const all = new Set(DEMO_PROTOCOLS.map((p) => p.defId));
    for (const id of randomPoolFromSeed('S').map((p) => p.defId)) expect(all.has(id)).toBe(true);
  });

  it('不同种子（通常）得到不同的池', () => {
    const a = randomPoolFromSeed('S1').map((p) => p.defId).join(',');
    const b = randomPoolFromSeed('S2').map((p) => p.defId).join(',');
    expect(a).not.toBe(b);
  });

  it('不修改入参池', () => {
    const src = [...DEMO_PROTOCOLS];
    randomPoolFromSeed('S', 12, src);
    expect(src.map((p) => p.defId)).toEqual(DEMO_PROTOCOLS.map((p) => p.defId));
  });

  it('newMatchSeed 两次调用不同且非空', () => {
    const a = newMatchSeed();
    const b = newMatchSeed();
    expect(a.length).toBeGreaterThan(0);
    expect(a).not.toBe(b);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run tests/state/random-pool.test.ts`
Expected: FAIL —— `randomPoolFromSeed` 未导出、`../../src/ui/match-seed` 不存在。

- [ ] **Step 3: 实现 `randomPoolFromSeed`**

在 `src/core/state/create.ts` 的 `getDraftPool`（`:72`）之前插入：

```ts
/** 随机池：由种子派生 count 套（G0）。用命名流派生，因此与调用时机无关 ——
 *  UI 可以"先掷硬币、再决定要不要随机池"，结果不变。 */
export function randomPoolFromSeed(
  seed: string,
  count = 12,
  pool: ProtocolDef[] = DEMO_PROTOCOLS,
): ProtocolDef[] {
  const a = [...pool];
  for (let i = a.length - 1; i > 0; i--) {
    const j = deriveInt(seed, `pool:${i}`, i + 1);
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a.slice(0, count);
}
```

并在 `create.ts` 的 import 区加：

```ts
import { deriveInt } from '../rng';
```

（若 Task 3 已加过 `shuffleWith` 的 import，合并为一行 `import { deriveInt, shuffleWith } from '../rng';`。）

- [ ] **Step 4: 实现 `src/ui/match-seed.ts`**

```ts
/**
 * 新对局种子（G0）。平台层职责：core 必须保持纯确定性，只接受种子、不生产种子。
 * 这是全项目**唯一**允许的种子随机来源。
 */
export function newMatchSeed(): string {
  const c = globalThis.crypto;
  if (c && typeof c.getRandomValues === 'function') {
    const bytes = new Uint8Array(16);
    c.getRandomValues(bytes);
    let out = '';
    for (const b of bytes) out += b.toString(16).padStart(2, '0');
    return out;
  }
  // 兜底：旧 Node 没有 globalThis.crypto
  return Math.random().toString(36).slice(2) + Math.random().toString(36).slice(2);
}
```

- [ ] **Step 5: 改 `home.ts` 的硬币**

`src/ui/home.ts:22-26` 的 `CoinNav` 加一个字段：

```ts
export interface CoinNav {
  backHome(): void;
  /** 本局种子（G0）：硬币结果由它派生，保证双方一致、且可复现 */
  seed: string;
  beginGame(draftStarter: PlayerId): void;
}
```

`:361` 的

```ts
    const landed: 1 | 2 = Math.random() < 0.5 ? 1 : 2;
```

改为：

```ts
    // G0：硬币结果由种子派生（原来是 Math.random）—— 动画只是把已确定的结果演出来
    const landed: 1 | 2 = deriveInt(nav.seed, 'coin', 2) === 0 ? 1 : 2;
```

并在 `home.ts` 的 import 区加：

```ts
import { deriveInt } from '../core/rng';
```

- [ ] **Step 6: 改 `main.ts`**

删除 `:495-503` 的整个 `randomDraftPool` 函数：

```ts
/** 随机池：从三代全部协议（45 套）中随机抽取 12 套 */
function randomDraftPool(): typeof DEMO_PROTOCOLS {
  const pool = [...DEMO_PROTOCOLS];
  for (let i = pool.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [pool[i], pool[j]] = [pool[j], pool[i]];
  }
  return pool.slice(0, 12);
}
```

`:505-515` 的 `showCoin` 改为：

```ts
function showCoin(): void {
  // G0：种子在开局前生成一次，硬币与随机池都由它派生 → 可复现、可联机
  const seed = newMatchSeed();
  renderCoin(root, {
    backHome: showModeSelect,
    seed,
    beginGame: (starter) => {
      state = createGame({
        seed,
        draftStarter: starter,
        firstToPlay: (1 - starter) as PlayerId,
        draftMode: gameOptions.ban ? 'ban' : 'normal',
        draftPool: gameOptions.randomPool ? randomPoolFromSeed(seed, 12) : undefined,
      });
      renderApp(root, state, cb);
```

（`renderApp(...)` 之后的原有代码保持不变。）

> 若 `DEMO_PROTOCOLS` 在 `main.ts` 中已不再被使用，一并从 import 中移除（tsconfig 未开 `noUnusedLocals`，不会报错，但留着是噪音）。

在 `main.ts` 的 import 区加：

```ts
import { newMatchSeed } from './ui/match-seed';
```

并把 `createGame` 所在的那一行 import 补上 `randomPoolFromSeed` 与 `setSeedNonce`（`createGame` 已从 `./core/state/create` 导入）。

然后在 `main.ts` 的模块顶层（其它初始化调用附近）注入一次运行期 nonce：

```ts
// 启动时注入运行期 nonce（G0）：使任何未显式传 seed 的 createGame() 也不会跨重启重复同一牌序
setSeedNonce(newMatchSeed());
```

- [ ] **Step 7: 跑 tsc 与测试**

Run: `npx tsc --noEmit`
Expected: 0 错。若报 `renderCoin` 缺 `seed`，检查是否有第二处调用 `renderCoin`（已知只有 `main.ts` 的 `showCoin`）。

Run: `npx vitest run`
Expected: 全绿。

- [ ] **Step 8: 人工验收（热座）**

Run: `npx vite build` → 成功。然后请用户在自己的 5173 上确认：进入热座 → 掷硬币 → 硬币动画正常、结果出现 → 开始对局后开局手牌与之前无异常。**同一种子下重开一局，硬币面与随机池应与第一次一致**（可用开发者模式打印 `state.rng.seed` 验证）。

- [ ] **Step 9: 提交**

```bash
git add src/ui/match-seed.ts src/core/state/create.ts src/ui/home.ts src/main.ts tests/state/random-pool.test.ts
git commit -m "feat(G0): 掷硬币与随机池改由开局种子派生" -m "新增 src/ui/match-seed.ts（平台层种子来源，core 保持纯确定性）；create.ts 新增 randomPoolFromSeed（命名流派生，与调用时机无关）；home.ts 硬币结果、main.ts 随机池不再用 Math.random。" -m "showCoin 开局前生成一次种子，同时用于硬币与随机池，并传给 createGame。"
```

---

## Task 7: 确定性测试台 D1 / D2 / D3

**Files:**
- Create: `tests/determinism/lib.ts`
- Create: `tests/determinism/replay.test.ts`
- Modify: `tests/fuzz/lib.ts:92-104`（`setupGen3Game` 传显式 seed）

**Interfaces:**
- Consumes: Task 2 的 `createGame({seed})`、Task 5 的 `stateFingerprint`
- Produces（`tests/determinism/lib.ts` 导出）：
  - `type Step = { t: 'action'; player: PlayerId; kind: ActionKind; args?: unknown } | { t: 'answer'; id: string; choice: string[] }`
  - `applyStep(s: GameState, step: Step): void`
  - `recordRandomSteps(s: GameState, r: () => number, max: number): Step[]`

**三条不变式**：
- **D1 重放一致** —— 同种子 + 同步骤 → 同指纹
- **D2 存盘恢复一致** —— 每步 `JSON.parse(JSON.stringify())` 后继续 → 指纹不变（抓"状态不自足"）
- **D3 跨模块实例一致** —— `vi.resetModules()` 后全新 import 跑同步骤 → 指纹不变（抓**模块级全局状态**；`uidCounter` 就是被这条抓出来的）

- [ ] **Step 1: 给 fuzz 库传显式种子**

`tests/fuzz/lib.ts:92-104` 的 `setupGen3Game` 中，把 `const s = createGame();` 改为：

```ts
  const s = createGame({ seed: String(seed) });
```

> 这一行是 D1/D2/D3 能在"同种子"前提下对比的前提。其余逻辑不动。

- [ ] **Step 2: 写 `tests/determinism/lib.ts`**

```ts
import type { GameState, Line, PlayerId } from '../../src/core/models/types';
import { executeAction, getLegalActions, type LegalAction } from '../../src/core/game';
import { answerEffect, runStack } from '../../src/core/effects/resolve';
import { randomAnswer } from '../fuzz/lib';

/** 与引擎 ActionKind 同源，避免手写字面量漂移 */
type ActionKind = LegalAction['kind'];

/** 可重放的一步（G0 内部用；正式档案格式 MatchFile 见设计稿 §3，属于 G3） */
export type Step =
  | { t: 'action'; player: PlayerId; kind: ActionKind; args?: unknown }
  | { t: 'answer'; id: string; choice: string[] };

/**
 * 施加一步。
 * **必须按 kind 分支调用**：`executeAction` 是窄化重载（game.ts:115-121），
 * 传一个联合类型的 kind 无法通过重载解析。这里与 main.ts 的 `onAction` 分发同构。
 */
export function applyStep(s: GameState, step: Step): void {
  if (step.t === 'answer') {
    answerEffect(s, step.id, step.choice);
    runStack(s);
    return;
  }
  switch (step.kind) {
    case 'play': {
      const a = step.args as { cardUid: string; faceUp: boolean; line: Line; target?: PlayerId };
      executeAction(s, step.player, 'play', a);
      break;
    }
    case 'compile':
      executeAction(s, step.player, 'compile', step.args as { line: Line });
      break;
    case 'resolve-trigger':
      executeAction(s, step.player, 'resolve-trigger', step.args as { cardUid: string });
      break;
    case 'effect-choice':
      executeAction(s, step.player, 'effect-choice', step.args as { promptId: string; choice: string[] });
      break;
    case 'rearrange-protocols':
      // 本驱动不产生该步（由 UI 模态直接提交）——显式抛出，同时让 default 收窄为无参重载
      throw new Error('rearrange-protocols 由 UI 模态驱动，不经过本步骤驱动');
    default:
      // 此处 kind 已收窄为 refresh | advance | clear-cache（无参数重载）
      executeAction(s, step.player, step.kind);
      break;
  }
}

/** 用可复现选择流推进并记录步骤。
 *  与 tests/fuzz/lib.ts 的 playRandomGameInner 保持同一判断顺序。 */
export function recordRandomSteps(s: GameState, r: () => number, max: number): Step[] {
  const steps: Step[] = [];
  for (let i = 0; i < max; i++) {
    if (s.winner !== null || s.phase !== 'turn') break;
    const top = s.pendingEffects[s.pendingEffects.length - 1];
    if (top?.prompt) {
      const step: Step = { t: 'answer', id: top.id, choice: randomAnswer(top.prompt, r) };
      steps.push(step);
      applyStep(s, step);
      continue;
    }
    const legal: LegalAction[] = getLegalActions(s, s.turnPlayer);
    if (legal.length === 0) break;
    const action = legal[Math.floor(r() * legal.length)];
    // getLegalActions 不产生这两类（重排由 UI 模态直接提交）——防御性跳过
    if (action.kind === 'effect-choice' || action.kind === 'rearrange-protocols') continue;
    const player = s.turnPlayer;
    let step: Step;
    if (action.kind === 'play') {
      step = {
        t: 'action',
        player,
        kind: 'play',
        args: { cardUid: action.cardUid!, faceUp: action.faceUp!, line: action.line!, target: action.target },
      };
    } else if (action.kind === 'compile') {
      step = { t: 'action', player, kind: 'compile', args: { line: action.line! } };
    } else if (action.kind === 'resolve-trigger') {
      step = { t: 'action', player, kind: 'resolve-trigger', args: { cardUid: action.cardUid! } };
    } else {
      // 收窄为 refresh | advance | clear-cache
      step = { t: 'action', player, kind: action.kind };
    }
    steps.push(step);
    applyStep(s, step);
  }
  return steps;
}
```

> **为什么不能写 `executeAction(s, step.player, step.kind, step.args as never)`**：`executeAction` 用重载声明（`game.ts:115-121`），TS 无法用联合类型的 `kind` 命中任何一条重载。这不是风格问题，是编译错误。上表的 `switch` 是必须的。

- [ ] **Step 3: 写 `tests/determinism/replay.test.ts`**

```ts
import { describe, it, expect, vi } from 'vitest';
import type { GameState, Line, PlayerId } from '../../src/core/models/types';
import { stateFingerprint } from '../../src/core/fingerprint';
import { rng, setupGen3Game } from '../fuzz/lib';
import { applyStep, recordRandomSteps, type Step } from './lib';

const SEED = 20260913;
const STEPS = 80;
/** 选择流种子：与引擎流分开（引擎流现由状态种子决定） */
const PICK_SEED = SEED ^ 0x5bf03635;

/** 基准：录制步骤 + 基准指纹 */
function baseline(): { steps: Step[]; fp: string } {
  const s = setupGen3Game(SEED);
  const steps = recordRandomSteps(s, rng(PICK_SEED), STEPS);
  return { steps, fp: stateFingerprint(s) };
}

/**
 * D3 专用：用**全新模块实例**的引擎函数施加步骤。
 * 不能复用 ./lib 的 applyStep —— 它绑定的是旧模块实例。
 */
function applyWith(
  mods: {
    game: typeof import('../../src/core/game');
    resolve: typeof import('../../src/core/effects/resolve');
  },
  s: GameState,
  step: Step,
): void {
  if (step.t === 'answer') {
    mods.resolve.answerEffect(s, step.id, step.choice);
    mods.resolve.runStack(s);
    return;
  }
  switch (step.kind) {
    case 'play': {
      const a = step.args as { cardUid: string; faceUp: boolean; line: Line; target?: PlayerId };
      mods.game.executeAction(s, step.player, 'play', a);
      break;
    }
    case 'compile':
      mods.game.executeAction(s, step.player, 'compile', step.args as { line: Line });
      break;
    case 'resolve-trigger':
      mods.game.executeAction(s, step.player, 'resolve-trigger', step.args as { cardUid: string });
      break;
    case 'effect-choice':
      mods.game.executeAction(s, step.player, 'effect-choice', step.args as { promptId: string; choice: string[] });
      break;
    case 'rearrange-protocols':
      throw new Error('unexpected rearrange-protocols step');
    default:
      mods.game.executeAction(s, step.player, step.kind);
      break;
  }
}

describe('确定性测试台 D1~D3（G0）', () => {
  it('基线可录制且步数 > 0', () => {
    const { steps, fp } = baseline();
    expect(steps.length).toBeGreaterThan(0);
    expect(fp).toMatch(/^[0-9a-f]{16}$/);
  });

  it('D1 重放一致：同种子 + 同步骤 → 同指纹', () => {
    const { steps, fp } = baseline();
    const s = setupGen3Game(SEED);
    for (const step of steps) applyStep(s, step);
    expect(stateFingerprint(s)).toBe(fp);
  });

  it('D2 存盘恢复一致：每步 JSON 往返后指纹不变', () => {
    const { steps, fp } = baseline();
    let s = setupGen3Game(SEED);
    for (const step of steps) {
      applyStep(s, step);
      s = JSON.parse(JSON.stringify(s)) as typeof s;
    }
    expect(stateFingerprint(s)).toBe(fp);
  });

  it('D3 跨模块实例一致：全新 import 跑同步骤 → 同指纹', async () => {
    const { steps, fp } = baseline();

    vi.resetModules();
    const create = await import('../../src/core/state/create');
    const game = await import('../../src/core/game');
    const resolve = await import('../../src/core/effects/resolve');
    const fpMod = await import('../../src/core/fingerprint');
    const data3 = await import('../../src/data/cards3');

    // 用全新模块实例重建同一开局（复制 setupGen3Game 的流程，但只走新实例）
    const s = create.createGame({ seed: String(SEED) });
    s.draftPool = [...data3.ALL_PROTOCOLS_3];
    const draftR = rng(SEED);
    let guard = 0;
    while (s.phase === 'draft' && guard++ < 100) {
      const avail = create.getDraftPool(s);
      if (avail.length === 0) break;
      create.performDraftPick(s, avail[Math.floor(draftR() * avail.length)].defId);
    }
    s.turnPlayer = draftR() < 0.5 ? 0 : 1;

    for (const step of steps) applyWith({ game, resolve }, s, step);
    expect(fpMod.stateFingerprint(s)).toBe(fp);
  });
});
```

> `rng` 来自 `tests/fuzz/lib.ts`，`vi.resetModules()` 之后它仍指向旧实例，但它本身是纯函数（不依赖引擎模块状态），所以不影响 D3 的结论。

- [ ] **Step 4: 跑测试**

Run: `npx vitest run tests/determinism/replay.test.ts`
Expected: PASS（4 项）。

若 **D3 失败**：说明 `src/core` 内仍有模块级可变状态。用 `git grep -n "^let \|^var " -- src/core` 找出候选，逐个判断是否影响状态生成。

- [ ] **Step 5: 全量回归**

Run: `npx vitest run`
Expected: 全绿。

- [ ] **Step 6: 提交**

```bash
git add tests/determinism/lib.ts tests/determinism/replay.test.ts tests/fuzz/lib.ts
git commit -m "test(G0): 确定性测试台 D1/D2/D3（重放 / 存盘恢复 / 跨模块实例）" -m "新增 tests/determinism/lib.ts 记录-重放步骤；D3 用 vi.resetModules() 模拟新进程，专抓模块级全局状态（uidCounter 就是被这条抓出来的）。fuzz 库 setupGen3Game 改为传显式 seed。"
```

---

## Task 8: D4 —— fuzz 全域确定性 + 移除 `Math.random` 补丁

**Files:**
- Modify: `tests/fuzz/lib.ts:106-122`（移除 `Math.random` 补丁）、`:114`（返回值加 `fingerprint`）
- Create: `tests/determinism/fuzz-determinism.test.ts`

**Interfaces:**
- Consumes: Task 5 的 `stateFingerprint`、Task 7 的 `setupGen3Game`
- Produces: `playRandomGame(seed, maxSteps, stats?)` 的返回类型扩展为 `{ steps: number; finished: boolean; fingerprint: string }`（**向后兼容**：只加字段）

**为什么这是 D4 的关键**：`tests/fuzz/lib.ts:107-111` 现在这样写：

> 注意：引擎内部（洗牌/随机揭示等）也用 Math.random，因此单局开始前**同时替换 Math.random** 为同种子的可复现流

这个补丁**就是不确定性存在的证据**。G0 完成后它必须变成多余的 —— 删掉它，然后要求"同种子两次独立重跑指纹相同"。删补丁这个动作本身就是最有力的验收。

- [ ] **Step 1: 移除补丁并扩展返回值**

`tests/fuzz/lib.ts` 的 `playRandomGame`（`:114-122`）改为：

```ts
/** 单局驱动：随机合法行动 / 随机应答，直至分出胜负或步数上限。
 *  G0 之后引擎随机已完全由状态种子决定，**不再需要替换 Math.random**：
 *  同一 seed 两次调用必然得到同一个 fingerprint。 */
export function playRandomGame(
  seed: number,
  maxSteps: number,
  stats?: GameStats,
): { steps: number; finished: boolean; fingerprint: string } {
  const s = setupGen3Game(seed);
  const r = rng(seed ^ 0x9e3779b9);
  const res = driveGame(s, r, seed, maxSteps, stats);
  return { ...res, fingerprint: stateFingerprint(s) };
}
```

同时在文件顶部 import 区加：

```ts
import { stateFingerprint } from '../../src/core/fingerprint';
```

并把 `playRandomGameInner`（`:130-171`）改成接收已建好的状态与选择流 —— 也就是把现在的 `playRandomGameInner(seed, maxSteps, stats)` 重命名为 `driveGame(s, r, seed, maxSteps, stats)`，删掉它内部的前两行：

```ts
  const r = rng(seed ^ 0x9e3779b9); // 选择流与引擎流分开（都已固定种子 → 整体可复现）
  const s = setupGen3Game(seed);
```

签名改为：

```ts
function driveGame(
  s: GameState,
  r: () => number,
  seed: number,
  maxSteps: number,
  stats?: GameStats,
): { steps: number; finished: boolean } {
```

函数体其余部分（循环、`collectField`、`checkInvariants`）保持不变。

- [ ] **Step 2: 跑既有 fuzz 测试确认没坏**

Run: `npx vitest run tests/fuzz`
Expected: PASS —— 150 局随机对局在**没有** `Math.random` 补丁的情况下仍然无异常。若失败，说明 `src/core` 里还有非确定性来源，回到 Task 4 的 `git grep -n "Math.random" -- src/core` 排查。

- [ ] **Step 3: 写 D4 测试**

创建 `tests/determinism/fuzz-determinism.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { playRandomGame } from '../fuzz/lib';
import { stateFingerprint } from '../../src/core/fingerprint';
import { setupGen3Game } from '../fuzz/lib';
import { rng } from '../fuzz/lib';
import { applyStep, recordRandomSteps } from './lib';

/** 独立重跑对比的种子数：每局要跑两遍，故取较小值 */
const REPEAT_SEEDS = 12;
/** 记录-重放对比的种子数：多跑一遍重放，成本较低 */
const REPLAY_SEEDS = 60;
const MAX_STEPS = 400;

describe('D4：全域确定性（全卡池 fuzz）', () => {
  it('同种子两次独立重跑指纹相同', () => {
    const bad: string[] = [];
    for (let seed = 1; seed <= REPEAT_SEEDS; seed++) {
      const a = playRandomGame(seed, MAX_STEPS);
      const b = playRandomGame(seed, MAX_STEPS);
      if (a.fingerprint !== b.fingerprint) bad.push(`seed=${seed}`);
    }
    expect(bad, `以下种子两次重跑不一致：${bad.join(', ')}`).toEqual([]);
  }, 300000);

  it('录制步骤后重放，指纹与原局相同', () => {
    const bad: string[] = [];
    for (let seed = 1; seed <= REPLAY_SEEDS; seed++) {
      const s = setupGen3Game(seed);
      const steps = recordRandomSteps(s, rng(seed ^ 0x5bf03635), MAX_STEPS);
      const fp = stateFingerprint(s);
      const s2 = setupGen3Game(seed);
      for (const step of steps) applyStep(s2, step);
      if (stateFingerprint(s2) !== fp) bad.push(`seed=${seed}`);
    }
    expect(bad, `以下种子重放后指纹不一致：${bad.join(', ')}`).toEqual([]);
  }, 300000);

  it('RNG 消耗次数随对局推进单调增长（防"随机被绕过"）', () => {
    const a = playRandomGame(7, MAX_STEPS);
    const b = playRandomGame(7, MAX_STEPS);
    expect(a.steps).toBe(b.steps);
  });
});
```

- [ ] **Step 4: 跑 D4**

Run: `npx vitest run tests/determinism/fuzz-determinism.test.ts`
Expected: PASS（3 项）。这是重负载测试，可能需要 1~3 分钟。

- [ ] **Step 5: 全量回归 + 验证门**

Run: `npx tsc --noEmit` → 0 错
Run: `npx vitest run` → 全绿
Run: `npx vite build` → 成功
Run: `npm run texts:check` → 差异 0 处

- [ ] **Step 6: 提交**

```bash
git add tests/fuzz/lib.ts tests/determinism/fuzz-determinism.test.ts
git commit -m "test(G0): D4 全域确定性 + 移除 fuzz 的 Math.random 补丁" -m "fuzz 库此前必须替换 Math.random 才能复现对局，该补丁现已多余——删除它本身即最有力的验收。playRandomGame 返回值新增 fingerprint。D4：12 个种子独立重跑对比 + 60 个种子录制-重放对比。"
```

---

## Task 9: 源码守卫 —— `src/core` 内禁随机与时钟

**Files:**
- Create: `tests/core-purity.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: 一条会在未来回归时立刻失败的守卫

**为什么需要**：G0 的成果全靠"core 里没有第二随机源"这一条。没有守卫，以后随手写一句 `Math.random()` 就会悄悄破坏联机 —— 而且症状是"偶发两边不一样"，极难查。

- [ ] **Step 1: 扩展守卫测试**

Task 4 已经创建了 `tests/core-purity.test.ts`（含 `walk` / `read` / `rel` 工具与「不得出现 Math.random」一条断言）。把它扩展为下面这个完整版本 —— **只补新断言，不要重复定义工具函数**：

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync, readdirSync, statSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { join } from 'node:path';

/**
 * G0 守卫：src/core 必须保持纯确定性。
 * 见 docs/2026-09-13-联机与多端-设计稿.md §2.2 / §2.8。
 */

const CORE_DIR = fileURLToPath(new URL('../src/core/', import.meta.url));

function walk(dir: string, out: string[] = []): string[] {
  for (const name of readdirSync(dir)) {
    const full = join(dir, name);
    if (statSync(full).isDirectory()) walk(full, out);
    else if (name.endsWith('.ts')) out.push(full);
  }
  return out;
}

const FILES = walk(CORE_DIR);

function read(file: string): string {
  return readFileSync(file).subarray(0, 4 * 1024 * 1024).toString('utf8');
}

/** 相对路径（报错时好读） */
function rel(file: string): string {
  return file.slice(CORE_DIR.length).replace(/\\/g, '/');
}

describe('src/core 纯确定性守卫（G0）', () => {
  it('扫描到足够的源文件（防路径写错导致空扫描假绿）', () => {
    expect(FILES.length).toBeGreaterThan(30);
  });

  it('不得出现 Math.random 调用', () => {
    // 只匹配"调用"（后跟左括号）：注释里提到 Math.random 不构成违规
    // （src/core/rng.ts 的文档注释里就写着这条铁律，宽松正则会把它误判为违规）
    const bad = FILES.filter((f) => /\bMath\s*\.\s*random\s*\(/.test(read(f))).map(rel);
    expect(bad, `以下 core 文件调用了 Math.random（会破坏联机确定性）：${bad.join(', ')}`).toEqual([]);
  });

  it('不得出现时钟调用（仅 trace.ts 豁免 Date.now）', () => {
    const bad: string[] = [];
    for (const f of FILES) {
      const src = read(f);
      // 一律只匹配"调用"形式：注释里提到 Date.now 不构成违规
      if (rel(f) !== 'trace.ts' && /\bDate\s*\.\s*now\s*\(|\bnew\s+Date\s*\(/.test(src)) bad.push(rel(f));
      if (/\bperformance\s*\.\s*now\s*\(/.test(src)) bad.push(rel(f));
    }
    expect(bad, `以下 core 文件使用了时钟（诊断请走 trace.ts）：${bad.join(', ')}`).toEqual([]);
  });

  it('不得自行取随机（种子由平台层提供）', () => {
    const bad = FILES.filter((f) => /getRandomValues\s*\(|randomUUID\s*\(/.test(read(f))).map(rel);
    expect(bad, `以下 core 文件自行取随机（应由 src/ui/match-seed.ts 提供种子）：${bad.join(', ')}`).toEqual([]);
  });

  it('不得引用 UI 层或 DOM（core 保持 DOM-free）', () => {
    const bad: string[] = [];
    for (const f of FILES) {
      const src = read(f);
      if (/from\s+['"][^'"]*\/ui\//.test(src)) bad.push(rel(f));
      // 只匹配真实成员调用，避免命中注释（如 render 相关说明里的字面量）
      if (/\bdocument\s*\.\s*\w+\s*\(|\bwindow\s*\.\s*\w+\s*\(/.test(src)) bad.push(rel(f));
    }
    expect(bad, `以下 core 文件引用了 UI/DOM：${bad.join(', ')}`).toEqual([]);
  });
});
```

- [ ] **Step 2: 跑测试**

Run: `npx vitest run tests/core-purity.test.ts`
Expected: PASS（5 项）。

若「不得引用 UI/DOM」失败：说明 core 里原本就有 `window`/`document` 引用（例如注释里出现）。**先看是真引用还是注释**；若只是注释文字，把断言改精确（如 `/\bwindow\.(addEventListener|setTimeout)/` 之类）。不要为了过测试而放宽到失去意义。

- [ ] **Step 3: 提交**

```bash
git add tests/core-purity.test.ts
git commit -m "test(G0): 新增 src/core 纯确定性守卫" -m "禁 Math.random / Date.now / crypto 随机 / DOM 与 UI 引用（trace.ts 的 Date.now 豁免）。防止以后随手写一句 Math.random 悄悄破坏联机确定性。"
```

---

## Task 10: 全量验证 + 文档收口

**Files:**
- Modify: `docs/2026-09-13-联机与多端-设计稿.md`（§2.3 命名对齐、§2.7 D4 刻度说明、§2.8 勾选）
- Modify: `docs/2026-09-13-G0-确定性内核-实现计划.md`（勾选本计划中已完成的任务）

- [ ] **Step 1: 走完验收门**

逐条执行并记录结果：

```bash
npx tsc --noEmit
npx vitest run
npx vite build
npm run texts:check
```

Expected：
- tsc 0 错
- vitest 全绿，测试数 ≥ 842 + 新增（Task 1:9、Task 2:5、Task 4:3、Task 5:8、Task 6:6、Task 7:4、Task 8:3、Task 9:5 ≈ 新增 43 项）
- vite build 成功
- texts:check 差异 0 处

- [ ] **Step 2: 核对 §2.2 的五处不确定性全部消除**

```bash
git grep -n "Math.random" -- src/core
git grep -n "uidCounter" -- src/core
git grep -n "Date.now" -- src/core
```

Expected：
- `Math.random` → 无输出
- `uidCounter` → 无输出
- `Date.now` → 只有 `trace.ts`

- [ ] **Step 3: 对齐设计稿 §2.3 的命名**

设计稿 §2.3 写的是 `rngNext(r: RngState)` / `randInt(s, bound)` / `randPick(s, arr)` / `shuffleWith(s, arr)`。实际实现把底层命名为 `nextValue` / `nextInt` / `pickFrom` / `shuffleArr`（作用于 `RngState`），`GameState` 包装沿用设计稿名字。

把 §2.3 代码块中的 `rngNext(r: RngState)` 一行改为：

```ts
/** 原地推进 r.n，返回 [0,1) 的确定性数值（实现名：nextValue） */
export function nextValue(r: RngState): number;
```

并在该代码块后补一句：

> 实现说明：底层四个函数（`nextValue`/`nextInt`/`pickFrom`/`shuffleArr`）作用于 `RngState`；`randInt`/`randPick`/`shuffleWith` 是它们的 `GameState` 包装，引擎调用点统一用包装版。

- [ ] **Step 4: 在 §2.7 记录 D4 的实际刻度**

在 §2.7 的表格后补：

> **D4 实现刻度（2026-09-13）**：独立重跑对比取 12 个种子、录制-重放对比取 60 个种子（每局至多 400 步）。原稿的"200 局"由两条共同达成：D4 的 60 局重放 + 既有 `tests/fuzz/gen3-random-games.test.ts` 的 150 局（该测试在 G0 后**不再需要替换 `Math.random`**，本身即成为广度上的确定性回归）。把 D4 提高到 200 局独立重跑会让这个重负载测试耗时翻倍，收益不抵成本。

- [ ] **Step 5: 勾选 §2.8**

把 §2.8 的验收清单改为已完成（把 `- [ ]` 改为 `- [x]`），并在末尾加一行：

```
- [x] `src/core` 源码守卫测试就位（禁 Math.random / Date.now / crypto 随机 / DOM 引用）
- [x] 兜底种子跨重启不同：`setSeedNonce` 由平台层注入（人工验收：重启应用后连开两局，开局手牌不同）
```

- [ ] **Step 6: 清理过时注释与文档引用**

G0 过程中以下三处在实现后变得不准确（均为 Task 3/4 评审记下的 Minor），逐处修正：

1. `src/core/effects/cards/love.ts` 中 `takeRandom` 的说明写着「真随机 Math.random，owner 变更到效果属主」—— 该路径已改走 `randInt`（`resolve.ts` 的 `takeRandom` 分支）。改为「随机取自状态种子（`randInt`），owner 变更到效果属主」。
2. `src/core/effects/cards/lust.ts` 中「真随机（love-3/运气3 先例）」的措辞同上修正（love-3 的先例已不再是 `Math.random`）。
3. `docs/2代效果开发手册.md`（约第 350、499 行）与 `docs/批1规格-幸运明镜和平混沌明晰.md`（约第 50、527、550、558 行）里对**已删除的 `shuffle`** 的引用 → 改为 `shuffleWith(s, arr)`，或注明该 API 已并入 `src/core/rng.ts`。
4. `docs/2026-09-13-联机与多端-设计稿.md` §2.2 表格里写的 `shuffle(s, arr)` → 改为 `shuffleWith(s, arr)`（§2.3 已经是后者）。

Run: `git grep -n "Math\.random" -- src/core`
Expected: 只剩 `rng.ts` 的铁律注释一处（调用数为 0）。

- [ ] **Step 7: 提交**

```bash
git add src tests docs
git commit -m "chore(G0): 清理过时注释与文档引用" -m "love.ts/lust.ts 的「真随机 Math.random」注释已与实现不符（takeRandom 改走 randInt）；2代效果开发手册与批1规格里对已删除 shuffle 的引用改为 shuffleWith；设计稿 §2.2 的 shuffle(s,arr) 改为 shuffleWith(s,arr)。" -m "均为 Task 3/4 评审记下的 Minor，集中在收口阶段一并处理。"
```

---

## 验收对照（G0 完成的定义）

对应设计稿 §2.8：

| 验收项 | 由哪个任务保证 |
|---|---|
| 五处不确定性全部消除，`src/core` 内 `Math.random` 零命中 | Task 3、4、9 |
| D1 重放一致 | Task 7 |
| D2 存盘恢复一致 | Task 7 |
| D3 跨模块实例一致（抓模块级全局） | Task 7 |
| D4 全域确定性（全卡池） | Task 8 |
| `tsc` 0 错、`vitest` 全绿、现有 842 项不减少 | 每个任务的收尾 + Task 10 |
| `stateFingerprint` 稳定且能区分状态 | Task 5 |
| 现有热座玩法行为零变化 | Task 2/3/4/6 的全量回归 + Task 6 Step 8 人工验收 |

**G0 完成后不做的事**（属于后续阶段，不要顺手做）：不实现 `forfeit` 操作（G6）、不建 `MatchDriver`（G4）、不建 `MatchFile`（G3）、不动 `src/ui` 的远程对战页（G2）。
