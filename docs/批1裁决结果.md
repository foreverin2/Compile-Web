# 批1 裁决结果（用户 2026-09-05 拍板）

> 对应 `docs/批1裁决问题清单.md` 17 条；全部按推荐采纳，两处用户自定义见下。实现照此执行。

## 幸运 luck
- **[Q1]** 命中多张时**自选 1 张揭示**，可打出它；未揭示命中卡留手牌。
- **[Q2]** 打出被揭示卡：**正面须匹配目标线协议、反面任意线**（常规落线）。
- **[Q3]** 「宣告1个协议」范围 = **仅本局双方已选 6 套**（用户自定义选 B；非全池）。
- **[Q4]** 命中后「删除1张牌」= 任意 1 张**未被覆盖**的场卡（双方皆可选）。FAQ 运气3 勘误原文截断未补全——按 A 执行并留待卡面/官方核对。
- **[Q5]** luck-4 弃**自己**牌库顶。
- **[Q6]** 阈值统一按**现时值**（cardPointValue：正面=牌面、被盖反面=2、暗2 类修正线=4）。

## 明镜 mirror
- **[Q7]** mirror-1 复制目标 = 对手场上**未被覆盖且正面朝上**的卡。
- **[Q8]** 复制执行：「你」= 明镜1 持有者（执行者）；「此牌/此列」自指 = 被复制卡。
- **[Q9]** mirror-2 取 **txt：两个堆叠整体换线**（docx 异文弃用）。
- **[Q10]** mirror-3「同一链路」= 第一句所翻自己的牌所在线。
- **[Q11]** mirror-4/peace-4 底槽反应**仅未覆盖顶卡触发**（被盖失效）；war-0/2/3 等批 3 底部槽反应沿用；corruption-2 反应在顶部槽（被盖仍生效）不受影响。

## 和平 peace
- **[Q12]** peace-1 双方弃所有手牌：**效果属主先弃、对手后弃**。

## 混乱 chaos
- **[Q13]** chaos-0 每行翻 1 张**被覆盖且反面**的卡为正面（静默翻正，不连锁中指令）。
- **[Q14]** chaos-1 重新排列 = **任意顺序完整重排**（双方各一次、必须修改双方、终态≠初态；需新 op/UI）。
- **[Q15]** chaos-3 从手牌打出本卡时**豁免协议匹配**可正面打任意线；其它被动限制（psychic-1/plague-0/metal-2）照常。corruption-0 沿用。

## 明晰 clarity
- **[Q16]** clarity-0 值修正只作用于**拥有者自己**该线总值（自己该线每 1 张卡 +1）。
- **[Q17]** 用户自定义「自选一张揭示」→ 从牌库（揭示内容）**自选 1 张阈值 1 的卡**抽入手；末句「打出1张阈值为1的卡牌」= 打出**刚抽到的那张**（推荐 A；朝向自选：正面匹配协议线/反面任意线）。

## 参考项目（用户指示）
2代 效果实现参考 `E:\studyE\AI大模型相关资料\自己的项目\compile\compile-apo`（React 版 Compile，JSON DSL 卡效果 + logic/effects/actions 各 executor），以减少语义错误与 bug；有歧义时其 custom_protocols/*.json 与 txt/FAQ 交叉核对。
