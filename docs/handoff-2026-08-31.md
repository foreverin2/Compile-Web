# Compile（译世界）网页版 — 接力开发交接文档

> 生成时间：2026-08-31（**更新至 2026-09-03**）。本文件是**无记忆/无上下文环境下的唯一接力入口**——后续模型先读本文件，再读《全局记忆》与 SDD 台账，即可继续开发。
> **当前里程碑：15/15 套协议全部有真实效果 + 10 套新协议专属特效（FX-1..6）+ 8 项用户特效修改 + 新 10 协议「已编译」常驻特效（三轮评审 Approved）+ 已编译特效/开发者模式多批微调（2026-09-03）+ **2代（MN02 英文版）资源+数据入库（spec/quality 双 Approved，未接玩法，未推送）**——测试 523/523 全绿（57 文件），build clean，已推送 origin（main @ `e57b1b4`，同步 0，MN02 入库改动未推送）。**
> **用户流程偏好（重要）**：视觉/特效类改动的最终观感由用户本人在 5173 确认；未经用户明确要求，**不要用无头浏览器等重工具实测视觉效果**——实现侧用几何推演 + build + 逻辑审查保证正确性即可。
> **规则权威**：官方 FAQ 文件 `E:\studyE\compile\compile1\compileFAQ文本.txt`（本阶段大量裁决来源于此，见 §5）。

---

## 0. 项目概览

- **游戏**：桌游《译世界》(Compile) 网页版。纯前端：HTML5 + Vite + TypeScript（strict），热座双人。无后端、无新依赖。
- **仓库**：origin = `https://github.com/foreverin2/Compile-Web.git`（私有；**已推送 main @ `e57b1b4`，同步 0**；MN02 入库改动未推送）。
- **本地路径**：`E:\studyE\AI大模型相关资料\自己的项目\compile`；开发服务器 `npm run dev`（http://localhost:5173）。
- **协议数据**：15 套协议 × 6 张指令卡 = 90 张卡（defId 唯一，如 `death-0`、`apathy-5`）。**每张卡全游戏只有一张**；卡可能易主。
- **当前状态**：**15/15 协议有效果**（§3 列全部卡效果语义）+ **10 套新协议专属特效**（§3 特效小节）；开发者模式（Ctrl+Shift+P → 密码 `上上下下左右左右BABA` 或 `ssxxzyzybaba` → `get 牌名`/`clean`/`compile <协议>`）。
- **特效**：5 套旧协议（fire/light/darkness/water/life）有专属编译环/触发特效 + **10 套新协议专属特效已实现**（§3 特效小节，FX-1..6 全部 Approved）+ **10 套新协议「已编译」常驻特效已实现**（§3 已编译小节，2026-09-03，三轮评审 Approved）。观感由用户 5173 确认后可微调（时长/颜色集中在 CSS 类与 JS 常量）。

## 1. 环境 Runbook（每条派发都必须带上——违反即坏）

- 不要修改 `vite.config.ts` 的 `test.pool`（必须保持 `'threads'`）。
- 禁止 `npm install` / `npm ci`（node_modules 有本地 vite 补丁；确实要装时用 `--cache node_modules/.npm-cache`）。
- 测试：`npm test` 或 `npx vitest run tests/<file>.test.ts`。测试 import 规则：`tests/<子目录>/` 用 `'../../src/...'`（两层）；`tests/helpers.ts` 用 `'../src/...'`。
- TypeScript strict，禁止 `any`。每任务提交一次；**推送需用户明确指示**。
- 推送时若 git 代理（`127.0.0.1:7890`）失效，用直连绕过：
  `git -c http.proxy= -c https.proxy= -c "http.https://github.com.proxy=" -c "https.https://github.com.proxy=" push origin main`

## 2. 架构（先读这些文件）

| 层 | 文件 | 说明 |
|---|---|---|
| 类型 | `src/core/models/types.ts` | Card(secret)/GameState(compileBlocked/pendingCompile)/PendingEffect(topCommand)/PendingLanding(belowUid)/Op(含 draw.player/fromOpponentDeck、playTopDeck.player/belowUid、rearrangeProtocols.player、give、takeRandom、discardMany)/ChoiceRequest(chooser)/TriggerKind(含 before-flip/before-compile/after-draw/after-discard/after-delete/after-clear-cache)/TriggerDef(top)/CardEffects(valueModifier) |
| 状态创建 | `src/core/state/create.ts` | createGame、草案（DRAFT_ORDER）、stackValue（valueModifier 集成：修正卡必须正面、被盖正面仍生效）、lineTopCommandActive、cardPointValue、getLineValue |
| 行动门面 | `src/core/game.ts` | getLegalActions（**被动限制接线**：psychic-1 禁正面、plague-0 禁此列、metal-2 禁此列反面、spirit-0 跳过 check-cache、spirit-1 任意线）/executeAction（compile/refresh 分支处理挂起；resolve-trigger 传 topCommand）；cacheClearGen（discardMany + after-clear-cache） |
| 打出 | `src/core/actions/base.ts` | playCard（**执行层守卫**与 getLegalActions 一致；效果授予的 playFromHand op 不受限——卡牌文本优先）、isPlayableFaceUp（spirit-1 放宽） |
| 效果引擎 | `src/core/effects/resolve.ts` | runStack（LIFO + FIFO 落地 + pendingCompile 消费 + pop-by-id）、executeOp（全部 op；discard 按被弃卡 owner；flip before-flip 钩子 + 被盖翻正不连锁 FAQ 127；playTopDeck 不洗牌 FAQ 142）、completePlay（belowUid 插下方）、sourceValid（topCommand 跳过未覆盖） |
| 候选/上下文 | `src/core/effects/context.ts` | listCandidates、findCard、isUncovered、deckTopAvailable（只查牌库——playTopDeck 不洗牌） |
| 触发 | `src/core/effects/triggers.ts` | collectTriggers（end/start 整堆遍历，被盖卡仅 top 标志触发）、fireReactive（after-* 即时连锁，topCommand）、resolveTrigger（opts.topCommand）、collectTriggerFor |
| 限制规则 | `src/core/rules/restrictions.ts` | 被动限制纯查询（spirit-1/psychic-1/plague-0/metal-2/spirit-0/apathy-2/metal-1 compileBlocked） |
| 规则 | `src/core/rules/{compile,compile-body,control}.ts` | executeCompile/Unchecked（speed-2 before-compile 前置）、compile-body（编译本体，不触发文本）、控制权 |
| 引擎 | `src/core/engine/{turn,deck}.ts` | advanceStep（turnCount、compileBlocked 清除）、drawCards（洗弃牌堆、secret 解禁、after-draw 连锁） |
| 卡效果 | `src/core/effects/cards/{fire,light,darkness,water,life,death,spirit,gravity,psychic,plague,metal,speed,love,hate,apathy}.ts` | **15 套协议全部注册** |
| 事件总线 | `src/core/events/bus.ts` | gameBus.subscribe（FX 层消费） |
| 卡牌数据 | `src/data/cards.ts` | 15 协议定义 + 90 张卡中文文本（权威：`compile1文本.txt`） |
| 渲染/特效 | `src/ui/render.ts`、`src/ui/effects/index.ts`、`src/ui/styles.css` | 编译环（新协议走通用骨架）、基础行为特效 |
| 开发者模式 | `src/ui/devmode.ts` | Ctrl+Shift+P → 密码 → get/clean/compile 指令 |
| 诊断 | `src/ui/diag.ts` | 全量 console 捕获 + 导出 txt |

## 3. 15/15 协议效果一览（本轮 10 套的语义要点）

> **10 套新协议专属特效已实现（2026-08-31 用户指定，FX-1..FX-6 全部 Approved）**：
> - 死：删除时镰刀划过+深紫边框光+骷髅头（下排牙齿动）2s；恨：血红手指抓握+血滩+血红边框光
> - 重力：打牌堆顶/平移时品红牌库框光+终点黑洞+射线（由粗变细再变粗）1.5s+卡框品红到终点后 1s
> - 速度：平移/抽牌时灰白卡框光+飓风沿路径移动 1.5s
> - 念能：弃牌紫粉粒子环绕 1s 汇聚；念能1 持续对手三线粒子闪烁（暗2 模式）
> - 瘟疫：弃牌深绿光芒+浓雾覆盖；瘟疫0 持续被限制方该线深绿浓雾循环
> - 爱：抽牌（含刷新）牌库粉红光+卡背爱心跳动 2s；给牌/收牌爱心+飞行到对方手牌末尾；揭示幽灵粉红爱心（存在期间持续）
> - 金属：金属0 对方能量槽金属光泽边框；金属1 对手三链边框金属光泽（一次性 3s）；金属2 被限制方链路铁板+斜光扫过；金属6 手牌 man.png 每 2s 渐现渐隐
> - 冷漠：翻转灰雾覆盖+灰光 1s；冷漠0 链路灰雾循环；冷漠2 该列双方卡灰色滤镜（放大查看器不受影响）
> - 灵魂：灵魂0 手牌区紫/紫粉光+check-cache 20 条亮紫锁链（起点终点分段唯一、倾斜）；灵魂1 手牌边框光芒+四角紫色护边
> - **特效实现要点**：一次性附加特效在 `src/ui/effects/index.ts` 按事件 triggerProtocol 叠加（前置段→基础特效延后→收尾，事件时捕获 rect 经 buildFxCardAt 重建）；持续型特效用暗2 黑烟同款注册表（`render.ts` syncMetalPlates/syncMetal0Glows/syncMetal6Mans/syncPsychicParticles/syncPlagueMists/syncApathyMists/syncSpirit0Glows/syncSpirit1Cards + prevStep 锁链驱动），body 级 fixed 层跨重渲染不重置；resetUiState 清全部注册表；CSS 类 `.fx-<协议>-*` 加 styles.css 末尾（时长/颜色集中便于 5173 微调）。
>
> **新 10 协议「已编译」常驻特效（2026-09-03，`docs/superpowers/plans/` 无计划文档——直接在 render.ts 新增区块实现；commit `9d80372`；第二批微调 commit `312b18f`）**：
> - **实现位置**：`render.ts` 注释 `2026-09-03：新 10 协议「已编译」常驻特效` 起的区块（`appendNewCompiledFx` 分发 + 每协议 builder + `scheduleCompiledLoop` 周期驱动器 + `fxTimer/clearCompiledFxTimers` 调度注册表）；`styles.css` 末尾「新 10 协议已编译特效」整段（呼吸边框/护边/各协议元素）。
> - **两条硬约束（用户明确，勿改）**：① 动画属持续性——全部挂在 body 级持久层（compiledFx，跨重渲染从不 detach/重建、只被 syncCompiledFxLayers 重定位）→ 步骤切换不重置动画进度；② 特效跟随协议位置（层按 holder 矩形重定位 + main.ts scroll/resize rAF），不固定在页面。**切勿把新特效挂回每次重建的 .protocol DOM 树**。
> - **间隔规则**：周期大特效（灵魂锁链伸收/重力坍缩/念能爆发/恨意血涌/冷漠故障）触发时间随机，但「特效结束→下次开始」≥10s（`COMPILED_MIN_GAP_MS`=10000 + 0..16000 抖动；`scheduleCompiledLoop` 在 burst 的 done() 之后才排下一次=从结束起计时）。
> - 各协议内容：死=深紫↔亮紫边框闪烁 + 偶尔骷髅（💀）渐现渐隐 + 缕缕黑烟；灵魂=亮紫↔紫粉边框 + 四角紫护边 + 锁链周期伸向中心后缩回（复用 CHAIN_LINK_* 椭圆环链）；重力=品红↔亮紫边框 + 中心黑洞由小到大→6-8 条射线射向隐形范围圆（R=min×0.46）→由快变慢回收且黑洞放大→快速坍缩→几缕随机紫曲线（灰尘）；念能=暗紫粉↔亮紫边框 + 边框周围 14 颗小粒子微闪（爆发期间暂停淡出）+ 周期 16 粒子定住→绕中心旋转→汇聚→蓄力→烟花；瘟疫=深绿↔亮绿灰边框 + 四角深绿护边 + 随机形状深绿浓雾循环；金属=金属光泽环（mask 渐变+扫光）+ 四角金属护边 + 卡面 30% 铁板 + 偶发斜光扫过 + 周期伸出/收回金属刺；速度=灰白↔灰边框 + 四角灰白护边 + 周期 2-3 个龙卷风（中心由小到大→蛇形走位由慢到快→渐隐，带风拖尾 rAF）；爱=粉↔粉红边框 + 四角粉红护边 + 爱心周期由小变大飘散（--hs 尺寸 / --fx-ex/ey 飞向）；恨=血红↔深红边框 + 偶发渗血 + 周期 10 指贴边框（上下2+左右3，±5% 抖动）缓慢向中心收缩→突停→血涌覆盖整卡→渐隐；冷漠=亮灰↔暗灰边框 + 卡内偶发马赛克团 + 周期整卡故障风暴（卡面克隆 .compiled-apathy-face 为载体，P2 rot-180 由 positionCompiledFxLayer 同步；apathy-glitch-a/b/c 三套「平台+ε 瞬跳」滤镜模板随机选+随机时长/负延迟）。
> - **清理闭环**：compiledFxTimers 注册表（key=defId→Set<timeout>）；未编译分支与 resetUiState 移除层前统一 clearCompiledFxTimers；所有 burst 每步守卫 layer.isConnected（层没了序列自然终止，不再排下一次）。
> - **可调参数**：CSS styles.css 末尾各 `.compiled-<协议>-*` 块（呼吸双色/时长/尺寸）；JS builder 顶部常量（COMPILED_MIN_GAP_MS、重力 dHole/R、念能 N/R0、恨 EDGE/scaleTo、速度 TW/TH/dur、爱 --hs 等）。
> - **第二批微调（2026-09-03，commit `312b18f`，用户 8 项）**：① devmode `Compile` 指令加协议模糊预览（`compile` 前缀分流，defId/中文名候选 + 本局场上提示，点击=强制编译，空参数列全部；新增 `searchProtocols` + 6 测试）；② 死边框 深紫↔黑 柔和交替（3.8s 不闪）+ 骷髅 3 只高频（~1.4-3.1s 一只）+ 偶尔死神镰刀（6-12s）；③ 灵魂锁链 8-10 根、间隔「结束→下次 ≥5s」（`scheduleCompiledLoop` 增可选 minGapMs/jitterMs，灵魂覆盖全局 ≥10s——勿把全局 10s 改掉）、边框 浅青蓝 #9fe8ff↔深靛蓝 #242f86；④ 金属刺完全伸出协议边框外（-24px 贴框外、尖端朝外）；⑤ 重力射线终点圆 R=0.8·min（≈1.74×，终点出协议框）；⑥ 爱每次 2-3 颗爱心；⑦ 速度龙卷风移动距离 ≈2×（吹出协议外）；⑧ 四角护边补齐：无护边的 死/重力/念能/恨/冷漠 全加——护边几何重构为通用 `.compiled-l-corner` 骨架 + 各协议 `--cc/--ccg` 变量配色（金属保持独立金属渐变 clip-path，代码侧内联建节点不走通用骨架）。
> - **第三批微调（2026-09-03，commit `048c543`，评审通过）**：① **light（光）已编译新增 A 日芒射线环绕（16 根金光射线中心向框外辐射、整层 26s 缓转+柔和呼吸）+ D 边框内侧暖色流光带（inset 3px+2px mask 环、金橙白 7s 流动）+ E 偶发星芒闪耀（clip-path 四芒星随机 pop 淡出，fxTimer('light') 调度）**——用户从方案中选定 A+D+E；② **灵魂协议锁链改深靛蓝**（stroke #b26bff → #2f3dbb、辉光 rgba(96,116,240,…)；两处：已编译锁链 .compiled-spirit-chain 与 check-cache 手牌锁链 .fx-spirit-chains-svg；gravity 紫尘 pick 中的 #b26bff 不是锁链勿动）；③ **速度龙卷风粒子化重做**（旧 4 band+core 观感像棍子+平面图形）：新共享模块 `src/ui/fx-tornado.ts` `buildTornadoFx()`——30 粒光点按高度锥形分布（顶部云帽宽、向下收窄触地尖），每环绕自身高度竖直中轴自转（负 delay 错相）+ 粒子向轴/母线向上涌动 + 明暗闪烁 → 绕竖直中轴旋转汇聚上升的漩涡；effects/index.ts 与 render.ts compiled speed 两处调用点均委托该构建器（评审备注 A 已修：环心恒在中轴线上不偏心）；（后补 `4a18d4b`）龙卷风再追加 **3 条弹簧式线条**（fx-tornado.ts `buildSpringLine` + styles.css `.fx-spring-line`：相位 120° 均布的递减振幅螺旋细线 SVG polyline，顶到底 4 圈，r(y) 与粒子锥形一致 → 顶部线圈宽、向下逐圈收小；线挂粒子下作骨架层）。

- **死 death**：d0 另两列各删1（FAQ 131 逐行）；d1 顶 start 可选抽→删另1→删自己（top；**无目标时整条不执行**——用户拍板）；d2 选列删所有 cardPointValue∈{1,2}（含被盖）；d3 删1反面；d4 删1张0/1分；d5 弃1
- **灵魂 spirit**：s0 中刷新（**含消耗控制组件** FAQ 161 + 抽1）+ 底跳过 check-cache（A2）；s1 顶任意列打（A2）+ 中抽2 + 底 start 弃1或翻自己；s2 可选翻1；s3 顶 after-draw 可平移自己（top，含被盖 allowCovered）；s4 双选线交换协议；s5 弃1
- **重力 gravity**：g0 此列每2张（**双方合计 floor/2**，参考实现确认）打牌堆顶到自己下方（belowUid）；g1 抽2+平移进/出此列；g2 翻1+平移进此列（FAQ 137 被盖也移）；g4 反面牌移进此列（已在列跳过）；g5 弃1；g6 对手此列打牌堆顶（player=opp）
- **念能 psychic**：p0 抽2+对手弃2（chooser=opp，discardMany）+揭示手牌（Case B）；p1 顶对手只能反面打（A2）+底 start 翻自己；p2 对手弃2+重排对手协议；p3 对手弃1+平移对手牌；p4 底 end 可选回手对手牌→翻自己；p5 弃1
- **瘟疫 plague**：p0 对手弃1+底禁此列打（A2）；p1 顶 after-discard 你抽1（含缓存弃牌）+中对手弃1；p2 弃1+张、对手弃N+1（**尽力而为**）；p3 翻所有其他未盖正面（快照逐张）；p4 底 end 对手删自己反面牌+可选翻自己（整段 fizzle）；p5 弃1
- **金属 metal**：m0 顶对手此列总分-2（opponent-line valueModifier）+翻1；m1 抽2+对手下回合不能编译（compileBlocked）；m2 顶禁此列反面打（A2）；m3 抽1+删≥8张另一列全部；m5 弃1；m6 顶被盖或翻转前删自己（before-covered+before-flip）
- **速度 speed**：s0 效果打出1张（正/反面选线，**绕过 A2 限制**）；s1 顶 after-clear-cache 抽1+中抽2；s2 顶 before-compile 编译前平移自己（A1 pendingCompile 驱动）；s3 中平移另1张自己的+底 end 可选平移自己→翻自己（被盖跳过）；s4 平移对手反面牌；s5 弃1
- **爱 love**：l1 抽对手牌堆顶（**对手牌库空洗对手弃牌堆**）+底 end 可选给1张手牌→抽2；l2 对手抽1+刷新（含控制组件）；l3 随机拿1张（takeRandom 真随机，对手空 take fizzle **不影响 give**）+给1张；l4 揭示自己手牌（Case A）+翻1（每句独立）；l5 弃1；l6 对手抽2
- **恨 hate**：h0 删1；h1 弃3（尽力）+删1+再删1（重新列候选）；h2 删双方分值最高（**仅正面卡** FAQ 169；并列玩家选；**自己是最高删自己→第二句不触发** FAQ 172）；h3 顶 after-delete 抽1（不含编译删除）；h4 底 before-covered 删此列最低分被盖卡；h5 弃1
- **冷漠 apathy**：a0 顶此列每张反面牌给总分+1（own-stack，双方含被盖）；a1 翻此列所有其他正面（含被盖）；a2 顶无效化此列中指令（A2）+底被盖前翻自己；a3 翻1张对手正面；a4 可选翻自己被盖正面；a5 弃1

## 4. 引擎能力清单（新机制，开发/修 bug 必读）

- **Op**：discard/discardMany（批量，after-discard 一次）/delete/return/flip/shift/playTopDeck(player/belowUid)/playFromHand/draw(player/fromOpponentDeck)/reveal/rearrangeProtocols(player)/give/takeRandom。playTopDeck **不洗弃牌堆**（FAQ 142）。
- **触发**：before-covered / before-flip（flip op 前置，metal-6）/ before-compile（executeCompileUnchecked，pendingCompile 机制）/ after-draw / after-discard（弃牌者对手）/ after-delete（被删卡持有者，**不含编译删除**）/ after-clear-cache / end / start。after-* 由 fireReactive 即时 push（topCommand 跳过未覆盖检查）。
- **被动限制**（restrictions.ts 纯查询 + getLegalActions/playCard 双守卫）：spirit-1 任意线、psychic-1 对手只能反面、plague-0 禁此列、metal-2 禁此列反面、spirit-0 跳过缓存、apathy-2 无效化线中指令、metal-1 compileBlocked（被禁玩家回合结束清除）。
- **其他**：chooser（选择权）、top 标志（被盖仍生效）、cardPointValue（正面=牌面/反面=2或暗2线4/牌库2）、secret 卡（进手牌即解禁）、fizzle（空候选自动续接）、sourceValid、**尽力而为弃牌约定**（弃 N 张不足弃全部剩余，空跳过，后续指令照常——用户拍板全局）。

## 5. 官方 FAQ 关键裁决（本阶段依据，后续协议/勘误必查 `compileFAQ文本.txt`）

- FAQ 39/40：卡牌文本=效果非费用；**每句独立**处理；无法完成不影响其他句（若含"如果"条件则无该效果）。
- FAQ 94：多张弃牌=单次动作（一次性弃完，之后触发一次）。
- FAQ 98/99：顶命令被盖仍生效（含 death-1 删自己、life-0 end 删自己）。
- FAQ 127：被覆盖卡翻正不触发中指令。
- FAQ 139（2024-10 勘误）：**生0 无底部指令**，改为顶「结束：若此卡被覆盖则移除此卡」（已按此改 life-0）。
- FAQ 142/166：**从牌堆顶打出不强制洗牌**（已改 playTopDeck）。
- FAQ 152/153：金属6 被盖/翻转前删自己（翻面指令被消耗）。
- FAQ 159：速度2 编译时移走自己，不改变编译结果。
- FAQ 161：灵力0 刷新=常规刷新含消耗控制组件。
- FAQ 169/172：恨2 仅正面卡、自己是最高删自己→第二句不触发。

## 6. 已知待办/遗留

- **特效观感微调**：10 套新协议专属特效已实现（§3 特效小节），参数集中在 CSS 类与 JS 常量（`HATE_BLOOD_LINGER_MS`、`LOVE_GLOW_MS`、`GRAVITY_HOLE_SIZE`、`CHAIN_LINK_*`、`APATHY_MOSAIC_*` 等）——用户 5173 确认后可微调。已记录 FX Minors（水回手重叠、生命翻转 alpha、speed shroud 等）见台账。
- **候选过滤规则（用户拍板，勿改）**：`listCandidates` **排除全部结算中源卡**（非仅栈顶）——gravity-2 翻 life-1 → life-1 连锁效果无目标 fizzle 是预期行为（用户 2026-09-02 决定 revert 栈顶排除方案）。**未来勿再改动此规则**。
- **协议重排 UI 未实现**（water-2/spirit-4 用 rearrangeProtocols op 已可用；控制组件重置后的协议重排 UI 缺口——spirit-0/love-2 刷新时若持控制组件应可重排）。
- **已记录 Minor（deferred）**：见 SDD 台账各轮（如 import 环维护性——speed→base→resolve；reactive 触发 optional 字段无读取方；各协议边角测试缺口；陈旧注释若干；`.fx-speed-end-shroud` 等清扫已补）。
- **打包**（Electron/Tauri）未配置；AI 对手远期。
- **已推送**：origin @ `e57b1b4`（同步 0；2026-09-03 推送，含 新10协议已编译特效 `9d80372` + 微调 第二批 `312b18f`/第三批 `048c543`/弹簧线 `4a18d4b` 及各自 docs 提交）。
- **新 10 协议已编译特效已实现（2026-09-03）**：见 §3 已编译小节——老 5 协议编译特效之外，新 10 协议编译后各有专属常驻/周期特效（持久层挂载，不随步骤重置、跟随协议位置；周期大特效 ≥10s 间隔）。观感由用户 5173 确认后可微调（参数集中点见 §3）。
- **2代（MN02 英文版）已并池可玩（2026-09-03，本轮：资源+数据入库见 §9，随后用户拍板「直接并入协议选择池」）**：DEMO 池 = 1代+2代 30 套 / 180 卡（cards.test/mn02-pool 测试覆盖）。**2代 90 张卡效果尚未实现（EFFECTS 未注册）——引擎对未注册卡安全空转（可选链），能选/能打/能编译（分值数据照常）但卡文本无实际效果**；devmode get/compile 检索覆盖两代。UI 取图统一走 `data/demo` 的 `cardImgSrc/protocolImgSrc`（世代扩展名：MN01/AX01=.png、MN02/AX02=.jpg）。草稿池页面随池变大自然滚动。

## 7. 开发流程与交接机制

- **SDD 台账**：`.superpowers/sdd/progress.md`（git 忽略）——每轮合并后追加记录（提交、测试数、Minors）。恢复现场先读台账 + `git log`。
- **子代理驱动**：implementer（自包含 brief，`.superpowers/sdd/task-<n>-brief.md`）+ reviewer（spec+quality 双裁决，`.superpowers/sdd/task-<n>-review.md`），Approved 后合并 main。工具：`task-brief.ps1` / `review-package.ps1`（`.superpowers/sdd/`）。
- **全局记忆**：`E:\studyE\Deepseek memory\compile-web-project.md`（项目外，跨会话持久）。
- **用户偏好**：全程中文；规则细节严格拍板（规则类问题先问用户，**附 FAQ 原文**）；UI 改动一次到位；推送需明确指示；每张卡唯一、卡可易主。
- **本会话已压缩**：历史细节看 `git log --oneline` + SDD 台账。

## 8. 快速自检

- `npm test` → 517 通过（56 文件）；`npm run build` → clean。
- 开发者模式：Ctrl+Shift+P → 密码 `上上下下左右左右BABA` 或 `ssxxzyzybaba` → `get 死1`（给当前玩家手牌加牌）、`clean`、`compile 冷漠`。
- 新 10 协议已编译特效验证：开局后 devmode `compile <协议名>` 强制编译该协议 → 对应协议格出现专属常驻特效；周期大特效（锁链/黑洞/念能爆发/血涌/故障）间隔 ≥10s 需耐心等。观感微调参数：styles.css 末尾 `.compiled-<协议>-*` 块（颜色/时长）+ render.ts 各 builder 顶部常量。
- 修改后必跑：`npx vitest run tests/<file>` → `npm test` → `npm run build` → 提交 → （用户说推送才推送）。

## 9. 2代（MN02 英文版）——资源+数据已入库（2026-09-03，未接玩法，未推送）

- **用户任务**：把 `E:\studyE\compile\正版compile\compile2\英文版` 的协议卡牌与协议加入游戏。问答裁定（2026-09-03）：本轮**只做资源+数据（不接玩法）**，效果后续按协议分任务实现；协议中文名/defId 用现行译名（冰/明镜/和平/混乱/恐惧/明晰/腐化/时间/战争/勇气/幸运/烟雾/同化/多样性/统一；defId=ice/mirror/peace/chaos/fear/clarity/corruption/time/war/courage/luck/smoke/assimilation/diversity/unity，与 1代 零冲突）；**卡文权威源 = `E:\studyE\compile\自制compile\compile1\compile2文本.txt`**（甲x：A/B/C）；拼版内分值升序成立（含组合图三块内部）。
- **资产**：`tools/extract_mn02.py` 把扫描切成 `public/assets/protocols/<defId>/{card-<v>.jpg×6, protocol-loading.jpg, protocol-compiled.jpg}`（120 文件 ≈18.1MB；**JPEG**——1代 为官方 TTS PNG）。网格：单套 3X=2250×2100（2行×3列×750×1050、零留白、行优先升序，左上=最小分值）；`3同化3多样性3统一.jpg`=6750×2100 = **左同化/中多样性/右统一** 三块并排（2行×9列），各块内部同 2行×3列升序。分值集合：冰1-6、明镜0-5、和平1-6、混乱0-5、恐惧0-5、明晰0-5、腐化{0,1,2,3,5,6}、时间0-5、战争0-5、勇气{0,1,2,3,5,6}、幸运0-5、烟雾0-5、同化{0,1,2,4,5,6}、多样性{0,1,3,4,5,6}、统一0-5。尺寸例外：冰 compiled 864×1207、烟雾 loading 1760×2400（原生保留）。
- **数据**：`src/data/cards2.ts`（ALL_PROTOCOLS_2/ALL_CARD_DEFS_2；12 套 MN02 + 3 套 AX02——**AX02 编号为假设**，协议卡面编号待核对；90 卡文本按 compile2文本.txt 无损转写，转写规则/异文注释见文件头）。`src/core/models/types.ts` 的 ProtocolDef.set 联合扩为含 'MN02'|'AX02'。**1代 cards.ts/demo.ts 未动**；src 内无人 import cards2 → 游戏行为零变化（523/523，57 files，build clean）。
- **测试**：`tests/data/cards2.test.ts`（6 条：套数/set 分布、defId 唯一且与 1代 无冲突、每套 6 卡与分值集合、defId 格式、字段完整+无「空」残留/中文数量词/缺句号、资产存在+JPEG 魔数）+ `tests/node-types.d.ts`（项目无 @types/node 且禁 npm install 时的极简 node 声明，仅覆盖测试所用 3 函数）。
- **评审**：spec + quality 双 **Approved**（无必改项；报告 `.superpowers/sdd/task-MN02-assets-data-{spec,quality}-review.md`）。
- **遗留与接线注意（下一阶段必读）**：
  1. **效果实现**：90 卡逐套实现（引擎能力基本够用，需新增机制见下条）+ **世代/池结构**（新游戏选 MN01/MN02 或合并池——2代说明书=英文迷你手册，核心规则与 1代 相同：回合流程/覆盖/编译≥10 且>对手/重编译抽对手牌堆顶/控制组件/胜利条件均一致；差异仅在卡牌效果）。
  2. **接线时 render.ts/effects 硬编码 `.png` 扩展名**需扩展名策略（MN02 资源是 .jpg）。
  3. **术语语义留效果阶段逐张裁决**（txt 旧译，按惯例附原文问用户）：偏转=Shift=平移；阈值=卡牌分值或线总分（视语境）；召回=回手(Return)；中央效果=中部指令；切洗=洗牌。
  4. **异文已注释待卡面/用户定夺**：恐惧1（弃牌数-1 口径）、恐惧4（docx「随机弃」vs txt「抽取对手的牌后弃置」）、镜像2（txt 交换堆叠位置 vs docx 互换任意两栈所有牌）、时间3（txt 2026-09-03 修订为「将其正面朝下打出于其它链路」——「于」照录，若意为「至/在」待效果阶段按卡面确认；docx 作「将其面朝下放入一列中」）。txt 原「空?」（腐化3/时间3/同化0 底部指令存疑）已由用户 2026-09-03 修订为「空」= 确认无底部指令，cards2.ts 已同步。
  5. **卡面观感/裁切对版需用户在 5173 目检**（本会话无 vision，仅几何/格式/幂等核验）。
  6. 显示字段临时值：和平与 AX02 三套 loadingText 缺 docx/PDF 权威源（暂用 txt 标题）；混乱 loadingText 用标准成语「变幻莫测」（docx 作「变换莫测」疑错字）；war commands 取 PDF REACT,DISCARD→反击,弃牌（docx 战争词条疑排版误）。
  7. 官方英文关键词（2代说明书.PDF 加载面）：CHAOS DRAW,REARRANGE,COVERED / CLARITY DRAW,REVEAL / CORRUPTION FLIP,DISCARD / COURAGE DRAW,COMPARE / FEAR SHIFT,DISCARD / ICE SHIFT,PREVENT / LUCK RANDOM,DELETE,PLAY / MIRROR SHIFT,REPEAT / PEACE MUTUAL DISCARD,DRAW / SMOKE FACE-DOWN,SHIFT / TIME DISCARD,TRASH / WAR REACT,DISCARD。
